# API Reference

This reference documents GTKX's public TypeScript libraries. It is generated from the sources, so each signature here is the one your editor resolves. Project-specific libadwaita and GTK4 element references are generated from the GIR libraries on that project's system.
