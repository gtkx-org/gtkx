# API Reference

This reference documents every published package in GTKX's Adwaita-first React stack for GNOME applications. It is generated from the TypeScript sources, so each signature here is the one your editor resolves. Project-specific libadwaita and GTK4 element references are generated from the GIR libraries on that project's system.
