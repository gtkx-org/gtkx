# TODO-RS.md — `packages/native` audit findings

Multi-agent memory-safety and code-quality audit of `packages/native` (audit date: 2026-06-10). Process: 10 independent reviewers produced 83 raw findings, deduplicated to 55, each adversarially verified against the code and the generated bindings. 41 findings confirmed (10 high, 14 medium, 17 low); 14 claims refuted (recorded at the end so they are not re-reported).

**Resolution status (2026-06-11): all 41 findings and RS-T01 are fixed and verified** (cargo clippy `--all-targets` with pedantic+nursery, full xvfb cargo test suite, the aggregate 166-file vitest suite, AddressSanitizer, Miri, and the 100% llvm-cov gate all green). Three findings whose resolution shape differs from the headline prescription carry a `Resolution:` note inline. A follow-up multi-agent adversarial review of the full fix diff surfaced eleven additional defects (mostly in the newly introduced signal-closure and foreign-thread paths: array/hashtable signal parameters, closure thread pinning, a stale depth capture in the foreign-thread wait, deferred toggle-level reapplication, a binding-lookup teardown race, foreign-thread owned-value anchoring, and container-encode unwind leaks); every one is fixed and re-verified under the same gates.

Categories across confirmed findings: 13 logic-bug, 11 memory-leak, 8 non-idiomatic, 5 undefined-behavior, 2 race-condition, 2 use-after-free.

## High severity (10)

- [x] **RS-001** · `src/dispatch.rs:195` · logic-bug — **stop() while frozen deadlocks both threads permanently: run_freeze_loop has no shutdown escape and mark_stopped never notifies freeze_wake**

  run_freeze_loop exits only when freeze_depth reaches zero; it never consults the stopped flag, and mark_stopped wakes wake_js and wake_glib but not freeze_wake. If stop() is dispatched while freeze_depth > 0 (any unbalanced freeze: the React host config calls freeze() in prepareForCommit and unfreeze() in resetAfterCommit, so an error that skips resetAfterCommit leaves depth at 1; or any embedder calling the public freeze() export then stop()), the freeze loop itself drains the stop task — mark_stopped, the context drain, and main_loop.quit() all run inside the loop — and then parks forever in freeze_wake.wait() because unfreeze never arrives. The JS thread receives the stop result, returns from the wait, and blocks in GlibThread::join (state.rs:47-60) waiting for a GLib thread that will never exit. Both threads are parked in native waits: the process hangs, and even SIGINT cannot be serviced because the JS thread is blocked inside pthread_join, so only SIGKILL ends it. The next Ctrl+C/quit after an unbalanced freeze becomes a deterministic hard hang.

  <details><summary>Evidence and verification</summary>

  **Evidence:** dispatch.rs:195-206: `pub fn run_freeze_loop(&self) { self.freeze_loop_active.store(true, Ordering::Release); loop { self.dispatch_pending(); if self.freeze_depth.load(Ordering::Acquire) == 0 { break; } self.freeze_wake.wait(); } ... }` — the only exit condition is freeze_depth == 0. dispatch.rs:161-165: `pub fn mark_stopped(&self) { self.stopped.store(true, Ordering::Release); self.wake_js.notify(); self.wake_glib.notify(); }` — freeze_wake is not notified and the loop condition is unaffected. Trace: JS calls stop() while freeze_depth > 0 → module/stop.rs:42-49 dispatch_to_glib_and_wait pushes the stop task → schedule_glib sees freeze_loop_active and returns after push_glib_task notified freeze_wake (dispatch.rs:214-216, 231-233) → the freeze loop's dispatch_pending runs the stop task (mark_stopped; `while context.iteration(false) {}`; `main_loop.quit()`) → loop iterates: dispatch_pending empty, freeze_depth still > 0, freeze_wake.wait() consumes the earlier notify, loops once, parks with the flag false → parked forever; main_loop.run() never returns because it is below the freeze loop on the stack. JS thread: receives the stop result via wake_js (dispatch.rs:308-310), then module/stop.rs:51 `GlibThread::join()` blocks on the never-exiting thread. Host-config trigger: packages/react/src/host-config.ts:354-368 pairs freeze/unfreeze across prepareForCommit/resetAfterCommit.

  **Verifier:** Every factual claim verifies against the code. dispatch.rs:195-206 run_freeze_loop's only exit condition is freeze_depth == 0; it never consults the stopped flag. dispatch.rs:161-165 mark_stopped notifies wake_js and wake_glib but not freeze_wake. wait_signal.rs:35-47 shows wait() consumes the notified flag, so the freeze_wake notify generated when the stop task was pushed (dispatch.rs:214-216) is consumed by the wakeup that dispatches it; the next freeze_wake.wait() parks with the flag false. After mark_stopped, schedule_glib (dispatch.rs:225-227) drops all tasks before reaching push_glib_task, so no future freeze_wake notify can occur; unfreeze() is the only other notifier and can only come from the JS thread. The stop task in module/stop.rs:42-49 (mark_stopped, context drain, main_loop.quit) runs inside the freeze loop's dispatch_pending; the wrapper in js_bridge.rs:76-81 sends the result and dispatch_pending:308-310 wakes the JS thread, so stop() proceeds to GlibThread::join (state.rs:47-60), which blocks in pthread_join on a GLib thread parked forever in freeze_wake.wait() with run_freeze_loop on the stack above main_loop.run(). Both threads are permanently parked; with lifecycle.ts:78 signal listeners installed, SIGINT is routed through the blocked libuv event loop and cannot be serviced. The trigger is plausible: freeze/unfreeze/stop are public napi exports with no JS-side guard (ffi/src/lifecycle.ts:46, react/src/render.tsx:177), and host-config.ts:354-368 pairs freeze in prepareForCommit with unfreeze in resetAfterCommit exactly as claimed, so any unbalanced freeze (skipped resetAfterCommit, or an embedder calling freeze() then stop()) makes the next quit a deterministic hard hang. This also contradicts the module's documented lifecycle contract (dispatch.rs:35-40 says mark_stopped exists so blocked callers do not deadlock). A genuine high-severity logic defect with a clear fix (check stopped in the loop condition and notify freeze_wake in mark_stopped), not pedantry or style.

  </details>

- [x] **RS-002** · `src/dispatch/js_bridge.rs:156` · race-condition — **wait_for_node_result is unsound for off-GLib-thread trampolines: lost wakeups on shared wake_glib and GTK tasks drained on a foreign thread**

  wait_for_node_result assumes exactly one native thread ever waits on wake_glib (the GLib thread). Nothing in the trampoline path checks or asserts the calling thread, yet trampoline.rs:157 calls invoke_node_and_wait_with_cells from whatever thread the C library invokes the callback on. Libraries the runtime explicitly supports (arbitrary GIR libraries, including WebKit and GIO/GStreamer-style libraries that use worker threads) can emit connected signals or invoke async callbacks off the main thread. When that happens: (1) two threads (the GLib thread parked for one callback, the worker parked for another) wait concurrently on wake_glib, but WaitSignal (src/wait_signal.rs) is a single boolean permit with notify_one — a notification intended for waiter A can be consumed by waiter B, which rechecks its own empty channel and re-parks, leaving A parked with no future wakeup: the GLib main loop hangs indefinitely (app freeze). (2) While parked, the worker runs dispatch_pending_from_depth(callback_depth), which pops queued GLib tasks (GTK widget mutations pushed by the JS callback) and executes them inline on the worker thread — GTK4 API calls off the GLib thread, violating GTK's threading contract (undefined behavior: unsynchronized access to widget/renderer state).

  <details><summary>Evidence and verification</summary>

  **Evidence:** js_bridge.rs:156-172: `fn wait_for_node_result(&self, rx: &mpsc::Receiver<anyhow::Result<NodeCallbackResult>>, callback_depth: usize) -> anyhow::Result<NodeCallbackResult> { loop { self.dispatch_pending_from_depth(callback_depth); match rx.try_recv() { ... Err(mpsc::TryRecvError::Empty) => self.wake_glib.wait(), } } }`. Caller trampoline.rs:157: `let js_result = Mailbox::global().invoke_node_and_wait_with_cells(&self.js_func, values, capture_result, out_cell_indices);` — runs on whichever thread invokes the libffi closure; no thread check exists anywhere on this path. The single-permit signal: wait_signal.rs:24-47 — `notify()` sets one bool and calls `self.condvar.notify_one()`; `wait()` consumes the bool (`*notified = false`). Interleaving that loses a wakeup: GLib thread parked in wait_for_node_result for callback C1; worker-thread signal fires trampoline, pushes C2, parks on the same wake_glib; JS thread finishes C1, `result_tx.send(result)` then `self.wake_glib.notify()` (js_bridge.rs:199-203) — notify_one wakes the worker (wrong waiter), which consumes the flag, finds its own rx Empty, re-parks; the GLib thread, whose rx now holds C1's result, stays parked with the flag false. If no further notify arrives, the GLib thread never resumes. Independently, dispatch_pending_from_depth at js_bridge.rs:162 executes Boxed GLib tasks (dispatch.rs:296 `panic::catch_unwind(AssertUnwindSafe(task))`) on the worker thread — these tasks are GTK mutations pushed by dispatch_to_glib_and_wait, intended exclusively for the GLib thread.

  **Verifier:** Confirmed. The attempted refutation — finding an invariant that trampolines only fire on the GLib thread — fails. (1) No thread check exists on the path: trampoline.rs:157 calls Mailbox::invoke_node_and_wait_with_cells from whatever thread invokes the libffi closure, landing in wait_for_node_result (js_bridge.rs:156-172) which parks on the shared wake_glib. The single-GLib-thread assumption is documented (toggle_ref.rs: "The notify fires on whichever thread performs the ref/unref — the GLib thread in this binding"; multiple "Must run on the GLib thread" safety contracts) but never enforced for trampolines. (2) The trigger is concretely reachable through the default generated public API: node_modules/.gtkx/gi/glib/glib.js:3020/10853 generates GLib.Thread.new/tryNew with t.trampoline(..., scope: "async"), and gio.js:5113/17239 generates Gio.Task.runInThread/runInThreadSync — g_thread_new invokes its trampoline on the newly spawned thread and g_task_run_in_thread on a GIO pool worker; glib and gio are unconditional members of every app's generated store, and @gtkx/config additionally supports libraries: "*". (3) The lost-wakeup interleaving is valid: WaitSignal (wait_signal.rs:24-47) is a single boolean permit with notify_one; with the GLib thread and a worker both parked, the notify after C1's result (js_bridge.rs:199-203) can be consumed by the worker (which re-parks on its empty rx), and the notify after C2 can wake the worker again, leaving the GLib thread parked forever with its result unread — the only other wake_glib.notify sources (push_glib_task, mark_stopped) never fire in an idle app whose main loop is itself parked, so the freeze is indefinite. (4) The foreign-thread drain is valid: the parked worker runs dispatch_pending_from_depth(callback_depth) (js_bridge.rs:162) against the single global callback_depth counter (dispatch.rs:114), so GTK mutations enqueued at depth >= 1 by any concurrent JS callback are executed inline on the worker (dispatch.rs:279-301), violating GTK4's threading contract and the crate's own GLib-thread safety contracts. One caveat: the finding's named examples are weak — WebKitGTK signals and GIO GAsyncReadyCallbacks are main-context-bound (init.rs runs the default main context on the GLib thread) — but the core defect stands with a stronger, directly generated trigger (GLib.Thread.new / Gio.Task.runInThread), so the unsound-contract/high classification holds.

  </details>

- [x] **RS-003** · `src/ffi/value.rs:107` · logic-bug — **FfiValue::as_raw_ptr returns the payload pointer for Storage and panics for Trampoline, corrupting memory for Ref<HashTable> and panicking on Ref<Trampoline>**

  as_raw_ptr has inconsistent indirection semantics across variants: for numeric variants and Ptr it returns the address of the stored value (a slot the callee can write through), but for Storage it returns `storage.ptr()` — the payload pointer itself. Its only caller, RefType::encode's fallthrough arm (types/ref_type.rs:100-107), encodes the inner value and uses as_raw_ptr as the address of the out-parameter slot passed to C. The fallthrough arm is reached for every inner type not explicitly handled, which includes Type::HashTable and Type::Trampoline. For `t.ref(t.hashtable(...))` with a non-empty value, HashTableType::encode creates a live GHashTable and stores it as FfiStorage whose ptr IS the GHashTable*; as_raw_ptr hands that GHashTable* to the callee as if it were a GHashTable** slot, so the callee's out-write overwrites the first 8 bytes of the live GHashTable struct, and FfiStorage::drop then calls g_hash_table_unref on the corrupted table — heap corruption / undefined behavior. For `t.ref(t.trampoline(...))` with a callback value, the encoded inner is FfiValue::Trampoline and as_raw_ptr hits the `unreachable!` arm — the 'unreachable' claim is false — panicking the GLib thread on JS-controlled input; the panic is caught by dispatch_pending's catch_unwind, the result sender is dropped, and the blocked JS caller receives the misleading "GLib thread disconnected" while the already Box::into_raw'd TrampolineState leaks. This is an unsound contract of the public napi call API: a well-formed descriptor shape (GHashTable** out-params exist in GIR) silently corrupts memory instead of erroring. The conversion should be fallible or RefType::encode should reject these inner types at the descriptor-parsing boundary like can_be_return_type does for return slots.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/ffi/value.rs:94-115: `pub fn as_raw_ptr(&self) -> *mut c_void { match self { Self::U8(value) => value as *const u8 as *mut c_void, ... Self::Ptr(ptr) => ptr as *const *mut c_void as *mut c_void, /* address of the slot */ Self::Storage(storage) => storage.ptr(), /* the payload pointer itself */ Self::Trampoline(_) => { unreachable!(...) } } }`. Caller, types/ref_type.rs:100-107 (fallthrough `_` arm): `let ref_arg = Arg::new(*self.inner_type.clone(), *ref_val.value.clone()); let ref_value = Box::new(ffi::FfiValue::try_from(ref_arg)?); let ref_ptr = ref_value.as_raw_ptr();`. Ownership trace for Ref<HashTable>: HashTableType::encode returns FfiStorage::new(hash_table as *mut c_void, FfiStorageKind::HashTable(...)) where ptr is the GHashTable* (types/hashtable.rs:194-200) → as_raw_ptr returns that same GHashTable* → passed to the C function as the out-slot → callee performs `*out = result_table` writing 8 bytes over the GHashTable header → on scope exit FfiStorage::drop runs g_hash_table_unref(data.handle) on the clobbered table (src/ffi/storage.rs:214-218). For Ref<Trampoline>: src/types.rs:343 parses "ref" with any innerType including "trampoline"; FfiValue::try_from yields FfiValue::Trampoline (TrampolineType::encode at types/trampoline.rs:103 accepts Value::Callback) and as_raw_ptr executes the unreachable!() panic, which unwinds out of the task closure before tx.send, so wait_for_glib_result sees Disconnected and returns GlibDisconnectedError (src/dispatch/js_bridge.rs:101).

  **Verifier:** Every central claim checks out against the current code.

1. Inconsistent indirection in as_raw_ptr (src/ffi/value.rs:94-115): the ten numeric arms return `value as *const T as *mut c_void` and `Ptr(ptr)` returns `ptr as *const *mut c_void` — the address of the field inside the boxed FfiValue, i.e. a writable slot — while `Storage(storage) => storage.ptr()` returns the payload pointer itself, and `Trampoline(_)` executes `unreachable!(...)`. Its only production caller is the fallthrough `_` arm of RefType::encode (src/types/ref_type.rs:99-107), which boxes the encoded inner value to keep it alive and uses `ref_value.as_raw_ptr()` as the out-parameter slot address handed to C (the outer `FfiValue::Storage` is passed via `libffi::arg(storage.ptr_ref())` at value.rs:180, so the callee receives ref_ptr as its pointer argument). Slot semantics are correct for Integer/Float/Tagged/Boolean/Unichar inners, and the decode side (ref_type.rs:142-153) reads back through storage.ptr() accordingly — but break for Storage-encoding inners.

2. Ref<HashTable> reaches the fallthrough: the explicit arms cover only Boxed/Struct/GObject/Fundamental, Array, and String (ref_type.rs:47-98). Type parsing accepts "ref" with any innerType (src/types.rs:343; RefType::from_js_value at ref_type.rs:27-32 performs no validation), and can_be_return_type (types.rs:363-365) guards only the return slot (module/call.rs:150). HashTableType::encode with a non-empty tuple array builds a live GHashTable and returns FfiStorage whose ptr IS the GHashTable* (src/types/hashtable.rs:172-200, with `should_free: self.ownership.is_borrowed()` — and Ownership defaults to Borrowed). as_raw_ptr therefore hands the GHashTable* to the callee as if it were a GHashTable** slot; the callee's out-write clobbers the first pointer-sized field of the table header, and FfiStorage::drop then runs g_hash_table_unref on the corrupted table (src/ffi/storage.rs:214-218, 245-248). Heap corruption / UB, exactly as claimed.

3. Ref<Trampoline> panic: TrampolineType::encode accepts Value::Callback and returns FfiValue::Trampoline (src/types/trampoline.rs:103-170), so as_raw_ptr hits the `unreachable!` — the unreachability claim is false, and the path is constructible from JS via the public `call` export because Value::from_js_value parses `{ value: function }` into Value::Ref with inner Value::Callback (src/value.rs:281-292). The panic occurs on the GLib thread inside the dispatched task (handler.rs:27-30 → js_bridge.rs:66-83), unwinds before tx.send, is caught by dispatch_pending's catch_unwind (src/dispatch.rs:296-301), and the dropped sender makes wait_for_glib_result return GlibDisconnectedError, surfacing the misleading "GLib thread disconnected" (js_bridge.rs:99-104, dispatch.rs:322-326). The TrampolineState leak on unwind holds for the Notified/Forever/Async scopes (Box::into_raw at trampoline.rs:130/136/145, no reclaim on panic); for the default Call scope the owned box is dropped — a slight overstatement that does not affect the core defect.

4. Not caught by existing tooling: no test constructs RefType over HashTable or Trampoline (grep across src/ and tests/ shows only Integer/Float/Tagged/String/Array/GObject/Boolean inners), and tests/ffi_value.rs even pins the current behaviors (as_raw_ptr_storage_returns_storage_ptr, as_raw_ptr_trampoline_panics), so ASan/Miri default paths never exercise the corruption. GHashTable** parameters do exist in shipped GIR (4 occurrences in /usr/share/gir-1.0/Gio-2.0.gir, GMenuModel get_item_attributes/get_item_links vfuncs), and the codegen wraps any out parameter in `t.ref(...)` unconditionally (packages/codegen/src/writers/value.ts:186); those particular occurrences are vfuncs, so the common generated call path likely does not emit Ref<HashTable> today — which tempers exploitability but not the contract unsoundness: a well-formed, parser-accepted descriptor silently corrupts memory, and a JS-controlled input panics the GLib thread through a falsely-labeled unreachable arm. The proposed remedy (reject unsupported inner types at the descriptor boundary, mirroring can_be_return_type, or make the pointer conversion fallible) is the right shape of fix. High severity is justified under the rubric's "unsound public contract" clause.

  </details>

- [x] **RS-004** · `src/module/toggle_ref.rs:190` · use-after-free — **get_wrapper can call napi_get_reference_value on a napi_ref deleted by a concurrent toggle cleanup**

  get_wrapper reads the wrapper's raw napi_ref address on the GLib thread, then dereferences it on the JS thread with napi_get_reference_value. Between those two steps the deferred toggle-ref cleanup for a just-collected wrapper can delete that exact napi_ref, because the JS thread's mailbox wait loop executes pending node tasks (including the OP_DELETE reference operation) BEFORE it consumes the dispatch result. napi_delete_reference frees the underlying v8impl::Reference allocation, so the subsequent napi_get_reference_value dereferences freed memory — a cross-thread use-after-free in the engine that can crash or corrupt the Node heap. The window is exactly the documented 'rebind window' (object handed back between wrapper collection and teardown), which the design treats as a supported state and the toggle-rebind tests show is an expected production state. The code only guards the benign weak-ref-resolves-to-null case, not deletion.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/module/toggle_ref.rs:190-201: `let ref_addr: usize = Mailbox::global().dispatch_to_glib_and_wait(*env, move || unsafe { toggle_ref::wrapper_ref(gobject_addr as *mut _) as usize })...; if ref_addr != 0 { let raw_ref = ref_addr as sys::napi_ref; let mut raw_value: sys::napi_value = std::ptr::null_mut(); unsafe { sys::napi_get_reference_value(env.raw(), raw_ref, &mut raw_value) };`. Interleaving: (1) wrapper W1 of object O is GC'd; on_wrapper_finalize posts cleanup C via glib::idle_add_once (DEFAULT_IDLE, src/toggle_ref.rs:354). (2) JS calls get_wrapper(O); the dispatch task runs first on the GLib thread at HIGH_IDLE (src/dispatch.rs:235) while qdata is still set, and sends back the collected wrapper's ref_addr; wake_js is notified. (3) Before the JS thread is scheduled, the GLib main loop dispatches C: the generation matches → clears qdata → invoke_ref_op(ref_ptr, OP_DELETE) (src/toggle_ref.rs:375) → pushes a node task and parks in wait_for_node_result. (4) The JS thread wakes inside wait_for_glib_result, whose loop runs process_node_pending BEFORE rx.try_recv (src/dispatch/js_bridge.rs:96-104); it executes the toggle callback → apply_wrapper_ref_op (src/module/toggle_ref.rs:95-97) → napi_delete_reference frees the Reference. (5) try_recv then yields the stale non-zero ref_addr and get_wrapper calls napi_get_reference_value on the freed reference.

  **Verifier:** The finding stands. The claimed interleaving is reachable and every candidate refutation fails on the actual code. (1) get_wrapper (module/toggle_ref.rs:190-199) reads the raw napi_ref address on the GLib thread via dispatch_to_glib_and_wait, then dereferences it on the JS thread with napi_get_reference_value — a two-step read with no liveness guarantee across the gap. (2) The JS thread's wait loop (dispatch/js_bridge.rs:96-104) calls process_node_pending BEFORE rx.try_recv, so a queued OP_DELETE node task executes before the stale ref_addr is consumed; since cleanup C pushes that task strictly after T1 already sent the result (both run sequentially on the GLib thread), whenever the delete runs in that loop the very next try_recv returns the stale nonzero address and get_wrapper dereferences the freed reference. (3) GLib source priorities favor the dangerous ordering: the dispatch idle runs at HIGH_IDLE (dispatch.rs:235) while schedule_cleanup's idle_add_once (toggle_ref.rs:354) runs at default-idle, so T1 reads the still-set qdata ref even when C is already queued, and C fires microseconds later on the same thread while the JS thread is still waking from the WaitSignal condvar. (4) The generation guard does not reject the teardown: get_wrapper never bumps generation (only install via setWrapper does, and registry.ts:214-228 calls setWrapper only after getWrapper returns), so C takes the match path — clear qdata, then invoke_ref_op(ref_ptr, OP_DELETE) (toggle_ref.rs:370-375). (5) The installed toggle callback is exactly applyWrapperRefOp (packages/ffi/src/registry.ts:14), and apply_wrapper_ref_op OP_DELETE calls sys::napi_delete_reference (module/toggle_ref.rs:95-97); the wrapper's finalizer already ran (it scheduled C), so the v8impl::Reference is freed immediately, and napi_get_reference_value performs no validation — use after free per the Node-API contract. (6) The state is a documented, test-exercised production state: the rebind window (toggle_ref.rs:47-56, tests/module/toggle-rebind.test.ts). The only guard (line 200 null-value check) covers a live weak ref resolving to null, not a deleted ref — line 199's call is itself the UAF. The teardown-serialization invariant (toggle_ref.rs:36-45) serializes notify vs cleanup on the GLib thread but does not cover get_wrapper's cross-thread address handoff. High severity is correct: a real cross-thread engine UAF requiring the JS thread to lose a microsecond-scale wakeup race during the supported collection-to-teardown window.

  </details>

- [x] **RS-005** · `src/toggle_ref.rs:282` · use-after-free — **Deferred wrapper cleanup can complete between decode/lookup and install, making install() steal a native holder's reference or operate on a freed GObject**

  install()'s fresh-bind branch unconditionally releases "the single pending owned reference the decode path left" (the trailing g_object_unref at line 310). But the decision of whether decode leaves a pending reference is made one or more GLib-thread hops earlier, in types/gobject.rs tracked_gobject_value, based on has_wrapper() at decode time. A wrapper teardown queued by schedule_cleanup runs as a separate DEFAULT_IDLE source and can execute between the decode and the install dispatch, clearing qdata and removing the toggle ref. install then sees an empty qdata slot, takes the fresh-bind path, and unrefs a reference that was never left pending — stealing a reference owned by another native holder (a container, a list store). The object is later finalized while that holder still points at it, producing a use-after-free in GTK. If the toggle ref was the object's last reference, the cleanup finalizes the object before install even runs, and install itself then dereferences freed memory in binding_ptr/is_gobject and refs/qdata-writes/unrefs a dangling pointer. The window is real: mailbox tasks run at Priority::HIGH_IDLE (dispatch.rs:235) while the cleanup idle runs at DEFAULT_IDLE, and the JS registry (packages/ffi/src/registry.ts resolveWrapper) performs three separate round trips (getWrapper, getInstanceGType, setWrapper) between decode and install. The rebind invariant documented at lines 47-56 covers cleanup-vs-cleanup and notify-vs-cleanup ordering, but the decode->JS->install sequence is not atomic with respect to the cleanup idle.

  <details><summary>Evidence and verification</summary>

  **Evidence:** install() lines 280-311: `let existing = binding_ptr(gobject); let result = if existing.is_null() { ... g_object_add_toggle_ref(...); (cell_ptr as usize, 1) } else { ... }; glib::gobject_ffi::g_object_unref(gobject); result`. Decode side (types/gobject.rs:64-77): when has_wrapper is true, no pending reference is left (borrowed decode takes NO ref; full decode UNREFS). Cleanup path (toggle_ref.rs:362-384): clears qdata then `g_object_remove_toggle_ref(gobject, ...)`, which finalizes O when the toggle ref was its last holder. Stolen-reference interleaving: (1) object O sits in a GListStore (refcount: store 1 + toggle 1 = 2), wrapper A bound at generation 1; (2) JS GC collects A; the napi finalizer runs and schedule_cleanup posts a DEFAULT_IDLE task C1 (toggle_ref.rs:354 glib::idle_add_once); (3) before C1 runs, JS calls store.get_item(0) (transfer-full): decode sees has_wrapper==true and releases the returned ref (refcount back to 2); (4) JS calls getWrapper — the stale napi_ref resolves to null, so the registry starts building wrapper B; (5) the GLib loop goes idle and runs C1: generation matches, qdata cleared, napi_ref A deleted, g_object_remove_toggle_ref (refcount 2 -> 1); (6) setWrapper dispatches install: binding_ptr is null -> fresh-bind path -> g_object_add_toggle_ref (1 -> 2) then the trailing g_object_unref (2 -> 1) releases the store's reference and fires on_toggle_notify(is_last_ref=true), weakening wrapper B; (7) B is collected, its cleanup removes the toggle ref, refcount hits 0 and O is finalized while still an item of the store; the next store access touches freed memory. Freed-object variant: when O's only holder is its toggle ref, the cleanup at step 5 finalizes O outright, and set_wrapper's install task then runs on the dangling pointer — binding_ptr → is_gobject dereferences freed memory, and the fresh-bind path refs/qdata-writes/unrefs it.

  **Verifier:** The core claim stands: the decode->JS->install sequence is not atomic with respect to the deferred cleanup idle, and a matching-generation cleanup landing in that window breaks install()'s fresh-bind contract or runs the wrapper machinery on a finalized GObject. Verified chain: (a) resolveWrapper (packages/ffi/src/registry.ts:213-231) performs three separate GLib round trips (getWrapper, getInstanceGType, setWrapper), each dispatched as a one-shot idle at Priority::HIGH_IDLE (dispatch.rs:235), while schedule_cleanup posts its teardown via glib::idle_add_once at DEFAULT_IDLE (toggle_ref.rs:354) — between round trips the main loop has no HIGH_IDLE source ready and freely dispatches the cleanup; (b) in that window no rebind has run, so the cleanup's generation guard matches (toggle_ref.rs:363) and the FULL teardown executes: qdata cleared, g_object_remove_toggle_ref (toggle_ref.rs:372-382), finalizing the object when the toggle ref was its last reference; (c) install (toggle_ref.rs:281-310) then finds null qdata, takes the fresh-bind branch, and its unconditional trailing g_object_unref (line 310) consumes a "pending owned reference" that the decode path (types/gobject.rs:64-69, has_wrapper branch) never left — the decision was made one or more hops earlier. The documented invariants (toggle_ref.rs:36-80) cover notify-vs-cleanup, cleanup-vs-cleanup, and cleanup-AFTER-rebind (generation mismatch), but not cleanup-between-decode-and-install. One important correction, though: the finding's lead "stolen reference via GListStore" trace is impossible and that variant is refuted by the strengthen invariant — while any native holder (the store) holds the object, the 1→2 refcount crossing fired on_toggle_notify(is_last_ref=false) synchronously (toggle_ref.rs:230-245 via invoke_node_and_wait; registry.ts:14 applyWrapperRefOp -> napi_reference_ref), making the wrapper's napi_ref strong and pinning it against V8 GC; "JS GC collects A" while O sits in a store cannot happen, so install can never steal a persistent holder's reference. However, the freed-object variant the finding also asserts IS reachable: it requires exactly the state in which the wrapper is legitimately weak (refcount 1, toggle only) plus a weak-pointer re-acquisition path, and the generated bindings expose such paths — Binding.dupSource/dupTarget and BindingGroup.dupSource (node_modules/.gtkx/gi/gobject/gobject.js:2613-2620, 2692), backed by GWeakRef which holds no strong reference. Concrete trigger: create O (refcount 1, wrapper weak), bindProperty to get Binding B, drop O's wrapper, GC collects it and the finalizer posts cleanup C1 (DEFAULT_IDLE) while the loop is busy; call b.dupSource() — dispatched at HIGH_IDLE it outranks C1, g_weak_ref_get refs O 1->2 (strengthen lands on the dead napi_ref, no resurrection), decode's has_wrapper branch unrefs back to 1, and a non-owning borrowed_gobject handle (managed.rs:126-132) crosses to JS; in a gap among resolveWrapper's round trips C1 dispatches, generation matches, toggle ref removed, O finalized at refcount 0; getInstanceGType then dereferences the freed instance (module/gobject.rs:27-28) and install refs/qdata-writes/unrefs the dangling pointer. ASan/Miri cannot see this interleaving on default test paths. The defect is real (UAF on a plausible if timing-dependent path through public generated API); only the GListStore steal scenario in the evidence is wrong.

  </details>

- [x] **RS-006** · `src/types.rs:228` · non-idiomatic — **RawPtrCodec trait, numeric pointer helpers, and defer_oneshot_free are safe APIs that dereference or free arbitrary raw pointers, lint-suppressed instead of marked unsafe**

  *Resolution:* the `RawPtrCodec` trait methods, `FfiEncoder::ref_for_transfer`, the numeric `read_ptr`/`write_ptr`/`read_slice` helpers, `defer_oneshot_free`, and `FieldLocation::resolve` are all `unsafe fn` with `# Safety` contracts, and every `#[allow(clippy::not_unsafe_ptr_arg_deref)]` in `src/` is gone. Two read-only refcount probes in `tests/common/mod.rs` keep their allows: they have over a hundred call sites across seven test files and dereference nothing the tests do not own.

  The public RawPtrCodec trait declares safe methods (read_from_raw_ptr, ptr_to_value, write_value_to_raw_ptr, write_return_to_raw_ptr) whose every implementation dereferences a caller-supplied raw pointer, and the inherent helpers IntegerKind::read_ptr/write_ptr/read_slice and FloatKind::read_ptr/write_ptr do the same. Safe code can invoke Type::read_from_raw_ptr(garbage, ...) and reach undefined behavior with zero `unsafe` at the call site, so the public contract is unsound. The codebase is aware of the issue and silences clippy at three sites (`#[allow(clippy::not_unsafe_ptr_arg_deref)]` on FundamentalType::ref_for_transfer, the ArrayType::ptr_to_value impl, and defer_oneshot_free) instead of marking the functions unsafe and documenting their contracts. defer_oneshot_free is the starkest case: a safe fn taking `*mut TrampolineState` that schedules Box::from_raw on it — calling it twice with the same pointer, or with any pointer not produced by Box::into_raw, is a double free / arbitrary free reachable from safe code, with the guarding invariant (the AtomicPtr::swap in handle_call) living entirely outside the function, while the neighboring TrampolineState::destroy performing the identical Box::from_raw is correctly declared `pub unsafe extern "C" fn` with a documented safety contract. FieldLocation::resolve compounds this by computing base.add(offset) (which requires the result to stay in-allocation) on a JS-supplied base+offset pair inside a safe fn. Idiomatic shape: declare the trait methods (or the whole trait) unsafe fn with # Safety sections, or accept NonNull/typed wrappers whose construction is the audited unsafe step.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/types.rs:233-235 default impl: `fn read_from_raw_ptr(&self, ptr: *const c_void, context: &str) -> anyhow::Result<value::Value> { let inner_ptr = unsafe { *(ptr as *const *mut c_void) }; ... }` — a safe trait method dereferencing its raw-pointer argument. src/types/numeric.rs:33-39: `pub fn read_ptr(self, ptr: *const u8) -> f64 { unsafe { ... ptr.cast::<$ty>().read_unaligned() ... } }` — safe pub fn, same for write_ptr (41-48) and read_slice (55-66). Suppressions instead of unsafe contracts: src/types/fundamental.rs:80, src/types/array.rs:144, src/trampoline.rs:236 all carry #[allow(clippy::not_unsafe_ptr_arg_deref)]. src/trampoline.rs:236-241: `#[allow(clippy::not_unsafe_ptr_arg_deref)] fn defer_oneshot_free(state_ptr: *mut TrampolineState) { glib::idle_add_local_once(move || { drop(unsafe { Box::from_raw(state_ptr) }); }); }` — contrast src/trampoline.rs:100-107 where TrampolineState::destroy is correctly `pub unsafe extern "C" fn` with a documented safety contract. src/module/field.rs:49-54: `fn resolve(&self) -> anyhow::Result<*mut c_void> { ... Ok(unsafe { (self.base_addr as *mut u8).add(self.offset) as *mut c_void }) }`. Trigger: any safe caller (in-crate or downstream, the crate is a pub library with `pub mod types`) passing a stale or misaligned pointer gets UB with no unsafe block to audit.

  **Verifier:** Every factual claim verifies against the current code, and the pattern is genuinely non-idiomatic by Rust soundness conventions the crate itself follows elsewhere.

Accuracy: (1) /home/eugenio/gtkx/packages/native/src/types.rs:228-256 — RawPtrCodec is a safe trait whose default read_from_raw_ptr does `let inner_ptr = unsafe { *(ptr as *const *mut c_void) };` (line 234) and whose default write_return_to_raw_ptr writes through `ret` (line 244); the crate is built as rlib+cdylib (Cargo.toml `crate-type = ["cdylib", "rlib"]`) with `pub mod types` in lib.rs, so `Type::read_from_raw_ptr(garbage, ...)` is callable from safe code with zero unsafe at the call site and is UB on an invalid pointer. (2) src/types/numeric.rs:33-47, 55-66 (IntegerKind::read_ptr/write_ptr/read_slice) and 279-295 (FloatKind::read_ptr/write_ptr) are safe `pub fn`s doing unguarded `read_unaligned`/`write_unaligned`/`from_raw_parts` on the argument, with no `# Safety` docs. (3) All three cited `#[allow(clippy::not_unsafe_ptr_arg_deref)]` suppressions exist: src/types/fundamental.rs:80 (ref_for_transfer), src/types/array.rs:144 (RawPtrCodec::ptr_to_value impl), src/trampoline.rs:236 (defer_oneshot_free). (4) src/trampoline.rs:237-241 — defer_oneshot_free is a safe fn scheduling `Box::from_raw(state_ptr)` on an idle source; its only guard (the AtomicPtr::swap to null at trampoline.rs:148-152 ensuring single consumption) lives in handle_call, entirely outside the function. (5) src/module/field.rs:49-54 — FieldLocation::resolve computes `(self.base_addr as *mut u8).add(self.offset)` in a safe fn on a JS-supplied base+offset; `pointer::add` requires the result to stay within the allocation, so this is itself UB-capable from safe code.

Why it is a real non-idiomatic finding and not pedantry: the crate demonstrably knows and applies the correct idiom for identical operations — IntegerKind::call_cif_raw (numeric.rs:76-93) and FloatKind::call_cif_raw (numeric.rs:317-336) are `unsafe fn` with `# Safety` sections; TrampolineState::destroy (trampoline.rs:100-107) is `pub unsafe extern "C" fn` with a documented contract performing the very same Box::from_raw that defer_oneshot_free performs behind a safe signature; ArrayType has a documented `pub unsafe fn ptr_to_value` (array.rs:1020-1022) that the safe RawPtrCodec impl at array.rs:144-151 re-exposes as a safe method with a lint suppression directly above it — the unsafe contract is laundered into a safe interface at the trait boundary. Suppressing clippy::not_unsafe_ptr_arg_deref (a correctness lint) instead of propagating unsafety is exactly the anti-pattern the lint documents. This is not something rustfmt/clippy already governs on default paths, because the lint is deliberately silenced; and it undermines the auditability the project's own ASan/Miri CI depends on, since UB-capable operations are no longer findable by grepping for unsafe at call sites. The trigger scenario is concrete: any safe in-crate or rlib-consumer call with a stale/misaligned pointer (e.g. Type::read_from_raw_ptr, IntegerKind::write_ptr, or a second defer_oneshot_free on the same pointer) is immediate UB with no unsafe block to audit. Minor overstatement only: "every implementation" of the trait dereferences — the default ptr_to_value/write_value_to_raw_ptr bail instead — but the substantive impls (numeric, tagged, fundamental, array) and the two dereferencing defaults make the core claim stand. The unsound-safe-contract characterization fits the stated rubric for a high-rated non-idiomatic finding.

  </details>

- [x] **RS-007** · `src/types/array.rs:235` · undefined-behavior — **Transfer-full zero-terminated array args ignore container ownership: callee frees the Rust Vec buffer, then FfiStorage frees it again**

  NullTerminatedArrayEncoder ignores the container's Ownership entirely (the parameter is named `_ownership` and never consulted), unlike GListEncoder/GSListEncoder/GArray/GByteArray encoders, which all gate freeing on `should_free = ownership.is_borrowed()`. For an array argument described as `{kind:"array", ownership:"full"}` (GIR transfer-full or transfer-container zero-terminated array in-param, e.g. g_environ_setenv's envp), the callee takes ownership of the buffer and will g_free/g_strfreev it — but that buffer is a Rust `Vec<*mut c_void>` allocation owned by `FfiStorageKind::StringArray`/`ObjectArray`, whose Drop also frees it. The result is a foreign free of a Rust allocation (allocator-contract UB) followed by a second free when the FfiValue drops after the call. With non-duped elements (`dup_elements == false`), the callee additionally frees the CString element allocations that the Vec<CString> drop frees again. The sibling encoders show transfer-full containers are an expected descriptor shape; only the plain-array path mishandles it.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Lines 236-261 (encode_strings): `fn encode_strings(&self, cstrings: Vec<CString>, dup_elements: bool, _ownership: Ownership) -> ... { let mut ptrs: Vec<*mut c_void> = ...; ptrs.push(std::ptr::null_mut()); let ptr = ptrs.as_ptr() as *mut c_void; Ok(ffi::FfiValue::Storage(FfiStorage::new(ptr, FfiStorageKind::StringArray(cstrings, ptrs))))}` — `_ownership` unused; identical in encode_handles (lines 263-284). Contrast GListEncoder line 296 (and 323/353/380/625/755): `let should_free = ownership.is_borrowed();` which correctly leaks the GLib-allocated list to a transfer-full callee. Call sequence: CallRequest::execute (module/call.rs:62-92) encodes args, performs the call (callee frees the transfer-full buffer with g_free, hitting the Vec's heap block), then ffi_values drop and FfiStorage::drop (src/ffi/storage.rs:266-267, StringArray/ObjectArray arms) lets the Vec<*mut c_void>/Vec<CString> destructors free the same memory a second time through Rust's allocator.

  **Verifier:** The finding stands; every candidate defense was checked and refuted. (1) The trigger descriptor is emitted by codegen and present in the generated bindings as in-parameters: packages/codegen/src/writers/value.ts:30-34,446 maps GIR transfer full/container to `t.array(element, "array", "full")`, and node_modules/.gtkx/gi/glib/glib.js contains `{ type: t.array(t.string("full"), "array", "full"), optional: true }` as arg 0 of g_environ_setenv (line 4448), g_environ_unsetenv (line 4454), and g_strfreev (line 5231), with public passthrough wrappers environSetenv (line 12731) and strfreev (line 13920) that perform no copying. (2) The Rust path mishandles it exactly as claimed: ArrayType::encode (packages/native/src/types/array.rs:571-575) selects NullTerminatedArrayEncoder (line 557) whose encode_strings (lines 236-261) takes `_ownership: Ownership` and never consults it; the container passed to the callee (via FfiValue::Storage -> storage.ptr_ref(), packages/native/src/ffi/value.rs:180) is `ptrs: Vec<*mut c_void>`, a Rust-allocator heap block recorded in FfiStorageKind::StringArray. (3) Concrete trigger: a JS call to GLib.strfreev(["a"]) makes g_strfreev g_free the Rust Vec buffer (after freeing the g_strdup'd elements), and GLib.environSetenv(envp, "K", "V", true) makes g_environ_setenv g_realloc (g_renew) the Rust Vec buffer; g_environ_unsetenv returns the same buffer which the transfer-full return decode (decode_null_terminated_string_array, array.rs:965-972) g_strfreev's. In all cases, when ffi_values drops at the end of CallRequest::execute (packages/native/src/module/call.rs:62-99), FfiStorage::drop's StringArray arm (packages/native/src/ffi/storage.rs:266) does no GLib free but the Vec destructors still deallocate the same block via Rust's allocator — a foreign free of a Rust allocation followed by a double free, which is UB. (4) The strongest defense — that the ownership field on args describes only decode/return semantics — is refuted by the sibling encoders in the same file (GListEncoder array.rs:296/323, GSListEncoder 353/380, encode_gbytearray 625, encode_garray 755), which all gate freeing on `ownership.is_borrowed()` during encode, demonstrating encode-side container-transfer handling is the established contract for the same field; those encoders also show the correct strategy (GLib-allocated container, leaked to a transfer-full callee), which the plain-array path cannot follow because its container is a Rust Vec. (5) CI tools cannot see it: the ASan/Miri-covered marshalling tests never call these strv-consuming GLib functions. One refinement, not a refutation: with the descriptors codegen actually emits for transfer-full strv (item ownership "full"), elements are g_strdup'd (dup_elements=true), so the element-level double free only occurs for transfer-container shapes (dup_elements=false) — the finding stated this conditionally and correctly; the container double free is unconditional. Severity "high" (UB via unsound public contract on a plausible, user-reachable path; not the common widget path) is accurate.

  </details>

- [x] **RS-008** · `src/types/array.rs:1014` · memory-leak — **Transfer-full Sized/Fixed array return values never free the container buffer**

  decode_sized_array decodes the elements but never consults self.ownership and never frees the buffer. Every other transfer-full container decode in this file frees its container (decode_null_terminated_ptr_array g_frees at 944-946, decode_garray unrefs at 871-876, decode_glist frees at 849-855, decode_gptrarray unrefs at 897-899). For length-prefixed arrays returned as out-parameters the wrapper RefType::decode_with_context compensates (ref_type.rs:213-221 g_frees the PtrStorage target), but for a sized array in the function's direct return slot there is no compensation: decode_with_context (lines 800-818) routes straight to decode_sized_array. Every call to a GIR function returning a transfer-full array sized by an out parameter — common GLib APIs such as g_base64_decode (returns guint8* with '(array length=out_len) (transfer full)') and g_convert — leaks the entire returned allocation on the happy path, once per invocation.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Lines 1014-1018: `fn decode_sized_array(&self, ptr: *mut c_void, length: usize) -> anyhow::Result<value::Value> { let codec = self.item_codec("sized array")?; let values = self.decode_contiguous(codec, ptr.cast::<u8>(), length)?; Ok(value::Value::Array(values)) }` — no `if self.ownership.is_full() { g_free(ptr) }`. Call chain for a return value: CallRequest::execute (src/module/call.rs:96) → Value::from_ffi_value_with_args(&result, &self.result_type, ...) → ArrayType::decode_with_context line 800 `ArrayKind::Sized { size_index } => { let length = Self::size_from_args(...)?; ... return self.decode_sized_array(*ptr, length); }` → buffer leaked. The Ref out-param path proves intent to free: ref_type.rs:213-221 `if matches!(storage.kind(), FfiStorageKind::PtrStorage(_)) && array_type.ownership.is_full() && matches!(&array_type.kind, ArrayKind::Sized { .. } | ArrayKind::Fixed { .. }) { unsafe { glib::ffi::g_free(actual_ptr) }; }` — that free exists only on the out-param path, not the return path.

  **Verifier:** Confirmed as a real high-severity per-call leak; every refutation attempt fails. (1) The trigger is reachable from generated bindings: codegen's arrayExpression (packages/codegen/src/writers/value.ts:439-441) emits t.sizedArray(..., "full") for return types, and the generated store contains exactly that — node_modules/.gtkx/gi/glib/glib.js:4206-4209 declares g_base64_decode with return type t.sizedArray(t.uint8, 1, "full"). (2) Type::can_be_return_type (src/types.rs:363-365) permits Array returns. (3) ArrayType's FfiEncoder impl (src/types/array.rs:122-126) does not override call_cif, so the trait default (src/types.rs:194-203) returns a bare FfiValue::Ptr to the g_malloc'd buffer; FfiValue::Ptr (src/ffi/value.rs:21) has no Drop — the only Drop is on FfiStorage. (4) ArrayType::decode_with_context (src/types/array.rs:800-818) routes a Sized/Fixed Ptr return to decode_sized_array (lines 1014-1018), which copies elements and returns Value::Array without ever reading self.ownership or calling g_free — in contrast to decode_glist (849-855), decode_garray (871-876), decode_gptrarray (897-899), decode_null_terminated_ptr_array (944-946), and decode_null_terminated_string_array (965-972), which all free transfer-full containers. (5) The compensating g_free in RefType::decode_with_context (src/types/ref_type.rs:213-221) is gated on FfiStorageKind::PtrStorage, i.e. the out-parameter path only; the direct return slot has no equivalent, and the raw pointer never reaches JS, so nothing downstream can release it. The leaking path is also invisible to CI: the only sized-array-return test (src/module/call.rs:244-258) uses Ownership::Borrowed and expects a decode error, so ASAN/Miri never exercise a transfer-full sized return. Net effect: every call to a GIR function returning a length-out, transfer-full array (g_base64_decode, g_convert family, etc.) leaks the entire returned allocation on the happy path.

  </details>

- [x] **RS-009** · `src/types/ref_type.rs:100` · undefined-behavior — **C callee writes out-parameters through pointers derived from shared references (RefType encode fallthrough and null_ptr_storage)**

  Both out-parameter slot constructors hand the C callee a *mut pointer derived from a shared (&T) borrow of Rust-owned memory. The fallthrough arm of RefType::encode (every Ref<Integer/Float/Tagged/Boolean/Unichar> — the hot path of every scalar out-param call such as gtk_widget_measure) calls `ref_value.as_raw_ptr()`, which takes `&self` and returns `value as *const $ty as *mut c_void` pointers into the boxed FfiValue payload; null_ptr_storage casts `Box::as_ref()` (a `&*mut c_void`) to `*const` then `*mut`. Under Rust's aliasing model (Stacked/Tree Borrows) those pointers carry read-only provenance, so the callee's write into the Box allocation is UB even though it happens in foreign code — the allocation and pointer provenance are fully Rust-managed, and the compiler is entitled to assume the pointee is unchanged across the call, which would let the post-call read in decode be folded to the pre-call value. Additionally, the Box is moved by value after the pointer is derived (`FfiStorageKind::BoxedValue(ref_value)` / `FfiStorageKind::PtrStorage(ptr_storage)`), which under Stacked Borrows as implemented by Miri retags the Box and invalidates the derived pointer even for the subsequent read-back. CI cannot observe this: ci-miri.sh runs only the FFI-free miri_marshalling target, and the offending write happens inside the dlopen'd C library. The fix is mechanical: derive the pointer mutably (&mut/addr_of_mut! on the boxed payload, or UnsafeCell) after the storage reaches its final location.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/types/ref_type.rs:100-107 (fallthrough `_` arm of RefType::encode): `let ref_arg = Arg::new(*self.inner_type.clone(), *ref_val.value.clone()); let ref_value = Box::new(ffi::FfiValue::try_from(ref_arg)?); let ref_ptr = ref_value.as_raw_ptr(); Ok(ffi::FfiValue::Storage(FfiStorage::new(ref_ptr, FfiStorageKind::BoxedValue(ref_value))))` — as_raw_ptr(&self) (src/ffi/value.rs:94-106) does `Self::I32(value) => value as *const i32 as *mut c_void`, deriving *mut from &FfiValue; the Box is then moved into the enum constructor AFTER derivation. Same pattern at src/types/ref_type.rs:233-239 (null_ptr_storage): `let ptr_storage: Box<*mut c_void> = Box::new(std::ptr::null_mut()); let ptr = ptr_storage.as_ref() as *const *mut c_void as *mut c_void;` — as_ref() yields `&*mut c_void` (shared), then the Box moves into FfiStorageKind::PtrStorage. Trigger chain: JS calls e.g. gtk_widget_measure with `{type:'ref', innerType:{type:'int32'}}` args → CallRequest::execute (src/module/call.rs:62-92) encodes via this branch → `libffi::arg(storage.ptr_ref())` (src/ffi/value.rs:180) passes the derived pointer as `int *minimum` → GTK writes through it (write through SharedReadOnly-provenance pointer = UB) → collect_ref_updates reads it back via `int_type.read_ptr(storage.ptr())` (ref_type.rs:142-144), a read through a pointer invalidated by the Box move under Stacked Borrows.

  **Verifier:** Confirmed. The trace holds end to end: generated bindings emit ref descriptors for out-params (packages/ffi/src/helpers.ts:119 refT, used by codegen writers/param-classify.ts), so RefType::encode's fallthrough arm (ref_type.rs:100-107) is the hot path for every scalar out-param call. Inner scalar encoders produce inline payloads (FfiValue::I32 at types/boolean.rs:14, numeric.rs:305), so as_raw_ptr (ffi/value.rs:94-106) derives `value as *const i32 as *mut c_void` from `&self` — a shared borrow of the boxed payload with no UnsafeCell — and null_ptr_storage (ref_type.rs:233-239) derives from Box::as_ref() the same way. That pointer is what libffi hands the callee (call.rs:74-75, value.rs:180 `libffi::arg(storage.ptr_ref())`), and the callee writes through it by out-param contract; decode reads it back (ref_type.rs:139, 142-153 via call.rs:94 collect_ref_updates). Every refutation attempt fails: (a) "no live reference at write time" does not help because the pointer's provenance is SharedReadOnly/Frozen under both Stacked and Tree Borrows, and std's documented contract for &-derived pointers (as_ptr family: memory must never be written through any pointer derived from it) forbids the write regardless of reference liveness — which is exactly why the adjacent Ref<String> arm correctly uses buffer.as_mut_ptr() (ref_type.rs:93) while these two slots do not; (b) "foreign code is outside the AM" fails under the accepted opsem/Miri-native-lib model where foreign accesses carry the passed pointer's provenance; (c) CI genuinely cannot see it: scripts/ci-miri.sh runs only tests/miri_marshalling.rs (ArrayType decode only), ASan does not model aliasing, and rustc's invalid_reference_casting fires only on Rust-visible writes; (d) no mutable re-derivation happens between encode and the call (append_libffi_args takes &self). Two sub-claims are overstated — today's compiler cannot actually fold the post-call read (no noalias/readonly scope spans the opaque libffi call once as_raw_ptr returns), and the Box-move retag-invalidating-the-read claim depends on Miri retag-fields configuration — but these do not undermine the primary defect: handing C a write target whose provenance derives from &T is UB under both candidate aliasing models Rust requires unsafe code to satisfy, on a common path, with the mechanical fix of deriving the pointer mutably after the storage reaches its final location. High severity is appropriate (unsound contract/UB on a plausible path, not a demonstrated miscompile).

  </details>

- [x] **RS-010** · `src/types/ref_type.rs:158` · logic-bug — **RefType::decode has no Boolean/Unichar arms, breaking generated out-param bindings**

  RefType::encode accepts Ref<Boolean> and Ref<Unichar> through its default branch (lines 99-108: any inner type not matched earlier is encoded via Arg::new + FfiValue::try_from into a BoxedValue slot), but RefType::decode's match (lines 137-162) only handles GObject/Boxed/Fundamental/Struct, Integer, Tagged, Float, String, and Array. Boolean and Unichar fall into the final arm and bail with 'Unsupported ref inner type for reading'. Generated bindings emit exactly these descriptors: node_modules/.gtkx/gi/gtk/gtk.js line 2511 has `{ type: t.ref(t.boolean) }` for g_action_group_query_action, and pango.js line 1275 has `{ type: t.ref(t.unichar) }` for pango_layout_set_markup_with_accel (also in gio, glib, gdk, gtksource, javascriptcore). The decode failure happens in CallRequest::collect_ref_updates AFTER the native call already executed, so the whole call surfaces as an error despite having run, and for g_action_group_query_action the transfer-full GVariant out-params the callee already wrote are never decoded and leak. The trampoline path is unaffected (seed_ref_cell and flush_out_cells use read_from_raw_ptr/write_value_to_raw_ptr, which Boolean and Unichar implement); the gap is exclusively the call-path FfiDecoder::decode.

  <details><summary>Evidence and verification</summary>

  **Evidence:** ref_type.rs:158-161: `_ => bail!("Unsupported ref inner type for reading: {:?}", self.inner_type)`. Encode path, ref_type.rs:99-107: `_ => { let ref_arg = Arg::new(*self.inner_type.clone(), *ref_val.value.clone()); let ref_value = Box::new(ffi::FfiValue::try_from(ref_arg)?); ... FfiStorageKind::BoxedValue(ref_value) }` — Boolean encodes to FfiValue::I32, Unichar to FfiValue::U32, so encoding succeeds and the C function runs. Call sequence: module/call.rs execute() line 89 runs the FFI call, then line 94 `collect_ref_updates` → line 118 `Value::from_ffi_value_with_args(&ffi_values[i], &arg.ty, ...)` → Type::Ref dispatch → RefType::decode_with_context (ref_type.rs:190-227, non-array falls through to `self.decode(ffi_value)` at line 226) → bail at line 158 → execute() returns Err after the side effects already happened. Generated trigger: gtk.js:2508-2515 `const g_action_group_query_action = t.fn(..., [{ type: t.object("borrowed") }, { type: t.string("borrowed") }, { type: t.ref(t.boolean) }, ...])`.

  **Verifier:** Every factual claim checks out against the code. RefType::decode (packages/native/src/types/ref_type.rs:137-162) handles GObject/Boxed/Fundamental/Struct, Integer, Tagged, Float, String, and Array inner types but has no arm for Type::Boolean or Type::Unichar (separate enum variants per src/types.rs:270/279), so they hit the `_ => bail!("Unsupported ref inner type for reading")` arm at line 158. The encode side is asymmetric: the default branch at ref_type.rs:99-108 encodes Ref<Boolean>/Ref<Unichar> via BooleanType::encode → FfiValue::I32 (boolean.rs:14) and UnicharType::encode → FfiValue::U32 accepting Value::Number (unichar.rs:13-17), with as_raw_ptr (ffi/value.rs:100-101) providing the writable out-slot — so encoding succeeds and the native function runs. Generated bindings exercise exactly this: gtk.js:2511 `t.ref(t.boolean)` for g_action_group_query_action (wrapper queryAction at gtk.js:19964 seeds `{ value: false }`, which from_js_value turns into Value::Ref per value.rs:285-292), and pango.js:1275/1570/1619/1638 `t.ref(t.unichar)` (setMarkupWithAccel seeds `{ value: 0 }`, encodable as Number); ref(boolean) also appears in glib, gdk, gio, gtksource, javascriptcore. The ordering claim is correct: CallRequest::execute (module/call.rs:89-94) runs call_cif before collect_ref_updates, which dispatches Type::Ref → RefType::decode_with_context (ref_type.rs:190-227) → non-array falls to self.decode at line 226 → bail. So the call errors after side effects executed, and the two transfer-full GVariant out-params of queryAction (gtk.js:2514-2515) are written by the callee but never decoded, leaking a reference on every invocation. The trampoline-path exemption is also accurate: RefType implements RawPtrCodec::read_from_raw_ptr (ref_type.rs:176-187) and Boolean/Unichar implement the raw-ptr codecs. CI tooling cannot see this: collect_ref_updates is coverage(off) because Value::Ref needs a live JS runtime, so cargo test/ASan/Miri never reach the decode gap. This is a genuine logic defect making a class of generated public methods always fail at write-back, with a leak in the transfer-full case; high severity is justified.

  </details>

## Medium severity (14)

- [x] **RS-011** · `src/ffi/storage.rs:282` · undefined-behavior — **Inout array buffers are written by the callee through Vec::as_ptr-derived (read-only provenance) pointers**

  All numeric/float FfiStorage constructors record the buffer address with `vec.as_ptr() as *mut c_void` — a mutable pointer derived from a shared borrow of the Vec. For plain in-arguments this pointer is only read, which is fine; but for Ref<Array> inout parameters the native callee writes elements into the buffer through this pointer, which is undefined behavior under both Stacked Borrows and Tree Borrows (write through a pointer with shared/read-only provenance, no UnsafeCell). The decode path then reads the C-written contents back. The crate already demonstrates the correct idiom one layer up: the Ref<String> buffer at src/types/ref_type.rs:93 uses `buffer.as_mut_ptr()`. The Vec is owned (`from(vec: Vec<$ty>)`), so switching to `as_mut_ptr()` on a `mut vec` binding is a zero-cost fix. Miri cannot catch this because the write is performed by the foreign callee, which the miri_marshalling CI target cannot execute.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/ffi/storage.rs:279-308: `impl From<Vec<$ty>> for FfiStorage { fn from(vec: Vec<$ty>) -> Self { Self { ptr: vec.as_ptr() as *mut c_void, kind: FfiStorageKind::$vec_variant(vec) } } }` (and the f32/f64 impls at lines 292-308). Trigger chain: JS passes `{type:'ref', innerType:{type:'array', itemType:{type:'int32'}, kind:'sized', ...}}` with a non-empty array → RefType::encode Type::Array branch (src/types/ref_type.rs:56-63) → ArrayType::encode → IntegerKind::to_ffi_storage → this From impl → CallRequest passes `storage.ptr()` as the `int *data` argument → the C function fills the caller-allocated buffer (write through read-only-provenance pointer = UB) → RefType::decode_with_context (src/types/ref_type.rs:196-211) reads the elements back via decode_sized_array.

  **Verifier:** The finding stands, with one mechanism correction. Trigger chain verified end to end: RefType::encode Type::Array branch (src/types/ref_type.rs:56-63) with a non-empty array delegates to ArrayType::encode (src/types/array.rs:560-570), which reaches IntegerKind::to_ffi_storage (src/types/numeric.rs:68-74) and the From<Vec<$ty>> impls at src/ffi/storage.rs:279-308 that record `ptr: vec.as_ptr() as *mut c_void`. The libffi conversion (src/ffi/value.rs:180, `FfiValue::Storage(storage) => libffi::arg(storage.ptr_ref())`) hands that exact pointer to the C callee, which for a Ref<Array> inout parameter writes elements into the buffer; RefType::decode_with_context (src/types/ref_type.rs:196-211) then re-reads the callee-written contents via decode_sized_array — the design explicitly expects the foreign write. Attempted refutation: std's Vec::as_ptr is guaranteed not to materialize a reference (stable 1.95 source, alloc/src/vec/mod.rs:1867-1870), so the returned pointer carries the allocation's full read-write provenance and writes through it are accepted by Miri under both implemented Stacked Borrows and Tree Borrows — the finding's claim of "shared/read-only provenance" UB under those models is operationally inaccurate. But this defense fails to refute the conclusion: the same documentation block (alloc/src/vec/mod.rs:1863-1865) normatively requires "the memory the pointer (non-transitively) points to is never written to (except inside an UnsafeCell) using this pointer or any pointer derived from it. If you need to mutate the contents of the slice, use as_mut_ptr." The foreign write violates this documented precondition of an std API — library-level UB that std reserves the right to exploit under future aliasing models, and the unsafe FFI code in this crate is responsible for upholding it. No invariant excludes the trigger: the napi call API is public and any GIR inout sized/fixed array with initial JS contents reaches this path. CI cannot see it: scripts/ci-miri.sh runs only the miri_marshalling target (no foreign callee), and clippy's as_ptr_cast_mut lint targeting this pattern is nursery, outside the default -D warnings set. The crate already uses the correct idiom for its other callee-writable buffer (buffer.as_mut_ptr() at src/types/ref_type.rs:93), and the From impls own the Vec by value, so switching to as_mut_ptr on a mut binding is zero-cost. Medium severity is right: contract-level UB on the inout edge path, no demonstrated miscompilation on current compilers since no Rust references to the elements are live across the call.

  </details>

- [x] **RS-012** · `src/managed/boxed.rs:144` · logic-bug — **Boxed::clone silently degrades from deep copy to non-owning alias for free_fn and gtype-less values, violating the Clone contract**

  NativeHandle::clone is documented as "duplicating the underlying NativeValue when owned" (managed.rs:85), but Boxed::clone fulfills that contract only for gtype-carrying values. For a custom-destructor boxed (free_fn set, e.g. a cairo_path_destroy-owned pointer) and for any owned gtype-less boxed (from_glib_full(None, ptr) struct transfers, or memcpy-copied from_glib_none_with_size values), clone returns owned:false with the same pointer — an alias whose validity silently depends on the source wrapper outliving it. The moment the owning original drops (free_fn(ptr) or g_free(ptr) on the GLib thread), the clone dangles, and any later as_ptr()/ptr() use is a use-after-free. Every current clone site (extract_handles and encode_handles in types/array.rs, decode_storage's ObjectArray readback, Ref::clone via ref_type.rs) happens to clone only borrowed handles built by Value::from_js_value, so no dereference of a dangling alias is reachable today; but the type derives Clone transitively through NativeValue (managed.rs:183) and Value (value.rs:200), so any new call path that clones an owned handle inherits the hazard invisibly, with no compiler or runtime diagnostic. The underlying modeling issue is that Boxed { owned, gtype: Option, free_fn: Option } makes destructor selection a product of three independent fields with invalid combinations representable; an explicit destructor enum plus an explicit alias()/borrow constructor would make the aliasing-clone case impossible to express accidentally.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Clone lines 144-159: `if self.free_fn.is_some() { return Self { ptr: self.ptr, owned: false, gtype: None, free_fn: self.free_fn }; } self.gtype.map_or_else(|| Self { ptr: self.ptr, owned: false, gtype: None, free_fn: None }, |gt| { ... g_boxed_copy ... })` — both fallback arms alias self.ptr without taking ownership, while Drop (lines 175-193) frees the source pointer via free_fn or g_free. Contract being violated: managed.rs:84-92 "Clones the handle, duplicating the underlying [NativeValue] when owned." Ownership trace for the hazard: owned Boxed B (free_fn) inside NativeHandle H -> H.clone() yields H' sharing B.ptr with owned:false -> H drops on the GLib thread -> free_fn(B.ptr) runs -> H'.ptr() now returns a freed pointer to any consumer. Propagation: BoxedType::decode with free_fn and Ownership::Full produces an owned custom-destructor Boxed (types/boxed.rs:104-105); #[derive(Clone)] on NativeValue (managed.rs:183) and Value (value.rs:200) propagate the aliasing clone.

  **Verifier:** Every factual claim verifies against the code. The documented contract at /home/eugenio/gtkx/packages/native/src/managed.rs:85 ("Clones the handle, duplicating the underlying [NativeValue] when owned") is violated by Boxed::clone at /home/eugenio/gtkx/packages/native/src/managed/boxed.rs:144-159: the free_fn arm returns `Self { ptr: self.ptr, owned: false, ... }` and the gtype-less arm likewise aliases self.ptr with owned:false, while Drop (boxed.rs:175-194) frees that same pointer via free_fn(ptr) or g_free when the owning original drops — so a clone of an owned custom-destructor or gtype-less boxed dangles as soon as the source is dropped, and as_ptr()/ptr() are safe methods that return the freed pointer. The hazardous owned states are genuinely producible: types/boxed.rs:104-105 (Boxed::from_glib_full_with_free_fn for Ownership::Full free_fn boxeds, e.g. cairo_path_destroy), types/boxed.rs:262 (Boxed::from_glib_full(None, ptr) for full-transfer structs), and boxed.rs:83-94 (owned memcpy copies). The clone-site inventory is also accurate: managed.rs:97, types/array.rs:473, types/array.rs:1002, value.rs:174, and types/ref_type.rs:100 are the only NativeHandle/Value clone paths, and all operate on encode-side values built by Value::from_js_value (value.rs:276-279), which produces borrowed handles with inner: None — so Boxed::clone is unreachable today and no live UAF exists, exactly as the finding concedes. The defect is still real: a Clone impl on a Drop-freeing resource wrapper that yields a lifetime-unlinked alias violates Clone's semantic contract and the type's own documentation, and #[derive(Clone)] on NativeValue (managed.rs:183) and Value (value.rs:200) propagates the hazard invisibly, leaving safety dependent on an unwritten invariant that owned handles are never cloned. This squarely matches the audit's "unsound API contract / ownership mistake" target, is not something clippy or rustfmt governs, and medium severity is appropriate given the absence of a currently reachable trigger. (Supporting observation: even the gtype deep-copy arm is inconsistent at the handle level — NativeHandle::clone at managed.rs:93-99 keeps ptr: self.ptr while inner owns a fresh g_boxed_copy, so the clone's ptr() reports the original allocation.)

  </details>

- [x] **RS-013** · `src/module/alloc.rs:40` · logic-bug — **alloc silently downgrades boxed allocations to plain-struct semantics when the GType is not yet registered**

  *Resolution:* the registration-timing dependence is removed by deferring the decision to release time. `Boxed::from_alloc` binds boxed semantics immediately when the name already resolves, and otherwise records the name (`BoxedDestructor::GBoxedFreeByName`) and retries `Type::from_name` at drop — by then the type has registered iff anything ever used it, so a `GValue` gets `g_boxed_free` (running `g_value_unset`) while a plain C struct name that never names a GType gets `g_free`. A loud failure was rejected because generated bindings legitimately pass non-GType struct names (`cairo_matrix_t`, `GtkBorder` before GTK init) on hot paths.

  AllocRequest::execute resolves the JS-supplied type_name with glib::Type::from_name, which only finds types whose get_type has already run. If the type is not yet registered, the result is silently None and the allocation is wrapped as a plain struct (freed with g_free, cloned as a non-owning alias in Boxed::clone) instead of failing or registering the type. The same JS call therefore produces different ownership semantics depending on whether anything happened to register the GType first — timing-dependent behavior. The broader contract is also unvalidated: size is uncorrelated with the named type (a short alloc makes any later g_boxed_copy read out of bounds), and Drop runs the type's registered boxed free function on g_malloc0'd zeroed memory, which is undefined for boxed types whose free function is not a plain deallocation (e.g. refcounted boxed types such as GskTransform, where g_boxed_free → unref underflows a zero refcount).

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/module/alloc.rs:33-43: `let ptr = unsafe { g_malloc0(self.size) }; ... let gtype = self.type_name.as_ref().and_then(glib::Type::from_name); let boxed = Boxed::from_glib_full(gtype, ptr); Ok(NativeValue::Boxed(boxed).into())`. Ownership trace: Boxed::from_glib_full stores owned=true with the resolved gtype (src/managed/boxed.rs:18-25); Drop (src/managed/boxed.rs:175-193) calls g_boxed_free(gtype, ptr) when Some — invoking the type's registered free function on memory the type's own allocator never produced — and g_free when the name lookup silently failed. Boxed::clone (src/managed/boxed.rs:153-171) calls g_boxed_copy with the recorded gtype, reading the full registered struct size from an allocation whose length was an arbitrary JS-supplied f64 cast with `size as usize` (src/module/alloc.rs:63).

  **Verifier:** The central claim is accurate and traceable to a concrete trigger. src/module/alloc.rs:40 resolves the JS-supplied type name solely with glib::Type::from_name, which returns None for any GType not yet registered; the None is silently absorbed and Boxed::from_glib_full wraps the g_malloc0 allocation with plain-struct semantics (Drop: g_free at boxed.rs:187; Clone: non-owning alias at boxed.rs:153-159) instead of boxed semantics (Drop: g_boxed_free at boxed.rs:184; Clone: owned g_boxed_copy at boxed.rs:160-171). Generated bindings feed real lazily registered boxed GType names into this path on common constructors — alloc(16, "GdkRGBA") (gdk.js:2914), alloc(16, "GrapheneRect") (graphene.js:2673), alloc(80, "GtkTextIter") (gtk.js:16403) — and the marshalling hot path allocates every GValue via alloc(GVALUE_SIZE, "GValue") (packages/ffi/src/gobject/gvalue-native.ts:169). Whether "GValue" or "GdkRGBA" resolves depends entirely on whether anything previously called the type's get_type, so identical JS calls produce different drop and clone semantics: an unregistered-GValue drop skips g_value_unset and leaks held string/object contents, and clones of unregistered boxed structs are pointer aliases (reachable via NativeHandle::clone at managed.rs:93-99 and array encoding at types/array.rs:473). The crate itself proves this is a defect, not a design choice: BoxedType::gtype() (src/types/boxed.rs:67-77) handles the same from_name miss by resolving and calling the get_type symbol from the library and reporting an error on failure — the alloc path has neither fallback nor diagnostics. One tail sub-claim is overstated: the GskTransform refcount-underflow is not reachable from generated code because the generated constructor passes size 0 (alloc(0, "GskTransform"), gsk.js:2193) and g_malloc0(0) returns NULL so execute bails; that and the short-alloc OOB read are unvalidated-contract observations (size as usize at alloc.rs:63, uncorrelated with the named type) without an in-repo trigger. The core finding stands as a genuine medium logic bug: silent, registration-timing-dependent ownership semantics on common allocation paths.

  </details>

- [x] **RS-014** · `src/module/call.rs:62` · memory-leak — **Transfer-full argument encodes (g_strdup, g_boxed_copy, g_object_ref, ref_fn) leak on every call-setup failure because encoding runs before symbol resolution**

  Encoders for Ownership::Full arguments pay the ownership transfer upfront — StringType g_strdup's, BoxedType makes a g_boxed_copy, GObjectType takes an extra g_object_ref via ref_for_transfer, FundamentalType calls the ref function — and return the result as a bare FfiValue::Ptr, which owns nothing and has no Drop (FfiStorage::drop only releases list/array/hashtable kinds). Ownership of the copy/reference is transferred only if the native call actually happens. In CallRequest::execute, argument encoding runs BEFORE library/symbol resolution and before call_cif: if a later argument fails to encode, if the library or symbol fails to resolve (typo, older GTK), or if call_cif bails, the `?` drops the Vec<FfiValue> and every transfer-full side effect leaks. Generated bindings do pass transfer-full string and object in-args (`{ type: t.string("full") }` appears in gtk.js, gsk.js, soup.js, glib.js), so a misspelled symbol or any bad sibling argument leaks one allocation per attempt; a leaked g_object_ref is particularly damaging because the GObject (e.g. a widget) can never be finalized. The design fix is type-level: ownership taken during encode should live in an owning storage variant (as FfiStorageKind already does for arrays with should_free) that releases on Drop and is disarmed only when the call succeeds.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Ordering in src/module/call.rs:62-87: `let ffi_values = self.args.iter().enumerate().map(|(i, arg)| arg.ty.encode(...)).collect::<anyhow::Result<Vec<ffi::FfiValue>>>()?;` (lines 62-71, aborts on the first failing arg, leaking earlier encoded ones), then `GlibThreadState::with(|state| { let library = state.library(&self.library_name)?; let symbol = library.get(...)?; ... })?` (lines 78-87) can fail with every transfer-full arg already encoded. Transfer sites returning non-owning Ptr: src/types/string.rs:35-37 `let glib_ptr = unsafe { glib::ffi::g_strdup(cstring.as_ptr()) }; Ok(ffi::FfiValue::Ptr(glib_ptr as *mut c_void))`; src/types/boxed.rs:147-149 `g_boxed_copy(...); return Ok(ffi::FfiValue::Ptr(copied));`; src/types/gobject.rs:99-112 `Ok(ffi::FfiValue::Ptr(self.ref_for_transfer(ptr)?))` where ref_for_transfer adds a reference; src/types/fundamental.rs:70-77 ref_fn. FfiValue has no Drop impl (ffi/value.rs). Trigger: `call('libglib-2.0.so.0', 'g_no_such_symbol', [{type:{type:'string',ownership:'full'}, value:'x'}], {type:'void'})` strdups the string, fails symbol lookup, and the duplicate is never freed.

  **Verifier:** Verified against the code; no refuting invariant exists. Ordering: /home/eugenio/gtkx/packages/native/src/module/call.rs encodes all args at lines 62-71 ("arg.ty.encode(&arg.value, arg.optional)" collected with `?`), then resolves library/symbol at lines 78-87, then calls at 89-92 — every `?` between encode and a successful call drops the Vec<FfiValue> with no release of transfer side effects. Transfer-full encoders pay ownership upfront and return a non-owning FfiValue::Ptr: src/types/string.rs:36-37 g_strdup; src/types/boxed.rs:147-149 g_boxed_copy; src/types/gobject.rs:98-112 ref_for_transfer adds a g_object_ref (from_glib_none + into_glib_ptr); src/types/fundamental.rs:70-77 ref_fn. FfiValue has no Drop (src/ffi/value.rs), and FfiStorage::drop (src/ffi/storage.rs:245-274) frees only GList/GSList/GArray/GByteArray/HashTable/StringGList kinds — never a bare Ptr. Error paths are recoverable: ModuleRequest::dispatch (src/module/handler.rs:27-38) converts execute() failures into napi exceptions and the process continues; the crate's own tests exercise unknown-symbol and encode-failure errors. Triggers are reachable: Arg parsing does not type-check values (test at call.rs:202-216 shows mismatches fail only at encode), and CString::new at string.rs:34 fails on an interior NUL — so a call whose first arg is transfer-full (strdup'd/boxed-copied) and whose second arg fails to encode leaks the copy; likewise any symbol/library resolution failure. Generated bindings do pass transfer-full in-args: grep of node_modules/.gtkx/gi finds `{ type: t.string("full") }` in-args at gobject.js:457, gsk.js:936, soup.js:716, glib.js:2817/2986, gtk.js:11802, and 55 transfer-full boxed in-args (e.g. gtk_css_section_unref takes t.boxed("GtkCssSection", "full", ...)). One overstatement: no generated in-arg uses t.gobject("full"), so the leaked-GObject-ref scenario requires a hand-built descriptor via the public call API (still allowed by contract); the string/boxed leaks are reachable from generated bindings with bad user input. Medium severity (leak on recoverable error path) is accurate.

  </details>

- [x] **RS-015** · `src/module/call.rs:94` · memory-leak — **Out-parameter GObject pending refs leak when return-value decoding or JS conversion fails after collect_ref_updates**

  Decoding a GObject without an existing wrapper takes a pending g_object_ref (transfer-none) or keeps the caller's ref (transfer-full) that is only released when JavaScript later calls setWrapper and toggle_ref::install consumes it. In CallRequest::execute, ref updates for Ref-typed out-parameters are decoded before the return value; if the return decode then fails, the RefUpdate values — including Value::Object handles whose decode took that pending ref — are dropped (NativeHandle::borrowed_gobject has no Drop), and the ref leaks, keeping the GObject alive forever. The same protocol leaks in Mailbox::execute_callback when converting a later callback argument to JS fails after an earlier GObject argument was decoded with a pending ref.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Order in execute(): `let ref_updates = self.collect_ref_updates(&ffi_values)?;` (line 94: decodes out-params, may take g_object_ref) then `let return_value = Value::from_ffi_value_with_args(&result, &self.result_type, ...)?;` (lines 96-98: failure drops ref_updates). The pending ref is taken in tracked_gobject_value (src/types/gobject.rs:76): `unsafe { glib::gobject_ffi::g_object_ref(gobject_ptr) };` with the doc chain in src/toggle_ref.rs (install, lines 253-259) confirming the decode path leaves 'exactly one pending owned reference' that only install() releases. NativeHandle::borrowed_gobject (src/managed.rs:126-132) carries no owning state, so dropping the Value releases nothing. Concrete trigger: a call with a Ref<GObject> out-parameter and a unichar return type where the native function returns an invalid codepoint — UnicharType::decode (src/types/unichar.rs:40-41) bails 'Invalid Unicode codepoint', the ref_updates vector is dropped, and the out-param object's pending ref leaks. Parallel path: Mailbox::execute_callback (src/dispatch/js_bridge.rs:251-265) converts trampoline args with `?`; a conversion failure drops already-decoded GObject args that hold pending refs from GObjectType::ptr_to_value (src/types/gobject.rs:139-143, Ownership::Borrowed).

  **Verifier:** The finding stands. The pending-ref protocol is explicitly documented in the code: tracked_gobject_value (src/types/gobject.rs:52-82) leaves exactly one owned reference on any unwrapped GObject (g_object_ref at line 76 for Borrowed; retained caller ref for Full), and the only release point is the g_object_unref at src/toggle_ref.rs:310 inside install(), reached solely when JS calls set_wrapper (src/module/toggle_ref.rs:161-165) — which @gtkx/ffi's resolveWrapper (packages/ffi/src/registry.ts:213-231) does only for handles that actually arrive in JS. The decoded Value::Object carries NativeHandle::borrowed_gobject (src/managed.rs:126-132) with inner: None, so Drop (managed.rs:158-162) is a no-op; dropping the Value releases nothing, and no Rust-side pending-ref bookkeeping exists. In CallRequest::execute (src/module/call.rs:94-98), collect_ref_updates decodes Ref out-params (RefType::decode at src/types/ref_type.rs:138-140 → GObjectType::decode → tracked_gobject_value, taking the pending ref) BEFORE the return-value decode; if that decode fails, execute returns Err and the RefUpdate vector is dropped, leaking the ref permanently. The trigger is reachable: the crate's own test execute_fails_when_return_value_cannot_be_decoded (call.rs:233-258) shows post-call decode failure, and data-dependent failures exist (UnicharType::decode bails on invalid codepoints, unichar.rs:40-41; load_type_class bails on null type class, gobject.rs:26-29). The same leak occurs via the ? at call.rs:123 when a later out-param decode fails after an earlier one succeeded, and in Mailbox::execute_callback (src/dispatch/js_bridge.rs:251-265), where GObject trampoline args decoded with pending refs (GObjectType::ptr_to_value, gobject.rs:139-143, Ownership::Borrowed) are dropped if a later arg conversion or out-cell wrap fails. Every candidate refutation fails: install is the documented sole consumer; borrowed handles have no owning Drop; only shutdown leaks are documented as intentional; has_wrapper short-circuiting protects only already-tracked objects, while fresh out-param objects always carry the pending ref. Medium severity (leak on an error/edge path) is the correct rating.

  </details>

- [x] **RS-016** · `src/toggle_ref.rs:230` · race-condition — **Toggle notify firing on a non-GLib thread races the GLib-thread teardown and uses a freed napi_ref**

  The teardown serialization invariant (module doc, lines 36-45) holds only because on_toggle_notify and the cleanup both run on the GLib thread. GLib gives no such guarantee: the toggle notify fires on whichever thread performs the boundary-crossing ref/unref, and libraries reachable through the generated bindings drop references on internal threads. GIO's GTask (behind every *_async API) can drop its final task reference on the worker-pool thread when the main-context completion callback finishes first; g_task_finalize then unrefs the task's source object on the pool thread. GDBus releases connection and message references on its private worker thread. If such an unref crosses the toggle boundary of a JS-wrapped object concurrently with that object's scheduled cleanup, on_toggle_notify reads the napi_ref from qdata before the cleanup clears the slot, the cleanup's OP_DELETE then frees the napi_ref on the JS thread, and the worker's invoke_ref_op makes apply_wrapper_ref_op call napi_reference_ref/unref on freed memory. A second consequence: invoke_node_and_wait called from the worker parks it in wait_for_node_result, whose dispatch_pending_from_depth drains GLib-bound mailbox tasks — executing queued GTK mutations on a foreign thread.

  <details><summary>Evidence and verification</summary>

  **Evidence:** on_toggle_notify lines 230-245: `let ref_ptr = unsafe { wrapper_ref(gobject) }; if ref_ptr.is_null() { return; } ... invoke_ref_op(ref_ptr, op);` — the null check and the use of ref_ptr are not atomic with respect to the cleanup running on another thread. schedule_cleanup match path (lines 370-383) clears qdata, then `invoke_ref_op(ref_ptr, OP_DELETE)` which executes napi_delete_reference (module/toggle_ref.rs:96), freeing the heap-allocated ref. Interleaving: worker thread executes g_object_unref crossing 2 -> 1 -> on_toggle_notify on the worker -> wrapper_ref returns the still-installed napi_ref; concurrently the GLib thread runs the pending cleanup for that same binding -> OP_DELETE frees the napi_ref on the JS thread; the worker then calls invoke_ref_op(ref_ptr, OP_WEAKEN) -> Mailbox::invoke_node_and_wait -> apply_wrapper_ref_op -> sys::napi_reference_unref(env, raw_ref, ...) on freed memory. The module doc itself states the assumption it cannot enforce: "The notify fires on whichever thread performs the ref/unref — the GLib thread in this binding" (lines 23-24).

  **Verifier:** The finding stands; no invariant refutes it. (1) The teardown serialization in toggle_ref.rs (doc lines 36-45) depends on on_toggle_notify and the scheduled cleanup both running on the GLib thread, but on_toggle_notify (toggle_ref.rs:230-245) has no thread guard, and GLib 2.88.1's own g_object_add_toggle_ref documentation (verified in /usr/share/gir-1.0/GObject-2.0.gir) explicitly warns that an unref on another thread can invoke the notify there and that the binding "must take care of that yourself". (2) The foreign-thread crossing is reachable through this binding's generated surface: node_modules/.gtkx/gi contains gio, soup, and webkit namespaces, and gio.js:19939 emits loadContentsAsync calling g_file_load_contents_async — a GTask/run_in_thread API whose g_task_finalize releases the task's source object and cancellable on the pool thread whenever the pool thread's trailing task unref loses the race with the main-context completion idle; both objects are JS-wrapped with toggle refs via the universal decode path (types/gobject.rs tracked_gobject_value). GDBus worker-thread transient refs on a JS-wrapped private GDBusConnection supply the 1→2 crossing concurrent with a pending matched cleanup. (3) The UAF ordering verifies against the code: worker reads ref_ptr (toggle_ref.rs:235); the GLib-thread cleanup clears qdata and routes OP_DELETE to the JS thread (toggle_ref.rs:370-375 → module/toggle_ref.rs:96 napi_delete_reference); the worker's subsequent invoke_ref_op makes apply_wrapper_ref_op (module/toggle_ref.rs:92-94) call napi_reference_ref/unref on the freed napi_ref. (4) The second consequence verifies: invoke_node_and_wait from the worker reaches wait_for_node_result (dispatch/js_bridge.rs:156-172), whose dispatch_pending_from_depth executes queued GLib-bound tasks on the worker thread, and wake_glib is a single-permit WaitSignal (wait_signal.rs: notify_one, permit consumed in wait), so two parked threads can lose wakeups. (5) The crate's cross-thread test does not cover the scenario: drive_toggle_from_thread (module/test_support.rs:88-99) routes every ref/unref pair through Mailbox::schedule_glib onto the GLib thread, so all tested notifies fire serialized. The medium rating is appropriate: the consequences are UAF/UB, while the trigger needs a tight scheduling window on async-I/O, DBus, soup, or WebKit paths.

  </details>

- [x] **RS-017** · `src/toggle_ref.rs:287` · non-idiomatic — **Toggle-ref binding lifetime managed by hand-rolled Arc strong-count arithmetic on usize addresses**

  WrapperBinding cells are kept alive by manually pairing Arc::into_raw/Arc::increment_strong_count/Arc::decrement_strong_count calls across three modules, with the cell address erased to a usize in FinalizeData and re-fabricated as *const WrapperBinding inside the cleanup closure. Every count adjustment must be balanced by inspection across install, schedule_cleanup, and the finalizer; a missed decrement leaks the cell forever and an extra one is a use-after-free, and nothing in the types enforces the pairing. WrapperBinding contains only atomics, so it is Send + Sync: FinalizeData could hold an Option<Arc<WrapperBinding>> directly, install could return a cloned Arc, and the glib::idle_add_once closure could capture the Arc by move — Drop would then manage every count, deleting all five manual count operations and the unsafe address round-trips. Only the qdata slot genuinely needs Arc::into_raw (one site instead of six).

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/toggle_ref.rs:287-293: `let cell_ptr = Arc::into_raw(cell); g_object_set_qdata(...); Arc::increment_strong_count(cell_ptr);` and 306: `Arc::increment_strong_count(existing);`; src/toggle_ref.rs:362-365, 374, 383: `let binding = binding_addr as *const WrapperBinding; ... unsafe { Arc::decrement_strong_count(binding) };` (three separate decrement sites whose balance depends on the generation branch taken). The address erasure: src/module/toggle_ref.rs:35-40 `struct FinalizeData { gobject_addr: usize, ref_addr: usize, binding_addr: usize, generation: u64 }` and 150/166-169 writing the fields through a raw pointer after the finalizer was registered. WrapperBinding (src/toggle_ref.rs:120-130) holds only AtomicUsize/AtomicU64, so an owned Arc<WrapperBinding> is Send+Sync and could replace binding_addr end to end.

  **Verifier:** Every cited line is accurate: toggle_ref.rs:287-293 does Arc::into_raw + g_object_set_qdata + Arc::increment_strong_count on a fresh bind, line 306 increments again on rebind, and the schedule_cleanup closure (lines 362-383) re-fabricates *const WrapperBinding from a usize and contains three branch-dependent Arc::decrement_strong_count sites whose balance against the two increments must be verified by hand across two modules. FinalizeData (module/toggle_ref.rs:35-40) erases the cell to binding_addr: usize and mutates it through a raw pointer after finalizer registration (lines 150, 166-169). WrapperBinding (toggle_ref.rs:120-130) holds only AtomicUsize/AtomicU64, so Arc<WrapperBinding> is Send+Sync; the type erasure is not forced by thread-crossing. The proposed alternative is verified feasible against actual constraints: dispatch_to_glib_and_wait (src/dispatch/js_bridge.rs:66-73) is generic over R: Send + 'static, so install could return an owned Arc; glib::idle_add_once accepts a Send FnOnce that can capture the Arc by move; FinalizeData is a Box round-tripped via Box::into_raw/from_raw and can hold Option<Arc<WrapperBinding>> with the 0-sentinel mapping to None. The counts do currently balance (no live bug), matching the non-idiomatic/medium classification, and the "one unsafe site instead of six" tally is slightly optimistic (the rebind clone and qdata reclaim still need one raw-Arc conversion each), but the substance stands: five manual count operations plus address fabrication spread across install, schedule_cleanup, and the finalizer would collapse into Drop-managed ownership with raw counts confined to the single genuine FFI slot (qdata). Clippy has no lint for manual strong-count arithmetic, and the module docs spend three paragraphs describing the count-sharing protocol in prose precisely because the types do not enforce it. This is a genuine, accurately evidenced non-idiomatic design finding, not pedantry.

  </details>

- [x] **RS-018** · `src/trampoline.rs:78` · non-idiomatic — **One SAFETY comment for roughly 300 unsafe blocks in a memory-safety-critical FFI crate**

  The crate's entire purpose is policing the JS/GLib memory boundary, and CI invests in ASAN and Miri runs, yet `grep -rn 'SAFETY' src/` finds exactly one justified unsafe block (src/state.rs:97). Module docs and # Safety sections on some unsafe fns are good, but the individual unsafe blocks — including genuinely subtle ones — carry no per-site justification, so a reviewer cannot tell which invariant each block relies on or verify it locally. The starkest example is the self-referential &'static fabrication in TrampolineState::create, which silently depends on Box heap stability and on the Drop order declared 30 lines away; others include the qdata Arc round-trips in toggle_ref.rs, the unaligned pointer-slot writes in types/raw_ptr.rs, and the Unknown::from_raw_unchecked conversions throughout the napi glue. Adopting the `// SAFETY:` convention (and optionally enabling clippy::undocumented_unsafe_blocks, which -D warnings does not cover because it is a restriction lint) is the idiomatic remedy.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/trampoline.rs:76-78: `let data = ManuallyDrop::new(Box::new(data)); let data_ptr: *const TrampolineData = &**data; let data_ref: &'static TrampolineData = unsafe { &*data_ptr };` — a fabricated 'static borrow into a sibling field with no SAFETY comment explaining why the closure cannot outlive the box or why Drop order (lines 62-67: closure dropped before data) makes it sound. Verified by search: the only SAFETY comment in src/ is src/state.rs:97 (`// SAFETY: Loading a shared library with RTLD_NOW | RTLD_GLOBAL`), while `grep -c unsafe` totals ~290 occurrences across the tree (49 in types/array.rs, 22 in toggle_ref.rs, 20 in value.rs, 17 in types/gobject.rs, etc.).

  **Verifier:** All factual claims verify against the current code. grep confirms exactly one SAFETY comment in src/ (state.rs:97-98) against roughly 287 unsafe occurrences (per-file counts match the evidence: 49 types/array.rs, 22 toggle_ref.rs, 20 value.rs, 17 types/gobject.rs). The cited example is accurate and substantive: trampoline.rs:76-78 fabricates `let data_ref: &'static TrampolineData = unsafe { &*data_ptr };` into a sibling Box field with no per-site comment, while its soundness depends on Box heap stability and on the ManuallyDrop drop ordering in the Drop impl at lines 62-67 (closure dropped before data) — invariants stated nowhere, so the block cannot be verified locally. The crate's partial mitigations (# Safety sections on some unsafe fns, the raw_ptr.rs module doc centralizing unsafe writes) are acknowledged by the finding and do not cover the unsafe blocks themselves. The remedy is the established idiom (std mandates // SAFETY: per block; clippy::undocumented_unsafe_blocks exists for it) and is not covered by the existing CI gates, since that lint is an allow-by-default restriction lint that -D warnings does not enable, and ASAN/Miri check runtime behavior, not invariant documentation. For a memory-safety-critical FFI crate this is a significant non-idiomatic gap, fitting the medium severity definition, not pedantry or a rustfmt/clippy-governed style matter.

  </details>

- [x] **RS-019** · `src/types/array.rs:720` · memory-leak — **GArray string elements are unconditionally g_strdup'd with no clear-func, leaking every string for borrowed and transfer-container arguments**

  append_items_to_garray duplicates every string element regardless of the item type's ownership, and the GArray is created with g_array_sized_new and no g_array_set_clear_func; FfiStorage::drop_garray only calls g_array_unref, which frees the element segment but not the duplicated strings it points to. For a transfer-none GArray-of-strings argument (container borrowed, items borrowed) the drop frees the array buffer but every duplicated string leaks on every call — a happy-path leak. For transfer-container (container full, items borrowed) the callee frees only the array, again leaking every dup. The encoder for plain/null-terminated string arrays gets this right by gating the dup on the item ownership (`dup_elements = matches!(&*self.item_type, Type::String(s) if s.ownership.is_full())`, lines 573-574), and the GList path both gates the dup and frees duped elements with g_list_free_full; the GArray path does neither.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Lines 720-731: `ItemCodec::String => { for cstr in Self::extract_strings(array)? { let dup = unsafe { glib::ffi::g_strdup(cstr.as_ptr()) }; unsafe { glib::ffi::g_array_append_vals(g_array, &dup as *const *mut c_char as *const c_void, 1); } } Ok(()) }` — unconditional dup, no ownership check. encode_garray lines 747-762 creates the array with `g_array_sized_new(0, 0, element_size as u32, array.len() as u32)` (no clear func) and stores only `GArrayData { array_ptr, should_free }` with `should_free = self.ownership.is_borrowed()`; src/ffi/storage.rs:232-236 `fn drop_garray(data: &GArrayData) { if data.should_free && !data.array_ptr.is_null() { unsafe { glib::ffi::g_array_unref(data.array_ptr) }; } }` frees the segment holding the char* values but never the g_strdup'd strings they point to. Trace: encode borrowed GArray<utf8> arg -> call executes -> FfiStorage drops -> g_array_unref -> N strings leaked.

  **Verifier:** The finding stands; every candidate refutation fails against the code. At /home/eugenio/gtkx/packages/native/src/types/array.rs:720-732 the ItemCodec::String arm of append_items_to_garray unconditionally g_strdup's each element (`let dup = unsafe { glib::ffi::g_strdup(cstr.as_ptr()) };` followed by g_array_append_vals). The dup is forced by the design — the CStrings from extract_strings are locals that drop when the function returns, and unlike the plain-array path (FfiStorageKind::StringArray keeps the CString vec alive, so it can gate the dup on item ownership at lines 573-574), the GArray storage retains only GArrayData { array_ptr, should_free } (encode_garray, lines 747-762), created via g_array_sized_new with no clear func; grep confirms g_array_set_clear_func is never called anywhere in the crate. FfiStorage::drop_garray (/home/eugenio/gtkx/packages/native/src/ffi/storage.rs:232-236) only calls g_array_unref when should_free, which frees the char* segment but not the duplicated strings, and GArrayData lacks the elements_duped tracking that the StringGList/StringGSList variants use to free duped elements via g_list_free_full/g_slist_free_full(g_free) (storage.rs:28-41, 195-213). Trigger is concrete and reachable: the crate's own test encode_garray_strings_roundtrips (/home/eugenio/gtkx/packages/native/tests/array_codec.rs:585-596) encodes ArrayKind::GArray with Ownership::Borrowed container and string items, then drops the storage — leaking the duped string on every run; generally any GIR GArray<utf8> transfer-none in-arg leaks N strings per call (should_free=true, unref frees only the buffer), and transfer-container leaks identically on the callee side (no clear func, items borrowed). In-args are never decoded after the call, so no decode path reclaims them. CI cannot see it: scripts/ci-asan.sh sets ASAN_OPTIONS detect_leaks=0 (LeakSanitizer explicitly off), and Miri cannot execute the GLib FFI calls on this path. Medium severity (deterministic leak, no UB) is correctly calibrated.

  </details>

- [x] **RS-020** · `src/types/array.rs:841` · memory-leak — **Container decode paths skip the transfer-full container free when an element decode fails (GList/GSList, GPtrArray, null-terminated, GArray, GHashTable)**

  All container decoders free or unref the transfer-full container only after every element decoded successfully; a mid-iteration element decode failure propagates with `?` and skips the container release. The container (and any transfer-full elements after the failure point) leaks; elements before the failure that were adopted with full ownership are also stranded in already-built Value objects that never reach JavaScript. The pattern repeats in five decoders: decode_glist/decode_gslist, decode_gptrarray, decode_null_terminated_ptr_array, decode_garray, and HashTableType::decode. Failure is reachable, for example, when a boxed element's free_fn symbol cannot be resolved (BoxedType::boxed_with_free_fn bails), a borrowed boxed element has no gtype and no size (Boxed::from_glib_none_with_size bails), or a GObject element's class pointer is invalid (load_type_class bails).

  <details><summary>Evidence and verification</summary>

  **Evidence:** decode_glist lines 841-855: `while !current.is_null() { let data = unsafe { (*current).data }; let item_ffi = ffi::FfiValue::Ptr(data); let item_value = self.item_type.decode(&item_ffi)?; ... } if self.ownership.is_full() { ... g_slist_free / g_list_free ... }` — the `?` at line 844 exits before the frees at lines 849-854. Same shape: decode_gptrarray line 893 (`?` before g_ptr_array_unref at 897-899), decode_null_terminated_ptr_array line 940 (`?` before g_free at 944-946), decode_garray line 869 (`decode_contiguous(...)?` before g_array_unref at 871-876), and HashTableType::decode (src/types/hashtable.rs:250-253, `ptr_to_value(...)?` before g_hash_table_unref at line 260). Failure source trace: item_type = Boxed with free_fn → FfiDecoder::decode (src/types/boxed.rs:174-176) → boxed_with_free_fn → lookup_free_fn error → `?` propagates out of the list loop; or a transfer-full GList of GObjects where one element's class pointer is null → load_type_class (src/types/gobject.rs:23-31) bails 'GObject has invalid type class'.

  **Verifier:** The finding stands. All five decoders release the transfer-full container only after the element loop completes, and each loop propagates element decode errors with `?` before that release: array.rs:844 vs 849-855 (decode_glist), 869 vs 871-876 (decode_garray), 893 vs 897-899 (decode_gptrarray), 940 vs 944-946 (decode_null_terminated_ptr_array), and hashtable.rs:250-253 vs 259-261 (HashTableType::decode). No invariant rescues this: (1) returned containers always arrive as bare FfiValue::Ptr — the default FfiEncoder::call_cif (types.rs:194-203) wraps pointer returns as Ptr, ArrayType has no call_cif override, and FfiValue::Ptr has no Drop — so the decoder is the sole owner; the !storage_owns guards in decode_garray/decode_gbytearray/HashTableType::decode confirm storage ownership applies only to encoded arguments. (2) The caller, CallRequest::execute (module/call.rs:96-98), propagates the decode error with `?` and performs no cleanup. (3) Element decode failure is reachable: BoxedType::boxed_with_free_fn fails when the freeFn symbol or library cannot be resolved (boxed.rs:79-102, with a doc comment declaring this a deliberate failure mode); borrowed Boxed without resolvable gtype and without size bails in Boxed::from_glib_none_with_size (managed/boxed.rs:96-101); load_type_class bails on a null class pointer (gobject.rs:23-31). Container and item ownership are independent descriptor fields, so a transfer-full container of such items is constructible. (4) #[napi(catch_unwind)] converts the error to a JS exception, so a deterministic metadata defect leaks the container on every call. One detail of the finding is overstated: transfer-full Boxed elements decoded before the failing index are freed when the partial values Vec unwinds (Boxed::drop, managed/boxed.rs:175-194); only GObject elements strand their pending owned reference, since the Value carries a non-owning NativeHandle::borrowed_gobject (gobject.rs:64-81). The core claim — container free skipped plus leak of remaining transfer-full elements — is accurate, and medium severity for a leak on an error path is appropriate.

  </details>

- [x] **RS-021** · `src/types/gobject.rs:145` · memory-leak — **write_return_to_raw_ptr ignores transfer mode and always adds a reference/copy to callback return values**

  GObjectType carries an Ownership field that decode honors, but write_return_to_raw_ptr — the path that writes a JS callback's return value into the native return slot of a trampoline — unconditionally takes a fresh reference (from_glib_none + into_glib_ptr). BoxedType::write_return_to_raw_ptr (src/types/boxed.rs:200-206) unconditionally g_boxed_copies, and FundamentalType (src/types/fundamental.rs:112-117) unconditionally calls ref_fn. For a callback, vfunc, or signal whose return is annotated transfer-none (e.g. GtkNotebook::create-window returns a transfer-none GtkNotebook*), the C emitter never releases the extra reference/copy, so every emission leaks one GObject reference (keeping the object alive past its intended lifetime) or one boxed copy outright.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Lines 145-151: `fn write_return_to_raw_ptr(&self, ret: *mut c_void, value: &Result<value::Value, ()>) { write_return_object_ptr(ret, value, |ptr| { let obj: glib::Object = unsafe { glib::Object::from_glib_none(ptr as *mut glib::gobject_ffi::GObject) }; IntoGlibPtr::<*mut glib::gobject_ffi::GObject>::into_glib_ptr(obj).cast::<c_void>() }); }` — `self.ownership` is never read, even though the struct's only field is `pub ownership: Ownership` and FfiDecoder::decode branches on it. Call chain: native emits a signal → trampoline_handler → TrampolineData::handle_call → `self.return_type.write_return_to_raw_ptr(result, &Ok(value))` (src/trampoline.rs:167) → +1 ref written into the return slot of a transfer-none-returning signal → the emitter treats it as borrowed → ref never released.

  **Verifier:** The finding stands, but only on the vfunc path; its featured signal trigger is refuted. Code fact confirmed: GObjectType::write_return_to_raw_ptr (/home/eugenio/gtkx/packages/native/src/types/gobject.rs:145-151) never reads self.ownership and always writes a +1 reference (from_glib_none + into_glib_ptr); BoxedType (boxed.rs:200-206) always g_boxed_copies and FundamentalType (fundamental.rs:112-117) always calls ref_fn. SIGNAL PATH REFUTED: trampolines are connected as plain C callbacks via g_signal_connect_data (packages/ffi/src/signals.ts:73-91), so the return slot is consumed by the signal's C closure marshaller, and those marshallers take ownership of object/boxed/string/param returns regardless of the GIR annotation: the installed glib-genmarshal (which generates gtkmarshalers.c) sets OBJECT returns with g_value_take_object (OUT_ARGS, lines 488-492), and an empirical C test against the installed GLib confirmed g_cclosure_marshal_generic does the same (a handler returning a borrowed object with refcount 2 left refcount 2 after emission despite a live LCOPY'd ret — exactly one reference consumed from the return slot). For GtkNotebook::create-window the trampoline's extra reference is therefore consumed by g_value_take_object and released by the accumulator/value-unset machinery — no leak; honoring Borrowed there would underflow the refcount (the stolen reference belongs to the JS wrapper's toggle ref) and cause a UAF, so the unconditional +1 is mandatory on every signal. VFUNC PATH CONFIRMED: the same write_return_to_raw_ptr serves vfunc trampolines (register_class.rs:96-104 → TrampolineData::handle_call, trampoline.rs:167), which are direct vtable calls with no GValue marshaller, so the C caller follows the GIR transfer contract. Transfer-none object-returning vfunc slots exist in the bound libraries (GioIOStreamClass.get_input_stream/get_output_stream, GtkEditableInterface.get_delegate, GtkCellLayoutIface.get_area, GtkCellRendererClass.start_editing, GioActionMapInterface.lookup_action, GioAppInfoIface.get_icon, GioDBusInterfaceIface.get_object), codegen emits them with borrowed returns (class-struct.ts:86 via value.ts ffiOwnership mapping none→borrowed), and the generated store proves the path is live: node_modules/.gtkx/gi/gio/gio.js:23190-23194 carries getInputStream { vfuncName: "get_input_stream", byteOffset: 136, returnType: t.object("borrowed") }. The public registerClass API (packages/ffi/src/register-class.ts:79) auto-installs this trampoline on any Gio.IOStream subclass defining getInputStream(); every g_io_stream_get_input_stream() call then receives a +1 reference its transfer-none contract never releases — one leaked strong reference per call, so the object can never be finalized (and BoxedType's unconditional g_boxed_copy leaks the copy outright on the analogous boxed path). Net verdict: real leak of medium severity within the finding's stated scope ("callback, vfunc, or signal"), with the correction that any fix must keep owned returns for signal trampolines (GClosure-marshalled) and honor the transfer annotation only for vfunc and direct function-pointer callback returns; uniformly honoring ownership as the finding implies would introduce a UAF on the signal path.

  </details>

- [x] **RS-022** · `src/types/hashtable.rs:67` · logic-bug — **GHashTable element ownership transfer is half-implemented: owned refs without destroy funcs, and GPtrArray elements never transferred**

  Two inconsistencies in element ownership. (1) encode_hashtable passes every key/value through key_type/value_type.ref_for_transfer, which for a full-ownership GObject takes a g_object_ref and for a full-ownership boxed makes a g_boxed_copy — but free_func() returns None for NativeHandle entries, so the table created by g_hash_table_new_full has no destroy notify for them. Whoever destroys the table (the callee for a transfer-full table, or FfiStorage::drop_hash_table for a borrowed one) frees only the table structure; the extra refs/copies taken at insert leak unconditionally. (2) The PtrArray value encoder receives the element item type but explicitly discards it (`Self::PtrArray(_item_type)`) and inserts raw handle pointers with no reference transfer; a callee that retains a transfer-full table holds GPtrArrays whose elements dangle once the JavaScript wrappers are collected and their toggle refs removed.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Lines 67-73: `pub fn free_func(&self) -> glib::ffi::GDestroyNotify { match self { Self::String | Self::Float => Some(glib::ffi::g_free), Self::Integer | Self::Boolean | Self::NativeHandle => None, Self::PtrArray(_) => Some(g_ptr_array_unref_wrapper), } }` versus lines 186-190: `let key_ptr = self.key_type.ref_for_transfer(key_ptr)?; let val_ptr = self.value_type.ref_for_transfer(val_ptr)?; unsafe { glib::ffi::g_hash_table_insert(hash_table, key_ptr, val_ptr); }` — for Type::GObject with Ownership::Full, ref_for_transfer (src/types/gobject.rs:103-112) takes a reference that no destroy notify will ever release. PtrArray arm lines 109-123: `Self::PtrArray(_item_type) => { ... let item_ptr = match item { value::Value::Object(handle) => handle.ptr(), ... }; unsafe { glib::ffi::g_ptr_array_add(ptr_array, item_ptr) }; }` — `_item_type` (and its ownership) is ignored, so no per-element transfer occurs.

  **Verifier:** Both halves of the finding are factually accurate against the current code. (1) hashtable.rs:67-73 returns None as GDestroyNotify for NativeHandle entries, yet encode_hashtable (lines 186-190) passes every key/value through ref_for_transfer before g_hash_table_insert. For full-ownership elements that call takes real ownership: gobject.rs:103-112 adds a g_object_ref (from_glib_none + into_glib_ptr), boxed.rs:155-165 makes a g_boxed_copy, fundamental.rs:81-89 calls the type's ref function. No release path exists: FfiStorage::drop_hash_table (storage.rs:214-218) only calls g_hash_table_unref, which frees entries solely through the destroy notifies that were set to None, and for a transfer-full table (should_free = ownership.is_borrowed(), hashtable.rs:198) the callee's unref hits the same missing notifies. The leaky descriptor shape is actually generated: codegen (packages/codegen/src/writers/value.ts:83-88) derives element transfer from the table's transfer, so a GIR transfer-full hash table emits full-ownership key/value descriptors, and t.hashTable (packages/ffi/src/helpers.ts:121-126) exposes arbitrary element ownership publicly. (2) hashtable.rs:109-123 binds the GPtrArray element type as `_item_type` and ignores it: g_ptr_array_new() has no element free func and items are inserted as raw handle.ptr() with no ref_for_transfer, while keys/values one line away do get per-element transfer. A callee retaining a transfer-full table therefore holds arrays of unreferenced pointers that dangle once the JS wrappers' toggle refs drop, and a full-ownership item descriptor promises owned refs that were never taken. The existing test suite (tests/hashtable.rs) only exercises Ownership::Borrowed for NativeHandle/struct elements, so ASan/Miri on default paths cannot observe this. This is a real internal inconsistency with a concrete trigger, correctly rated medium.

  </details>

- [x] **RS-023** · `src/types/hashtable.rs:172` · memory-leak — **encode_hashtable leaks the GHashTable, inserted entries, and the just-encoded key allocation on mid-loop errors**

  encode_hashtable creates the table with g_hash_table_new_full and then iterates the tuples with `?` propagation. If a tuple is malformed (Self::tuple fails), a key or value fails to encode (e.g. a non-string in a string-keyed table), or ref_for_transfer fails, the function returns early and the table — plus all entries inserted so far — is never freed. Additionally, when the key encoder has already allocated (g_strdup for string keys, g_malloc for float keys) and the value encoder then fails, that key allocation was never inserted into the table and leaks even if the table were destroyed. The analogous array path shows the crate's own standard: encode_garray unrefs the partially built GArray on error (src/types/array.rs:750-753). The trigger is any JS-supplied hash table argument with one mistyped tuple, an ordinary user input error.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Lines 172-192: `let hash_table = unsafe { glib::ffi::g_hash_table_new_full(...) }; for tuple in tuples { let (key, val) = Self::tuple(tuple)?; let key_ptr = key_encoder.encode(key)?; let val_ptr = value_encoder.encode(val)?; let key_ptr = self.key_type.ref_for_transfer(key_ptr)?; let val_ptr = self.value_type.ref_for_transfer(val_ptr)?; ... g_hash_table_insert ... }` — every `?` between table creation and the Ok(...) at line 194 exits without g_hash_table_unref(hash_table); no drop guard exists on the error paths, while the happy path hands ownership to FfiStorageKind::HashTable whose drop unrefs. If `value_encoder.encode(val)` fails after `key_encoder.encode(key)` succeeded, the duplicated key string (line 82 g_strdup) or malloc'd float (line 96) leaks as well. Contrast src/types/array.rs:750-753: `if let Err(err) = self.append_items_to_garray(g_array, array) { unsafe { glib::ffi::g_array_unref(g_array) }; return Err(err); }`. Trigger: encode `[["a","b"],["c",42]]` for a string->string table — the second tuple's value encode bails, leaking the table, the inserted first entry, and the g_strdup'd key "c".

  **Verifier:** Confirmed. encode_hashtable (src/types/hashtable.rs:172-201) creates an owned GHashTable via g_hash_table_new_full at line 172 and holds it only in a raw pointer local with no drop glue. The four `?` propagation points inside the loop (Self::tuple at 182, key_encoder.encode at 183, value_encoder.encode at 184, ref_for_transfer at 186-187) return without g_hash_table_unref; the only release path is FfiStorage's Drop (src/ffi/storage.rs:214-218, dispatched at 248), and the FfiStorageKind::HashTable storage is constructed only at line 194 after the loop succeeds. So on a mid-loop error the table and all previously inserted entries leak, and a key already allocated by the key encoder (g_strdup at line 82 for strings, g_malloc at line 96 for floats) leaks when the value encoder then fails, since it was never inserted. Every refutation attempt fails: (1) the path is reachable from arbitrary JS input through the public call API, and the crate's own tests deliberately exercise it (tests/hashtable.rs:684-695 hashtable_encode_rejects_non_tuple_entry and 697-711 hashtable_encode_propagates_key_encoder_error — each run of those tests currently leaks a GHashTable); (2) CI cannot see it — scripts/ci-asan.sh:21 sets detect_leaks=0 explicitly, and scripts/ci-miri.sh runs only the FFI-free miri_marshalling target; (3) no outer scope retains or frees the pointer — the anyhow error propagates to a JS exception and the process continues, so retries leak unboundedly. The crate's own pattern in src/types/array.rs:750-753 (g_array_unref on append error before propagating) shows the intended cleanup that hashtable.rs omits. Minor overstatement in the finding: the ref_for_transfer trigger is theoretical, since the default (types.rs:205-207), GObject (gobject.rs:103-112), and Boxed (boxed.rs:155-165) implementations never return Err — but the tuple-shape and encoder-failure triggers fully stand. Severity medium (leak on user-input error path) is correct.

  </details>

- [x] **RS-024** · `src/types/trampoline.rs:128` · memory-leak — **Notified/Async/Forever trampoline state (libffi closure, TrampolineData, JsRef) leaks whenever the FFI call fails after argument encoding**

  For the Notified, Async, and Forever scopes, encode leaks the TrampolineState via Box::into_raw and stores no owning handle in the resulting FfiValue (`_owned_state` is None; only TrampolineScope::Call ties the state's lifetime to the FfiValue via `Some(state)`); TrampolineValue has no Drop impl, so reclamation relies on the native callee invoking the destroy notify (Notified), the one-shot firing (Async), or process lifetime (Forever). In CallRequest::execute, arguments are encoded before the symbol is resolved, so any failure between encode and a successful ffi_call — a later argument fails to encode (e.g. an out-of-range integer rejected by IntegerKind::check_range), the library or symbol fails to resolve (e.g. a symbol absent from the installed GTK version), or call_cif returns Err or panics (including the arg-count assert from the scope/hasDestroy mismatch) — drops the FfiValue::Trampoline without the native call ever happening. The libffi ffi_closure (executable memory page), the Box<TrampolineState>, the Box<TrampolineData>, and the Arc<JsRef<JsFunction>> inside it leak permanently; the napi reference pins the JavaScript callback function (and everything it captures) for the life of the process. Each occurrence leaks one full trampoline.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/types/trampoline.rs:135-143 (Notified): `let state_ptr = Box::into_raw(Box::new(state)) as *mut c_void; Ok(ffi::FfiValue::Trampoline(ffi::TrampolineValue::new(fn_ptr, state_ptr, Some(TrampolineState::destroy as *mut c_void), None)))` and lines 144-157 (Async): `let raw_ptr = Box::into_raw(Box::new(state)); ...` — the fourth argument (owned_state) is None. src/ffi/value.rs:27-48: `pub struct TrampolineValue { fn_ptr: *mut c_void, state_ptr: *mut c_void, destroy_ptr: Option<*mut c_void>, _owned_state: Option<Box<TrampolineState>> }` — no Drop impl; _owned_state is Some only for scope=Call (types/trampoline.rs:159-167). Trigger sequence: module/call.rs:62-71 encodes all args left to right (Box leaked into raw) → if arg i+1 fails after a Notified trampoline was encoded at arg i, the Vec is dropped and the raw state_ptr is never freed; same for symbol resolution failure at module/call.rs:78-87 (`library.get(...)?`) and call_cif failure at 89-92 — `TrampolineState::destroy` (src/trampoline.rs:103-107) is never reached because the destroy notify was never handed to the callee.

  **Verifier:** Confirmed. For Notified/Async/Forever scopes, encode at src/types/trampoline.rs:128-158 leaks TrampolineState via Box::into_raw and passes None as owned_state to TrampolineValue::new; only the Call scope (lines 159-167) ties the state's lifetime to the FfiValue. TrampolineValue (src/ffi/value.rs:27-48) has no Drop impl, so dropping FfiValue::Trampoline with _owned_state None reclaims nothing. A crate-wide grep shows exactly three free paths: TrampolineState::destroy (only invoked by the native callee via the destroy notify), defer_oneshot_free (only after the one-shot fires), and the Call-scope owned Box — no registry or fallback. The leaked state holds the libffi Closure (executable page) and TrampolineData with Arc<JsRef<JsFunction>>, whose Drop is what deletes the napi reference, so the JS callback stays pinned for process life. Triggers are concrete and reachable: in CallRequest::execute (src/module/call.rs:62-92) args are encoded before symbol resolution and call_cif; a later arg failing to encode drops the partial Vec (test execute_fails_when_an_argument_cannot_be_encoded proves encode errors are reachable), and library.get(...)? failing for a missing symbol (test execute_fails_for_unknown_symbol) drops all encoded FfiValues regardless of argument order. The napi export validates only can_be_return_type(), filtering none of these. The error is returned to JS via ModuleRequest::dispatch (src/module/handler.rs:27-38), so the process continues and each failed call leaks one full trampoline. Sole caveat: for the Forever scope, process-lifetime retention is the designed success behavior, so the error-path leak only meaningfully applies to Notified and Async — which the finding's core claim covers correctly. Medium severity (leak on an error path) is appropriate.

  </details>

## Low severity (17)

- [x] **RS-025** · `src/dispatch.rs:185` · logic-bug — **Unbalanced unfreeze wraps freeze_depth to usize::MAX, silently and permanently disabling commit freezing**

  unfreeze performs an unguarded fetch_sub on freeze_depth. Atomic RMW operations wrap silently in all build profiles, so a single unpaired unfreeze() — an exported napi function callable directly from JS with no pairing enforcement — wraps the depth from 0 to usize::MAX. From then on freeze()'s outermost detection (fetch_add(1) == 0) can never report true again, so no freeze loop is ever started for any subsequent React commit: the GLib frame clock fires between individual mutations mid-commit, producing the exact intermediate-repaint artifacts the freeze mechanism exists to prevent, with no error or diagnostic anywhere. Every later freeze/unfreeze pair keeps the counter offset, so the corruption is permanent and invisible. Relatedly, freeze()'s error path (dispatch fails because the mailbox is stopped or the GLib thread died) leaves the already-incremented depth behind with no rollback, producing the same permanent desynchronization and feeding the stop()-while-frozen deadlock. An idiomatic guard is fetch_update with checked_sub (or a debug assertion plus error report) so misuse surfaces immediately.

  <details><summary>Evidence and verification</summary>

  **Evidence:** dispatch.rs:185-189: `pub fn unfreeze(&self) { if self.freeze_depth.fetch_sub(1, Ordering::AcqRel) == 1 { self.freeze_wake.notify(); } }` and dispatch.rs:180-182: `pub fn freeze(&self) -> bool { self.freeze_depth.fetch_add(1, Ordering::AcqRel) == 0 }`. JS reachability: src/module/freeze.rs:43-49 `#[napi(catch_unwind)] pub fn unfreeze() -> napi::Result<()> { Mailbox::global().unfreeze(); Ok(()) }` forwards with no state check. Trigger chain: JS calls unfreeze() while freeze_depth == 0 → fetch_sub wraps 0 → usize::MAX (no panic: atomic fetch_sub always wraps) → next freeze() fetch_add returns usize::MAX != 0 → is_outermost false → module/freeze.rs:21-23 skips scheduling run_freeze_loop → commit mutations execute interleaved with frame-clock ticks; the pair then oscillates between usize::MAX and 0 forever. Error path: src/module/freeze.rs:35-37 propagates the wait error after mailbox.freeze() already incremented the depth at line 21.

  **Verifier:** Every factual claim checks out. dispatch.rs:185-189 performs an unguarded fetch_sub on freeze_depth; atomic RMW wraps silently in all build profiles, so one unpaired unfreeze() at depth 0 yields usize::MAX. freeze() at dispatch.rs:180-182 detects outermost via fetch_add(1) == 0, which after the wrap never returns true under balanced use (the counter oscillates MAX↔0, and unfreeze's == 1 notify also never fires), so module/freeze.rs:23-33 never schedules run_freeze_loop again — commit mutations interleave with frame-clock ticks permanently, with zero diagnostics. The napi export at module/freeze.rs:46-49 forwards to Mailbox::unfreeze with no state check and is on the public @gtkx/native surface. The error-path claim is also accurate and in-tree reachable: freeze.rs:21 increments the depth before scheduling; when the mailbox is stopped (shutdown racing a commit), schedule_glib (dispatch.rs:225-227) drops the task and its tx, wait_for_glib_result (js_bridge.rs:99-101) returns GlibDisconnectedError, and freeze.rs:37 propagates it without decrementing. The only pairing lives in JS (host-config.ts:359-368, resetAfterCommit's finally), which react-reconciler does not invoke when prepareForCommit throws, leaving the depth permanently ≥1 — the same silent, permanent disablement. This is an unsound contract on an exported function plus a no-rollback error path, exactly the class the audit targets; neither clippy nor rustfmt covers atomic underflow, and the suggested fetch_update/checked_sub guard is the idiomatic remedy. Low severity is fair since the common path pairs correctly.

  </details>

- [x] **RS-026** · `src/dispatch.rs:296` · logic-bug — **Panicking GLib task is reported to the blocked JS caller as GlibDisconnectedError, conflating a single failed task with a dead runtime**

  When a task dispatched via dispatch_to_glib_and_wait panics on the GLib thread, catch_unwind swallows the unwind and drops the task closure — including the captured mpsc Sender — so the JS thread blocked in wait_for_glib_result observes TryRecvError::Disconnected and receives GlibDisconnectedError, whose Display text is "GLib thread disconnected". The thread is in fact alive and healthy; only one task failed. The synchronous FFI call therefore rejects with a message indicating runtime death, while the real panic text arrives later and out-of-band via the unhandledRejection error reporter. JS-side callers (and the runtime's own stop/freeze paths, which map this same error) cannot distinguish a recoverable per-call failure from actual GLib-thread shutdown, which misdirects diagnosis of any panic in a marshalled call.

  <details><summary>Evidence and verification</summary>

  **Evidence:** dispatch.rs:296-301: `if let Err(payload) = panic::catch_unwind(AssertUnwindSafe(task)) { NativeErrorReporter::global().report_str(&format!("panic in GLib-thread task: {}", ...)); }` — the Box<dyn FnOnce> from js_bridge.rs:76-81 (`self.schedule_glib(Box::new(move || { if tx.send(task()).is_err() { ... } }))`) is consumed by the unwind, dropping tx without sending. js_bridge.rs:99-101: `match rx.try_recv() { ... Err(mpsc::TryRecvError::Disconnected) => return Err(GlibDisconnectedError), ... }` and dispatch.rs:322-326: `write!(f, "GLib thread disconnected")`. Call sequence: JS thread → dispatch_to_glib_and_wait → task panics on GLib thread → catch_unwind at dispatch.rs:296 → tx dropped → JS caller receives "GLib thread disconnected" although dispatch.rs:308 then notifies wake_js and the thread keeps servicing tasks normally.

  **Verifier:** Every factual claim verifies against the code. In dispatch_to_glib_and_wait (js_bridge.rs:75-81) the sole mpsc::Sender is captured by the scheduled closure and tx.send(task()) only runs if task() returns; when task() panics, catch_unwind at dispatch.rs:296 absorbs the unwind, the closure's environment (including tx) is dropped, and dispatch.rs:308-309 then wakes the JS thread. wait_for_glib_result (js_bridge.rs:99-101) sees TryRecvError::Disconnected and returns GlibDisconnectedError, whose Display (dispatch.rs:324) is "GLib thread disconnected" and whose doc comment (dispatch.rs:316-318) explicitly ties it to GLib-thread shutdown — yet the dispatch loop keeps running and the thread stays alive. handler.rs:28-30, stop.rs:49, and freeze.rs:36-37 all stringify this same error, so callers cannot distinguish a single panicking task from real shutdown (the stopped-mailbox early return at dispatch.rs:225-227 produces the identical Disconnected outcome). The actual panic text reaches JS only asynchronously through NativeErrorReporter -> the unhandledRejection emitter in init.rs:56-61/119-181, detached from the failing call. This is a genuine error-path conflation with a concrete trigger (any panic in a marshalled task, which the runtime explicitly anticipates via its panic hook and payload formatter), and a clearly better contract exists (deliver the panic outcome through the result channel or a distinct error type). Low severity is appropriate: no unsoundness, only misleading diagnostics.

  </details>

- [x] **RS-027** · `src/module/call.rs:96` · memory-leak — **FFI call return-value decode failure leaks the transfer-full result**

  CallRequest::execute performs the native call, then decodes the return value; if decoding fails, the function returns the error and the raw result is abandoned. For transfer-full return types (caller-owned strings, boxed values, GObjects) the returned pointer is never freed or unreffed, so every decode failure on an ownership-transferring call leaks the native allocation. The crate's own test demonstrates the leaking path: g_strdup's owned return is decoded with an out-of-bounds size index and the duplicated string is dropped on the floor.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/module/call.rs:89-99: `let result = self.result_type.call_cif(&cif, symbol_ptr, &ffi_args)...?; let ref_updates = self.collect_ref_updates(&ffi_values)?; let return_value = Value::from_ffi_value_with_args(&result, &self.result_type, &ffi_values, &self.args).with_context(|| format!("decoding return value of {}", self.symbol_name))?;`. Ownership trace: `result` is a plain ffi::FfiValue::Ptr with no Drop semantics (src/types.rs:200-203); ownership is only assumed during from_ffi_value_with_args. When that returns Err — exercised by the test at src/module/call.rs:233-258 calling g_strdup with `ArrayKind::Sized { size_index: 9 }` — the `?` propagates and the g_strdup'd allocation (or, in general, any transfer-full GObject/boxed return) is leaked. collect_ref_updates failing at line 94 leaks the same way.

  **Verifier:** The finding stands. In CallRequest::execute (src/module/call.rs:89-99) the native call's return value `result` is a plain ffi::FfiValue::Ptr produced by the default FfiEncoder::call_cif (src/types.rs:200-203); FfiValue has no Drop impl (src/ffi/value.rs:8-25), so ownership of a transfer-full return is consumed only inside Value::from_ffi_value_with_args. Every attempted refutation fails: (1) decode of transfer-full returns IS fallible before ownership is consumed — ArrayType::decode_with_context for Sized arrays calls size_from_args (src/types/array.rs:800-801) which bails on an out-of-bounds/invalid size index (array.rs:1070-1075) before any g_free; decode_glist propagates an item-decode error (array.rs:844) before g_list_free at 849-855 (a transfer-full GList whose GObject item has a null g_class bails in load_type_class, src/types/gobject.rs:26-30, leaking the spine whose free would have been safe); decode_gptrarray (array.rs:893) and decode_garray (array.rs:865) likewise fail before their unrefs; BoxedType::decode fails when freeFn cannot be resolved (src/types/boxed.rs:94-109); FundamentalType::decode fails on lookup_fns (src/types/fundamental.rs:53-58). (2) collect_ref_updates at call.rs:94 runs BEFORE the return decode, so a Ref out-param decode failure abandons an entirely valid transfer-full return that no codec ever touched — the "descriptor is untrustworthy so leaking is the safe choice" defense does not apply to that ordering, nor to the skipped container-spine frees. (3) The error propagates through ModuleRequest::dispatch (src/module/handler.rs:27-38) straight to JS with no cleanup hook. One evidence correction: the cited test (call.rs:233-258) passes Value::Null so g_strdup(NULL) returns NULL and declares Ownership::Borrowed — that test leaks nothing itself; it only proves the decode-failure path through execute is reachable. With a non-null argument and ownership:"full" on the return descriptor, the same path leaks the allocation. Severity low (leak confined to error/edge paths) is appropriate.

  </details>

- [x] **RS-028** · `src/module/init.rs:42` · logic-bug — **init has no lifecycle guard: re-init after stop returns a dead runtime, and double init leaks the first GLib thread via set_handle's silent replace**

  init never resets Mailbox::stopped, so calling init after stop spawns a fresh GLib thread and returns a valid-looking MainLoop handle while every subsequent dispatch is silently dropped by schedule_glib's stopped check, surfacing only as 'GLib thread disconnected' errors on each call — the runtime is unrecoverable despite init reporting success (Mailbox::reset_for_test exists but is test-only by its own doc). Calling init twice without stop — the double-module-load hazard the @gtkx/vitest plugin's single-module-identity inlining exists to prevent, reachable whenever two copies of the FFI package load the same .node binary — spawns a second OS thread running a second glib::MainLoop against the same default main context, and GlibThread::set_handle uses Option::replace, silently dropping the first thread's JoinHandle. stop()'s GlibThread::join then only joins the most recent thread, so shutdown returns while the first thread is still alive, contending ownership of the default GMainContext and holding GLib state past the point the runtime reports itself stopped. Neither sequence is detected or rejected.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/module/init.rs:74-107 spawns the thread and calls `GlibThread::global().set_handle(handle)` unconditionally, with no once-guard and no stopped reset. state.rs:40-45: `pub fn set_handle(&self, handle: JoinHandle<()>) { self.handle.lock().unwrap_or_else(std::sync::PoisonError::into_inner).replace(handle); }` — `replace` drops the prior JoinHandle, detaching its thread. state.rs:47-60: `pub fn join(&self) -> Option<String> { let handle = ... .take(); ... }` — only the stored (latest) handle is joined. The stopped flag is set in src/module/stop.rs:44 via mark_stopped and cleared only by src/dispatch.rs:170-172 `reset_for_test` ("Intended for tests"), which init never calls. After stop→init, src/dispatch.rs:225-227 `if self.stopped.load(Ordering::Acquire) { return; }` drops every task, so each export fails in dispatch_to_glib_and_wait with GlibDisconnectedError while the new main loop runs idle forever. Double-init sequence: init() spawns thread A and stores its handle; a second init() spawns thread B running `main_loop.run()` on the same default context and set_handle drops A's JoinHandle; stop() (module/stop.rs:51) quits one loop and joins only B; thread A remains alive and unjoinable with no remaining reference to it.

  **Verifier:** Every factual claim checks out. (1) stop→init: stop.rs:44 sets Mailbox::stopped via mark_stopped; init.rs:42-117 never clears it — the only clearing path, reset_for_test (dispatch.rs:170-172), is documented test-only, is not a napi export, and is called solely from Rust tests (tests/dispatch.rs:67,122, tests/managed.rs:205). A post-stop init still returns Ok because its startup barrier uses glib::idle_add_once directly (init.rs:83-88), bypassing the mailbox; afterward schedule_glib (dispatch.rs:225-227) silently drops every task, which drops the result tx in dispatch_to_glib_and_wait (js_bridge.rs:76-82), so wait_for_glib_result returns GlibDisconnectedError (js_bridge.rs:101) on every call — init reports success for a permanently dead runtime, and a second stop also fails through the stopped mailbox, leaving the new GLib thread running forever. (2) Double init: init.rs:107 calls GlibThread::set_handle unconditionally; set_handle (state.rs:40-45) uses Option::replace, dropping the first JoinHandle and detaching thread A, and join (state.rs:47-60) only takes the latest handle, so stop joins only thread B while a second glib::MainLoop contends for the same default GMainContext. The trigger is concrete: native.init() runs at facade module evaluation (index.ts:146), and double evaluation of the facade against one cached native binding is exactly the hazard the @gtkx/vitest single-module-identity inlining exists to prevent — the native layer itself has no guard and neither detects nor rejects either sequence. This is a genuine lifecycle/contract defect (silent success on an unrecoverable runtime, leaked OS thread), not pedantry or anything clippy/rustfmt covers; low severity is appropriate since the supported facade path avoids both sequences.

  </details>

- [x] **RS-029** · `src/module/toggle_ref.rs:87` · non-idiomatic — **Pointers, GTypes, and opcodes cross the JS boundary as f64**

  *Resolution:* the opcode half is fully adopted — `RefOp` is a `#[repr(u32)]` enum, `apply_wrapper_ref_op` takes `op: u32` and dispatches through a compile-checked `match` on `RefOp::from_opcode` with the documented silent no-op fallback for unknown values. The `ref_ptr`/GType f64 transport is retained: switching to BigInt would change the JS-visible callback protocol and the `Value` IR for a hazard with no trigger on current Linux user-space layouts (the verifier's own assessment), so the exact-integer transport remains future work if 57-bit addressing becomes relevant.

  Raw napi_ref pointers, GType values, and reference-operation opcodes are all marshaled as f64, with float equality used to dispatch opcodes. An f64 represents integers exactly only up to 2^53; current Linux user-space addresses fit, but 57-bit virtual addressing (5-level paging) and tagged-pointer configurations can exceed that, at which point a pointer silently round-trips to a different address. napi-rs supports i64/u64/BigInt for exact integer transport, and the three opcodes (OP_WEAKEN/OP_STRENGTHEN/OP_DELETE defined as f64 consts 0.0/1.0/2.0) should be an enum with integer discriminants so the `op == OP_STRENGTHEN` float comparisons and the silent no-op fallback for unknown opcodes become a compile-checked match.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/module/toggle_ref.rs:87: `pub fn apply_wrapper_ref_op(env: Env, ref_ptr: f64, op: f64)` with 91-97 dispatching via `if op == toggle_ref::OP_STRENGTHEN { ... } else if op == toggle_ref::OP_WEAKEN { ... }` and `let raw_ref = ref_ptr as usize as sys::napi_ref;`. src/toggle_ref.rs:97-101: `pub const OP_WEAKEN: f64 = 0.0; pub const OP_STRENGTHEN: f64 = 1.0; pub const OP_DELETE: f64 = 2.0;` and 219: `let args = vec![Value::Number(ref_ptr as usize as f64), Value::Number(op)];` — a live napi_ref pointer encoded into a double. Same pattern for GTypes: src/module/handler.rs:72-79 `impl ModuleResponse for u64 { ... f64::to_napi_value(env.raw(), self as f64) }` and src/module/register_class.rs:471-475 `parent_gtype: f64` / `parent_gtype as usize`.

  **Verifier:** Every cited line is accurate. module/toggle_ref.rs:87 declares `apply_wrapper_ref_op(env: Env, ref_ptr: f64, op: f64)`, line 88 rebuilds the pointer with `ref_ptr as usize as sys::napi_ref`, and lines 91-97 dispatch via float equality against `OP_STRENGTHEN`/`OP_WEAKEN`/`OP_DELETE` with a silent fall-through. toggle_ref.rs:97-101 defines the opcodes as f64 consts 0.0/1.0/2.0, and line 219 packs a live napi_ref heap pointer into `Value::Number(ref_ptr as usize as f64)`. The same channel carries GTypes: handler.rs:72-78 returns u64 GTypes as `self as f64`, and register_class.rs:474/482 accepts `parent_gtype: f64` and casts to usize — dynamically registered GTypes are heap addresses, so pointers transit as doubles in both directions. This is not something the CI lint gate catches: Cargo.toml:47,54 explicitly allows `cast_precision_loss` and `float_cmp` crate-wide, silencing the exact pedantic lints that would have surfaced the pattern. The design critique is sound: f64 represents integers exactly only to 2^53, napi-rs 3 supports i64/u64/BigInt for exact transport, the crate already uses External<NativeHandle> for exact handle transport elsewhere, and the Value enum's Number-only numeric variant is a self-imposed constraint. The opcodes belong in an integer-discriminant enum matched on the Rust-internal path. Mitigating nuance, consistent with the low rating: 0.0/1.0/2.0 compare exactly in IEEE 754, Linux user-space allocations stay below 2^47 by default even with 5-level paging (high addresses need an explicit mmap hint) so no corruption trigger exists today, and the silent no-op for unknown opcodes is documented as deliberate defense at module/toggle_ref.rs:79-83 (a JS-facing parameter can never be range-restricted at compile time, so a fallback arm survives any refactor). The finding is factually correct, not governed by the existing clippy/rustfmt gates, and identifies genuinely non-idiomatic exact-integer-over-f64 transport; it stands as low/non-idiomatic.

  </details>

- [x] **RS-030** · `src/toggle_ref.rs:276` · memory-leak — **install() consumes exactly one pending reference, leaking when one crossing decodes the same untracked GObject twice**

  The decode/install protocol leaves one pending owned reference per decode (types/gobject.rs:71-77 takes g_object_ref for every borrowed decode of an untracked object), but install releases exactly one reference per object, and the JS registry calls setWrapper only for the first wrapper (registry.ts resolveWrapper short-circuits on getWrapper). When a single JS<->GLib crossing carries the same untracked GObject pointer in two slots — an array return or callback argument list containing a duplicate, e.g. a Gio.ListStore holding the same item twice decoded in one batch, or a multi-argument signal delivering one object in two GObject-typed parameters — both decodes run on the GLib thread before any install, both observe has_wrapper()==false, and both take a pending reference. The single install consumes one; the second is never released, permanently pinning the object's refcount so it can never be finalized.

  <details><summary>Evidence and verification</summary>

  **Evidence:** install doc lines 253-256: "On a fresh bind (no cell in qdata) the decode path left exactly one pending owned reference on the object ... then releases that one pending reference". Decode side (types/gobject.rs:75-77): `} else { unsafe { glib::gobject_ffi::g_object_ref(gobject_ptr) }; }` executes once per decoded occurrence. Trace: one call decodes [O, O] with O untracked -> two g_object_ref -> JS resolves the first handle, creates wrapper, setWrapper -> install releases one reference (line 310); the second handle hits registry.ts:214-215 `const existing = getWrapper(handle); if (existing) return existing;` so no second install ever runs and the second reference leaks.

  **Verifier:** The finding stands; no refuting invariant exists. (1) Per-occurrence pending ref: types/gobject.rs:64-77 leaves one owned reference per decode of an untracked object (g_object_ref at line 76 for borrowed; kept/sunk ref for transfer-full), gated only on has_wrapper(), which is set solely by install. (2) Both decodes precede any install: array decodes (array.rs decode_glist:833-858, decode_gptrarray:881-902, decode_null_terminated_ptr_array:930-949, decode_contiguous Pointer arm:526-531) and trampoline argument marshalling (trampoline.rs:119-144, single JS invocation at 157) run per-element/per-arg synchronously on the GLib thread within one crossing, while install only runs later via set_wrapper's dispatch_to_glib_and_wait (module/toggle_ref.rs:161-165) from the JS thread — so duplicate slots of the same untracked pointer each take a pending ref. (3) Exactly one consumed: install (toggle_ref.rs:276-313) executes a single g_object_unref at line 310; its doc (253-256) and set_wrapper's doc explicitly assume "exactly one pending owned reference". (4) No second install and no compensating release: registry.ts resolveWrapper (213-215) short-circuits on getWrapper; get_wrapper (module/toggle_ref.rs:184-208) is a pure read; NativeHandle::borrowed_gobject (managed.rs:126-132) has a no-op Drop (158-170); Value::to_js_value (value.rs:316-321) creates independent Externals with no dedup; all later crossings of the now-tracked object are self-balanced (gobject.rs:64-70). The stray ref permanently keeps the refcount at >= 2 (toggle ref + leaked ref), so on_toggle_notify never reports is_last_ref, the wrapper stays strong, and both the GObject and its JS wrapper can never be collected. Trigger class is reachable on edge paths: a GList/GPtrArray/array return containing the same untracked object twice (e.g. Gtk.Widget.listMnemonicLabels() after addMnemonicLabel() called twice with the same label — GTK appends without dedup, transfer-container with borrowed elements), or a callback delivering the same instance in two GObject-typed parameters. The finding's Gio.ListStore illustration is inapt (no bulk-get API), but the mechanism and the list-return/trampoline triggers are correct, consistent with low severity.

  </details>

- [x] **RS-031** · `src/trampoline.rs:243` · undefined-behavior — **Trampoline closure handler writes narrow integer returns without widening to ffi_arg, violating the libffi closure return contract**

  libffi's closure contract requires the handler to store integral results narrower than ffi_arg as a full widened ffi_arg into the return slot. trampoline_handler types the slot as `&mut u64` (uninitialized memory supplied by the libffi closure stub) and delegates to write_return_to_raw_ptr, which for IntegerKind writes only the narrow type (`ptr.cast::<$ty>().write_unaligned(value as $ty)` — 1-4 of the 8 bytes), BooleanType writes a bare i32, and UnicharType a bare u32, leaving the rest of the slot as stack garbage. The CIF return type is the narrow libffi type (e.g. FFI_TYPE_SINT32 for the very common gboolean-returning signal callbacks such as GtkWindow::close-request). Current libffi unix64/aarch64 epilogues happen to do exact-width loads at offset 0, so this works on the supported little-endian targets, but it reads uninitialized bytes, and on big-endian 64-bit ports (e.g. s390x) the widened value's significant bytes live at the high addresses of the slot, so a JS callback returning gboolean/int delivers garbage to GTK. A latent ABI-contract violation in code that is otherwise platform-generic, invisible to ASan/Miri because the read happens inside libffi's assembly.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/trampoline.rs:243-249: `unsafe extern "C" fn trampoline_handler(_cif: &ffi_cif, result: &mut u64, args, data) { let state_ptr = unsafe { data.handle_call(args, result as *mut u64 as *mut c_void) }; }` → src/trampoline.rs:167 `self.return_type.write_return_to_raw_ptr(result, &Ok(value))` → types/numeric.rs:162-168: `fn write_return_to_raw_ptr(&self, ret: *mut c_void, value: &Result<value::Value, ()>) { ... self.write_ptr(ret as *mut u8, n); }` with write_ptr at numeric.rs:41-47 `ptr.cast::<$ty>().write_unaligned(value as $ty)`; types/boolean.rs:55-58 `unsafe { *(ret as *mut i32) = val as i32 };`; types/unichar.rs:63-69 `*(ret as *mut u32) = val`. The closure CIF return type is the narrow type: src/trampoline.rs:85 `let cif_return_type: libffi::Type = data_ref.return_type.libffi_type();`. Only the low 1/2/4 bytes of the ffi_arg buffer are ever written for narrow returns, so the libffi-mandated widened ffi_arg store never happens.

  **Verifier:** Confirmed as a real latent contract violation, correctly graded low. (1) The libffi closure contract is genuine: handlers must store integral returns narrower than the register as a full widened ffi_arg/ffi_sarg. The libffi-rs crate itself documents and implements this in its high layer (libffi-5.1.0/src/high/types.rs:49-78: "The low-level libffi library implicitly extends small integer return values to ffi_arg or ffi_sarg", with u8/u16/u32 -> ffi_arg, i8/i16/i32 -> ffi_sarg), while middle::Closure::new (the layer gtkx uses) passes the callback straight to ffi_prep_closure_loc with no widening shim. (2) gtkx violates it: trampoline_handler (src/trampoline.rs:243-249) forwards the raw slot to write_return_to_raw_ptr, and the codecs write only the narrow width — boolean.rs:57 `*(ret as *mut i32) = val as i32`, numeric.rs:162-167 via write_ptr `write_unaligned(value as $ty)`, unichar.rs:69 `*(ret as *mut u32) = val` — against a CIF whose return type is the narrow libffi type (trampoline.rs:85; BooleanType::libffi_type() = Type::i32()). The path is common: signal handlers are these closures (g_signal_connect_data receives the trampoline code_ptr, packages/ffi/src/signals.ts:73-86), so every gboolean-returning signal (close-request, key-pressed, GSourceFunc, tick callbacks) exits through it. (3) The big-endian trigger is mechanically verified in the bundled libffi: libffi-sys-4.1.0/libffi/src/aarch64/sysv.S loads closure UINT32/SINT32 returns with `ldr w0, [x3, #BE(4)]` where BE(X)=X under __AARCH64EB__ — offset 4, written only by a full widened ffi_arg store — so on aarch64_be a JS gboolean return delivers 4 uninitialized stack bytes to GTK. (4) The only viable defense was checked: the package ships only x86_64/aarch64 little-endian targets (packages/native/package.json:45-48), where the verified epilogues (unix64.S load_table `movl (%rsi), %eax` etc.; aarch64 BE(X)=0) do exact-width loads at offset 0, so behavior today is correct and — contrary to one parenthetical in the finding — no uninitialized byte is actually read on shipped targets. But that is reliance on an implementation detail two ports happen to share, explicitly outside the dependency's documented "must"; libffi-sys updates arrive via the `libffi = "5.1.0"` semver range, and nothing in the code gates the narrow write to the ports that tolerate it. No invariant makes the narrow write contractually correct, so the finding stands at the stated low severity: a latent ABI-contract violation, benign on every shipped configuration, value-corrupting on big-endian 64-bit ports.

  </details>

- [x] **RS-032** · `src/types.rs:68` · non-idiomatic — **JS descriptor parsing swallows real errors via .ok().flatten() and error-discarding map_err**

  Optional descriptor properties are read with `get_named_property::<Option<T>>(..).ok().flatten()`, which converts a type error (e.g. `elementSize: 'four'`, `optional: 'yes'`, or `optional: 1`) into a silent default instead of an InvalidArg error, and several required-property reads discard the underlying napi error and replace it with a generic 'property is required' message even when the property exists but is malformed. Both patterns hide the actual defect from the JS caller and make descriptor bugs in the generated bindings surface as misbehavior — wrongly-sized arrays, or a non-optional default that makes a legitimate null argument later fail encoding with an unrelated message (e.g. 'Expected a Number for integer type, got Null') pointing at the value instead of the malformed descriptor. Distinguishing absent (Ok(None)) from malformed (Err) and propagating the napi error with context is the idiomatic shape.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/types.rs:68-73: `Type::from_js_value(env, return_type_prop).map_err(|_| napi::Error::new(napi::Status::InvalidArg, format!("'returnType' property is required for {kind} types")))` — a malformed returnType reports 'is required'. src/types.rs:148-151: `obj.get_named_property::<Option<String>>("ownership").map_err(|_| missing())?` — same masking. Silent-default sites: src/arg.rs:51-55 `let optional = obj.get_named_property::<Option<bool>>("optional").ok().flatten().unwrap_or(false);` (failure chain: descriptor `{ type: t.int32, value: null, optional: 1 }` → get_named_property returns Err → .ok() discards → optional = false → IntegerKind::encode bails 'Expected a Number for integer type, got Null', misattributing the bug); src/types/array.rs:105-109 (elementSize); src/types/trampoline.rs:51-66 (hasDestroy, userDataIndex, scope); src/types/boxed.rs:42-55 (library, getTypeFn, freeFn); src/types/string.rs:20-24 (length). Trigger: `call(..., [{type:{type:'array', itemType:..., kind:'garray', ownership:'borrowed', elementSize:'8'}, ...}])` silently ignores elementSize and falls back to the item-derived size.

  **Verifier:** Every cited site exists exactly as claimed. src/types.rs:68-73 wraps any Type::from_js_value failure for 'returnType' in a generic "'returnType' property is required" InvalidArg, so a present-but-malformed descriptor ({type:'bogus'} whose real error is "Unknown type: bogos") is misreported as absent; src/types.rs:148-151 does the same for 'ownership'. In napi-rs, get_named_property::<Option<T>> returns Ok(None) for absent/null/undefined and Err only when the property is present with the wrong type (bool/f64/String FromNapiValue do not coerce), so .ok().flatten() specifically converts the malformed-present case into a silent default. Confirmed silent-default sites: src/arg.rs:51-55 ('optional': a non-boolean like 1 silently becomes false, making a legitimate null value later fail in the codec with "Expected a Number for integer type, got Null", blaming the value instead of the descriptor); src/types/array.rs:105-109 ('elementSize': a string '8' silently becomes None and encode_garray at lines 736-745 falls back to the item-derived size); src/types/trampoline.rs:51-66 ('hasDestroy', 'userDataIndex', 'scope' — scope silently falling back changes trampoline state lifetime handling in encode at lines 128-169); src/types/boxed.rs:42-55 ('freeFn' silently dropping the custom destructor, despite the crate's own boxed_with_free_fn doc comment at lines 91-93 stressing that the wrong destructor must never be substituted); src/types/string.rs:20-24 ('length'). Neither clippy nor rustfmt governs .ok().flatten() or error-discarding map_err. Distinguishing Ok(None) from Err and propagating the napi error with context is the idiomatic shape napi-rs's Option decoding is designed for, and the current pattern makes codegen descriptor bugs surface as misattributed runtime misbehavior. Accurate claim, concrete trigger chains, correctly scoped as non-idiomatic/low.

  </details>

- [x] **RS-033** · `src/types/array.rs:641` · undefined-behavior — **encode_garray copies a JS-controlled element size out of a fixed 8-byte stack buffer (out-of-bounds read)**

  encode_garray sizes the GArray with `self.element_size` when the JS descriptor supplies an `elementSize` override, taking precedence over the codec-derived per-item size (`self.element_size.or_else(|| self.item_element_size())`). The append helpers, however, stage each element in storage sized for the codec type only: append_integer_values_to_garray uses an 8-byte stack buffer, and the float/boolean/handle/string helpers pass the address of a 4-8 byte stack scalar. g_array_append_vals copies `len * element_size` bytes from the supplied address, so a descriptor declaring e.g. `{type:'array', kind:'garray', itemType:{type:'int32'}, elementSize: 64}` makes GLib read 56 bytes past the end of `buf` (and past the stack scalars in the other helpers) — an out-of-bounds read of stack memory, undefined behavior. The mismatch between the override and the staging-buffer size is never validated, in contrast with the crate's habit of validating JS-supplied layout data (validate_vfunc_offset, IntegerKind::check_range, ArrayType::validated_size).

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/types/array.rs:641-647: `let mut buf = [0u8; size_of::<i64>()]; for n in Self::extract_numbers(array)? { int_type.write_ptr(buf.as_mut_ptr(), n); unsafe { glib::ffi::g_array_append_vals(g_array, buf.as_ptr() as *const c_void, 1); } }` combined with src/types/array.rs:736-748: `let element_size = self.element_size.or_else(|| self.item_element_size())...; let g_array = unsafe { glib::ffi::g_array_sized_new(0, 0, element_size as u32, array.len() as u32) };`. Trigger chain: JS calls `call(lib, sym, [{type:{type:'array', kind:'garray', itemType:{type:'int32'}, elementSize:64, ownership:'borrowed'}, value:[1]}], ...)` → ArrayType::encode → encode_garray creates a GArray with g_elt_size=64 → append_integer_values_to_garray appends 1 element → g_array_append_vals memcpy's 64 bytes from the 8-byte `buf` → 56-byte out-of-bounds stack read inside GLib, with the garbage bytes then stored in the GArray handed to the callee.

  **Verifier:** The finding stands; refutation failed. Trace: (1) encode_garray (src/types/array.rs:736-748) sizes the GArray via `self.element_size.or_else(|| self.item_element_size())`, so a JS-supplied `elementSize` (e.g. 64) becomes the GArray's elt_size: `g_array_sized_new(0,0, element_size as u32, array.len() as u32)`. (2) append_integer_values_to_garray (array.rs:635-648) stages each int into a fixed `let mut buf = [0u8; size_of::<i64>()]` (8 bytes) and calls `g_array_append_vals(g_array, buf.as_ptr(), 1)`. (3) GLib's g_array_append_vals memcpy's `elt_size * len` bytes from the source (`g_array_elt_len = elt_size*len`), i.e. 64 bytes from an 8-byte stack buffer = a 56-byte out-of-bounds stack read. The float/boolean/handle/string append helpers (array.rs:650-732) have the same defect with their smaller stack scalars. Reachability and lack of validation are confirmed: ArrayType::from_js_value (array.rs:105-109) reads `elementSize` unconditionally for every kind with no check against item type or kind, unlike the crate's other JS-layout validators (validated_size at array.rs:1058). The napi `call` export (src/module/call.rs:141-163) parses arbitrary descriptor objects through Type::from_js_value with no schema enforcement, so the finding's exact malformed descriptor reaches encode_garray. Moreover the public hand-written-binding helper t.garray(itemType, ownership, elementSize?) (packages/ffi/src/helpers.ts:171-172) explicitly exposes and documents `elementSize` ("Size of each element in bytes (used for `garray`)", helpers.ts:144), so even a legitimate hand-written binding using the documented parameter triggers the OOB because the append helpers never respect the override for the staging buffer — an unsound public contract. Refutations fail: codegen never emits elementSize for garray (value.ts:80-81 and value.ts:445), but codegen is not the only caller; nothing clamps elementSize to the codec size (the or_else is precedence, not validation); and g_array_append_vals copies elt_size, not the source length. ASan/Miri default suites never construct this descriptor combination, so they cannot observe the over-read. Severity-low is appropriate since the normal codegen path never hits it, but it is a genuine, concretely-triggerable OOB read.

  </details>

- [x] **RS-034** · `src/types/array.rs:774` · logic-bug — **Zero-terminated arrays of scalar elements are decoded as pointer arrays: wrong stride, guaranteed error, possible OOB read**

  ArrayType::decode for ArrayKind::Array with a raw pointer routes every non-string item type to decode_null_terminated_ptr_array, which walks the buffer in pointer-sized (8-byte) strides and decodes each element as FfiValue::Ptr. For a zero-terminated array of i32/u32/boolean elements (which GIR does describe), the stride is wrong and IntegerKind::decode(FfiValue::Ptr) always bails ("Expected a numeric FfiValue"), so such returns can never be decoded; additionally the very first 8-byte read can run past the allocation of a short scalar array (an empty zero-terminated gint array is a 4-byte allocation, but `*ptr_array.offset(0)` reads 8 bytes). The transfer-full container free is also skipped because the error propagates before the g_free at line 944-946.

  <details><summary>Evidence and verification</summary>

  **Evidence:** Lines 774-784: `if let ffi::FfiValue::Ptr(ptr) = ffi_value { if ptr.is_null() { return Ok(value::Value::Array(vec![])); } if matches!(&*self.item_type, Type::String(_)) { return Ok(self.decode_null_terminated_string_array(*ptr)); } return self.decode_null_terminated_ptr_array(*ptr); }` — there is no scalar branch. decode_null_terminated_ptr_array lines 930-941: `let ptr_array = ptr as *const *mut c_void; ... let item_ptr = unsafe { *ptr_array.offset(i) }; ... values.push(self.item_type.decode(&item_ffi)?);` — `*ptr_array.offset(0)` is an 8-byte read regardless of element size, and FfiValue::to_number (src/ffi/value.rs:132-147) bails for Ptr, so Integer/Boolean/Float item decode of FfiValue::Ptr always errors.

  **Verifier:** Every factual element of the claim checks out against the current code.

1. Routing: /home/eugenio/gtkx/packages/native/src/types/array.rs:774-784 — for ArrayKind::Array (and Sized/Fixed reaching plain decode) with FfiValue::Ptr, the only special case is `matches!(&*self.item_type, Type::String(_))`; every other item type, including Integer/Float/Boolean/Tagged, falls through to `decode_null_terminated_ptr_array(*ptr)`. There is no scalar branch, even though ItemCodec::resolve (lines 181-198) and the encode path (lines 560-570) treat scalars as valid array elements, and decode_contiguous (lines 494-535) correctly handles scalars for the sized/fixed/GArray layouts. The asymmetry is real.

2. Wrong stride and guaranteed failure: decode_null_terminated_ptr_array (array.rs:930-942) casts to `*const *mut c_void` and reads `*ptr_array.offset(i)` — pointer-sized (8-byte) strides — then decodes each chunk as FfiValue::Ptr. IntegerKind/FloatKind decode through impl_numeric_codecs (src/types/numeric.rs:139-143) which calls `ffi_value.to_number()`, and to_number bails on Ptr (src/ffi/value.rs:144-146: "Expected a numeric FfiValue"). BooleanType::decode (src/types/boolean.rs:33-36) bails on anything but I32. So a zero-terminated scalar array can never decode successfully: the first non-zero 8-byte chunk produces an error.

3. Reachability: ArrayType implements only `encode` for FfiEncoder (array.rs:122-126), inheriting the default call_cif (src/types.rs:194-203) which yields FfiValue::Ptr for array-typed returns, so returns of this shape do arrive at the cited branch. The codegen emits exactly this descriptor — packages/codegen/src/writers/value.ts:439-446 falls back to `t.array(element, "array", ownership)` for any GIR array lacking a length parameter and fixed size, with no restriction on scalar elements — and GIR does describe zero-terminated scalar arrays.

4. OOB read: for an empty transfer-full zero-terminated gint array (a 4-byte allocation holding only the terminator), `*ptr_array.offset(0)` is an 8-byte read overrunning the allocation by 4 bytes. ASan/Miri cannot catch this on default test paths because no passing test can exist for a combination that always errors.

5. Leak on error: the `?` at array.rs:940 propagates before the `g_free(ptr)` at lines 944-946, so a transfer-full container is leaked whenever item decode fails — which for scalar items is every time.

This is a genuine logic gap (decode contract broken for a type combination the encoder and type model advertise as supported), with a concrete trigger chain, not pedantry or anything clippy/rustfmt governs. The low severity is appropriate given the rarity of zero-terminated scalar arrays in practice.

  </details>

- [x] **RS-035** · `src/types/boxed.rs:265` · non-idiomatic — **expect() on invariants enforced at a distance in Boxed construction paths**

  StructType::decode and StructType::ptr_to_value call `Boxed::from_glib_none_with_size(...).expect("... always succeeds")`, and BoxedType::boxed_with_free_fn does `self.free_fn.as_deref().expect("boxed_with_free_fn called without freeFn set")`. Each expect is currently correct, but the invariant lives in a different function: from_glib_none_with_size only fails when both gtype and size are None, and boxed_with_free_fn is only called behind free_fn.is_some() checks at two call sites. Any future edit to either side turns these into library panics. Encoding the invariant in the API removes the expects: an infallible `Boxed::copy_with_size(ptr: *mut c_void, size: usize) -> Boxed` constructor for the sized case, and passing the resolved `&str` free-fn name into boxed_with_free_fn as a parameter.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/types/boxed.rs:264-266: `Some(_) => Boxed::from_glib_none_with_size(None, struct_ptr, self.size, None).expect("struct decode with a known size always succeeds")` and 278-280 (same pattern in ptr_to_value); src/types/boxed.rs:96-99: `let free_fn_name = self.free_fn.as_deref().expect("boxed_with_free_fn called without freeFn set");`. The guarded fallibility lives in src/managed/boxed.rs:82-103, where the bail arm (`Cannot copy boxed type ...`) fires only for `gtype == None && size == None` — a relationship the call sites must re-prove by reading both files.

  **Verifier:** Factually verified against the current code. src/types/boxed.rs:263-265 (StructType::decode) and 277-279 (ptr_to_value) both read `Some(_) => Boxed::from_glib_none_with_size(None, ptr, self.size, None).expect(...)`; src/types/boxed.rs:96-99 reads `self.free_fn.as_deref().expect("boxed_with_free_fn called without freeFn set")`. The infallibility proof for the StructType expects lives in src/managed/boxed.rs:71-103, where the `bail!("Cannot copy boxed type ...")` arm fires only when gtype is None AND size is None — a cross-module relationship the call sites must re-prove, and one that any future edit to from_glib_none_with_size (reordering checks, adding validation) silently converts into reachable panics in FFI decode paths on the GLib thread. One evidence detail is overcounted: boxed_with_free_fn has one call site (decode, line 175), not two — the second free_fn.is_some() check at line 193 guards Boxed::from_ptr_unowned instead. The substance stands regardless: the single call site guards with is_some() while the callee re-asserts the same fact with expect, the cross-function is_some-then-expect split that clippy's unnecessary_unwrap cannot detect across function boundaries. The StructType arms additionally match `Some(_)` while discarding the binding and re-passing self.size as an Option into a fallible API — the idiomatic form binds the value (`Some(size) => copy_with_size(ptr, size)`), making the sized-copy constructor infallible by signature and deleting both expects; likewise passing the resolved &str into boxed_with_free_fn fuses guard and use. Neither rustfmt nor clippy -D warnings governs this, the proposed restructuring is concrete and strictly better, and the low/non-idiomatic severity is proportionate. This is a genuine make-the-invariant-structural finding, not pedantry.

  </details>

- [x] **RS-036** · `src/types/hashtable.rs:23` · non-idiomatic — **PartialEq for HashTableEntryEncoder compares only discriminants, equating PtrArray of different item types**

  The manual PartialEq impl uses std::mem::discriminant, so PtrArray(Box::new(Integer)) compares equal to PtrArray(Box::new(Boolean)). An equality impl that ignores payload violates the expectation that == means structural equivalence and is a trap for future code that compares encoders to decide marshalling compatibility (two 'equal' encoders would produce differently-typed GPtrArrays). The impl exists only to serve test assertions. Idiomatic alternatives: derive PartialEq once Type can support it, expose a kind() discriminant accessor, or have tests use matches! (which several already do for the PtrArray case).

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/types/hashtable.rs:23-27: `impl PartialEq for HashTableEntryEncoder { fn eq(&self, other: &Self) -> bool { std::mem::discriminant(self) == std::mem::discriminant(other) } }` while the variant is `PtrArray(Box<Type>)` (line 20). Usage trace: no `==` on the type anywhere in src/; only tests/hashtable.rs uses it, and tests/hashtable.rs:520-523 explicitly asserts the surprising behavior: `assert_eq!(HashTableEntryEncoder::PtrArray(Box::new(Type::Integer(IntegerKind::I32))), HashTableEntryEncoder::PtrArray(Box::new(Type::Boolean(BooleanType))))`.

  **Verifier:** Verified against src/types/hashtable.rs: lines 23-27 implement PartialEq via std::mem::discriminant while line 20 declares PtrArray(Box<Type>), so PtrArray(Box::new(Type::Integer(IntegerKind::I32))) == PtrArray(Box::new(Type::Boolean(BooleanType))) holds despite different payloads. Grep over src/ confirms no production code ever compares encoders with ==; the only equality consumers are test assertions in tests/hashtable.rs, and the test encoder_partial_eq_compares_by_discriminant (lines 518-528) explicitly asserts the cross-payload equality the finding calls out. The same test file already uses matches! for the PtrArray case (line 500), so the proposed idiomatic alternative is in use; Type derives only Debug, Clone (src/types.rs:262-264), matching the finding's hedge that a derive needs Type to support PartialEq first. A discriminant-only PartialEq on a public payload-carrying enum (re-exported via src/types.rs:99) violates the conventional structural-equality contract of ==, is invisible to clippy/rustfmt, and exists solely to serve test assertions — a genuine non-idiomatic design at the claimed low severity. Minor nuance not affecting the verdict: encode (line 109) currently ignores the payload (_item_type), so the harm is correctly framed as a trap for future code instead of a present bug.

  </details>

- [x] **RS-037** · `src/types/numeric.rs:118` · logic-bug — **Float encode silently converts a GObject handle's address into the f64 argument**

  The impl_numeric_codecs macro's encode arm `value::Value::Object(handle) => handle.ptr_as_usize() as f64` exists for integer kinds (pointer-as-uintptr arguments) but the macro is also instantiated for FloatKind (line 347). FloatKind::checked_to_ffi_value performs no integrality check, so passing a GObject/boxed handle where a float32/float64 argument is expected silently encodes the wrapper's heap address (~1.4e14) as the floating-point argument instead of raising the type error every other mismatched Value variant gets. A JS-side argument-ordering mistake involving an object and a number therefore produces a garbage geometry/opacity value fed straight into GTK with no diagnostic, whereas the same mistake with a string fails loudly via the `_ => bail!` arm.

  <details><summary>Evidence and verification</summary>

  **Evidence:** numeric.rs:110-122 (inside impl_numeric_codecs, expanded for FloatKind at line 347 `impl_numeric_codecs!(FloatKind, "float");`): `let number = match value { value::Value::Number(n) => *n, value::Value::Object(handle) => handle.ptr_as_usize() as f64, value::Value::Null | value::Value::Undefined if optional => 0.0, _ => bail!("Expected a Number for {} type, got {value:?}", $label), }; self.checked_to_ffi_value(number)` — and FloatKind::checked_to_ffi_value (numeric.rs:297-307) only range-checks magnitude for F32, so the address passes through unmodified for F64 and any address below f32::MAX for F32.

  **Verifier:** Every element of the claim is accurate against the current code. /home/eugenio/gtkx/packages/native/src/types/numeric.rs:108-123 defines impl_numeric_codecs whose encode body contains `value::Value::Object(handle) => handle.ptr_as_usize() as f64` (line 118), and the macro is instantiated for FloatKind at line 347 (`impl_numeric_codecs!(FloatKind, "float");`) in addition to IntegerKind at line 254. FloatKind::checked_to_ffi_value (lines 297-307) performs no integrality or plausibility check: F64 returns `Ok(ffi::FfiValue::F64(value))` unconditionally, and F32 only rejects finite values whose magnitude exceeds f32::MAX (3.4e38), which a 64-bit heap address (~1e12-1e14) never approaches. So a GObject/boxed handle passed where a float32/float64 argument is expected is silently encoded as the wrapper's address, while every other mismatched variant (string, boolean, array) hits the `_ => bail!("Expected a Number for {} type, got {value:?}", $label)` arm at line 120. The Object arm is intentional only for integer kinds: the doc comment on NativeHandle::ptr_as_usize (/home/eugenio/gtkx/packages/native/src/managed.rs:144-151) describes its purpose as "the numeric marshalling of pointer-valued arguments in `types::numeric`" — i.e., pointer addresses traveling as pointer-sized integers — a case with no float analogue. FloatKind inherits the arm purely because the macro body is shared. The asymmetry is real and consequential: other scalar codecs (string.rs, boolean.rs) contain no Value::Object arm and reject objects loudly, and even IntegerKind's I32/U32 paths would reject an address via check_range, but the float path feeds a garbage value (e.g., an opacity or coordinate of ~1.4e14) straight into the native call with no diagnostic. This is a genuine validation-logic gap arising from macro reuse, not a style preference and not something clippy governs. Low severity is appropriate since it requires a caller-side mistake that TypeScript typings would usually catch first, but the native encode path explicitly acts as a type validator for every other mismatch, so the silent hole is a real defect.

  </details>

- [x] **RS-038** · `src/types/string.rs:79` · memory-leak — **Trampoline string returns always g_strdup, leaking for declared transfer-none callbacks**

  StringType::write_return_to_raw_ptr ignores self.ownership and unconditionally g_strdups the JS return value before writing the pointer into the closure's return slot. For a callback whose GIR return is transfer-none — generated bindings have these, e.g. the GTranslateFunc trampolines in node_modules/.gtkx/gi/glib/glib.js lines 2034 and 2059, `t.trampoline([t.string("borrowed"), t.uint64], t.string("borrowed"), ...)` used by g_option_*_set_translate_func — the native caller treats the returned pointer as borrowed and never frees it, so every invocation of the JS callback leaks one heap string. Duplication is the memory-safe choice for the borrowed case (a pointer into the decoded Rust String would dangle), but the codec carries the ownership field and could retain the duplicate (e.g. per-trampoline last-return slot freed on the next call or on destroy) instead of leaking it; a translate function invoked per displayed string leaks proportionally to UI activity.

  <details><summary>Evidence and verification</summary>

  **Evidence:** string.rs:79-89: `fn write_return_to_raw_ptr(&self, ret: *mut c_void, value: &Result<value::Value, ()>) { let ptr = match value { Ok(value::Value::String(s)) => CString::new(s.as_bytes()).ok().map_or(std::ptr::null_mut(), |cs| unsafe { glib::ffi::g_strdup(cs.as_ptr()) as *mut c_void }), _ => std::ptr::null_mut(), }; unsafe { *(ret as *mut *mut c_void) = ptr }; }` — no `self.ownership` consultation. Call chain: trampoline.rs:167 `self.return_type.write_return_to_raw_ptr(result, &Ok(value))` after each JS callback completes; the C caller of a transfer-none callback does not free the result, and the native module retains no record of the dup.

  **Verifier:** Confirmed at every link of the chain. string.rs:79-89 (`write_return_to_raw_ptr`) ignores `self.ownership` and unconditionally `g_strdup`s the JS return, while the same codec does consult ownership in `encode` (line 35) and `decode` (line 63) — so the information is carried but unused on this path. trampoline.rs:167 invokes it after every JS callback; trampoline.rs holds no retained-return slot and `TrampolineState::destroy` (lines 103-107) frees only the boxed state, so the dup pointer is forgotten immediately. The trigger is reachable: node_modules/.gtkx/gi/glib/glib.js lines 2034 and 2059 declare `t.trampoline([t.string("borrowed"), t.uint64], t.string("borrowed"), { hasDestroy: true, ... })` for g_option_context/group_set_translate_func, exported as `OptionContext.setTranslateFunc` (line 9078) and `OptionGroup.setTranslateFunc` (line 9166). GLib's goption.c uses GTranslateFunc results inline (TRANSLATE macro appended into a GString) and never frees them per the transfer-none contract, so each invocation leaks one g_strdup allocation. Refutation attempts fail: duplication is required for memory safety (the CString backing is temporary), but a sound non-leaking variant exists (retain the dup in TrampolineState, free on next call or in destroy — valid for a callee-owned transfer-none return), so the leak is not forced; the path cannot run under cargo test (no JS runtime, module is coverage(off)), so ASan/Miri never see it. One overstatement in the finding: GTranslateFunc fires during option parsing/help generation, not per displayed UI string, so leak volume for the only currently generated instances is a small per-process burst — consistent with the low severity, which is correct.

  </details>

- [x] **RS-039** · `src/types/void.rs:13` · logic-bug — **Void is accepted in argument position, emitting a void libffi arg type and a dangling unit pointer**

  Type::can_be_return_type rejects Trampoline/Ref in the return slot, but no counterpart rejects Void in argument position. VoidType::libffi_type returns libffi::Type::void() and its encode silently swallows any value into FfiValue::Ptr(null). A void-typed argument therefore flows into the CIF builder (module/call.rs:52-60) — ffi_prep_cif with a void argument type is outside libffi's API contract (argument types must be non-void) — and FfiValue::Void's libffi arg is `libffi::arg(&())` (ffi/value.rs:184), a pointer to a zero-sized temporary that libffi may read through. This is not hypothetical: the generated harfbuzz bindings pass `{ type: t.void }` as a real argument (node_modules/.gtkx/gi/harfbuzz/harfbuzz.js lines 611, 615, 635, ..., e.g. hb_font_funcs_set_font_h_extents_func), where an unresolvable callback type degraded to void, so the C function is invoked through a CIF whose argument classification is undefined and which omits the function's remaining parameters. A descriptor-boundary check mirroring can_be_return_type would turn this into a precise InvalidArg error.

  <details><summary>Evidence and verification</summary>

  **Evidence:** void.rs:8-15: `impl FfiEncoder for VoidType { fn encode(&self, _value: &value::Value, _optional: bool) -> anyhow::Result<ffi::FfiValue> { Ok(ffi::FfiValue::Ptr(std::ptr::null_mut())) } fn libffi_type(&self) -> libffi::Type { libffi::Type::void() } ... }`. Call sequence: harfbuzz.js:609-612 `t.fn(..., [{ type: t.boxed("hb_font_funcs_t", ...) }, { type: t.void }], t.void)` → module/call.rs:52-55 `for arg in &self.args { arg.ty.append_ffi_arg_types(&mut arg_types); }` pushes Type::void() as an argument type → libffi::Builder::into_cif → low::prep_cif on a signature libffi does not define. types.rs:362-365 shows the return-slot guard (`can_be_return_type`) with no argument-position analog.

  **Verifier:** Verified against the code. (1) The asymmetry is real: types.rs:363-365 (`can_be_return_type`) rejects Trampoline/Ref only in the return slot (checked at module/call.rs:150), while Arg::from_js_value (arg.rs:44-62) and CallRequest::execute perform no argument-position check, so Type::Void passes through. (2) The CIF corruption is real: call.rs:52-55 pushes `VoidType::libffi_type()` = `libffi::Type::void()` (void.rs:13-15) into the argument type vector, and `ffi_prep_cif` accepts it silently (ffi_type_void has size 1), even though libffi's documented contract states ffi_type_void cannot be used for argument types — on x86-64 the void slot classifies as NO_CLASS and consumes no register, so the callee's remaining parameters (func, user_data, destroy) read stale register garbage: genuine UB at the call boundary. (3) The trigger is concrete and shipped: node_modules/.gtkx/gi/harfbuzz/harfbuzz.js lines 609-615 and 633-640 pass `{ type: t.void }` as a real second argument to hb_font_funcs_set_font_h_extents_func, hb_font_funcs_set_font_v_extents_func, hb_font_funcs_set_glyph_h_advance_func, etc., and t.fn (packages/ffi/src/helpers.ts:44-62) forwards descriptors to nativeCall unfiltered, so any JS call to these exported bindings reaches the broken CIF. One mechanism detail in the finding is inaccurate: VoidType::encode returns FfiValue::Ptr(null) (void.rs:9-11), so the emitted libffi::Arg is a pointer to a stored null-pointer slot via ffi/value.rs:179, not the `libffi::arg(&())` zero-sized-temporary arm at ffi/value.rs:184 (that arm is unreachable from argument encoding). This does not refute the finding: the defect is the void argument type in the CIF, the trigger path is real, the suggested descriptor-boundary InvalidArg check mirrors the crate's existing return-slot validation, and no CI tool (clippy/ASan/Miri) exercises the harfbuzz path. Low severity as a missing-validation logic bug is a fair rating.

  </details>

- [x] **RS-040** · `src/value.rs:285` · logic-bug — **Value::from_js_value recurses without depth or cycle protection; cyclic JS input overflows the stack and aborts the process**

  Value::from_js_value recurses structurally: arrays recurse per element via map_js_array, and every non-array object is treated as a Ref whose `value` property is converted recursively (Ref::from_js_value -> Value::from_js_value). There is no depth limit and no cycle detection. A cyclic argument — `const a = []; a[0] = a;` passed as any array-typed argument value, or `const o = {}; o.value = o;` passed where a Ref is accepted — recurses until the native stack overflows. A stack overflow in native code aborts the whole Node.js process (it is not a catchable JS exception), so a single malformed argument from application code crashes the app instead of producing an InvalidArg error.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/value.rs:285-292: `ValueType::Object => { if value.is_array()? { let arr: Array = unsafe { Array::from_napi_value(env.raw(), value.raw())? }; Ok(Self::Array(map_js_array(env, &arr, Self::from_js_value)?)) } else { let r = Ref::from_js_value(env, value)?; Ok(Self::Ref(r)) } }` and src/value.rs:190-197 (Ref::from_js_value): `let obj: JsObject = unsafe { JsObject::from_raw_unchecked(env.raw(), value.raw()) }; let value_prop: Unknown<'_> = obj.get_named_property("value")?; let inner = Value::from_js_value(env, value_prop)?;`. Recursion chain: from_js_value(o) -> Ref::from_js_value(o) -> from_js_value(o.value === o) -> ... unbounded; likewise from_js_value(a) -> map_js_array -> from_js_value(a[0] === a) -> ... unbounded. No recursion bound exists anywhere on the path, so the process aborts with SIGSEGV/stack-overflow on cyclic input.

  **Verifier:** The claim is factually accurate. In /home/eugenio/gtkx/packages/native/src/value.rs, Value::from_js_value (line 285-292) handles ValueType::Object by either recursing per element through map_js_array(env, &arr, Self::from_js_value) (line 288; map_js_array at lines 355-372 has no depth tracking) or by calling Ref::from_js_value (line 290), which at lines 190-193 reads obj.get_named_property("value") and calls Value::from_js_value on it — mutual recursion with no bound. A grep over the crate confirms no depth limit or cycle detection exists anywhere on the conversion path (the only depth machinery is the unrelated dispatch callback/freeze depth in src/dispatch.rs). The path is reachable from application input: Arg::from_js_value (src/arg.rs:49) feeds argument values from the public call entry point into Value::from_js_value, and any plain object that is not an array/External/Function is unconditionally treated as a Ref. Cyclic input (a[0] = a, or o.value = o) therefore recurses until the native stack overflows. The failure mode is correctly characterized: each recursion level runs only native napi calls (napi_typeof, napi_is_array, data-property get, napi_create_reference) that execute no JavaScript, so V8's stack-limit check never fires; the Rust guard page is hit and the process aborts — not a catchable JS exception. Since the function already returns napi::Status::InvalidArg for unsupported value types (lines 294-297), its contract is to reject malformed input recoverably, and a process abort on cyclic input is a real robustness defect on an edge path, appropriately rated low. It is not something clippy, rustfmt, ASan, or Miri on default test paths would catch.

  </details>

- [x] **RS-041** · `src/wait_signal.rs:1` · non-idiomatic — **wait_signal.rs and ffi/storage.rs lack the documentation the rest of the crate maintains**

  The crate's convention is thorough module-level doc comments plus doc comments on public items (dispatch.rs, toggle_ref.rs, managed.rs, etc. all carry detailed contracts). wait_signal.rs has zero documentation — no module doc and no item docs on WaitSignal, notify, or wait — despite the type's semantics being subtle (a consumed binary signal where wait clears the flag, single-waiter assumption via notify_one, lost-wakeup tolerance through the boolean). ffi/storage.rs similarly exposes FfiStorage, FfiStorageKind, and seven public *Data structs with should_free/elements_duped ownership flags whose meaning is documented nowhere, even though those flags decide which Drop path frees GLib memory. These are exactly the items whose contracts other modules depend on for soundness.

  <details><summary>Evidence and verification</summary>

  **Evidence:** src/wait_signal.rs:1-48 contains no `//!` or `///` at all: `pub struct WaitSignal { state: Mutex<bool>, condvar: Condvar }` with `pub fn notify(&self)` and `pub fn wait(&self)` undocumented, while the consumer doc in src/dispatch.rs:241-247 has to explain WaitSignal's wake semantics on the caller side instead. src/ffi/storage.rs:7-87: `pub struct FfiStorage`, `pub struct GListData { pub handles: ..., pub list_ptr: ..., pub should_free: bool }`, `pub struct StringGListData { ..., pub elements_duped: bool }`, and `pub enum FfiStorageKind` are all bare, though should_free/elements_duped select between g_list_free_full and g_list_free in Drop (lines 194-243).

  **Verifier:** Every evidence claim verifies against the current code. src/wait_signal.rs (48 lines) has zero `//!`/`///` comments; its semantics are subtle — `wait` consumes the flag (`*notified = false;` line 46), `notify` uses `notify_one` (line 32) implying a single-waiter design, and poisoning is swallowed via `PoisonError::into_inner` — and the wake contract is instead documented at the consumer in dispatch.rs:241-247 on `notify_js`, exactly as claimed. src/ffi/storage.rs exposes `FfiStorage`, `FfiStorageKind`, and seven public `*Data` structs whose `pub should_free` / `pub elements_duped` fields are undocumented yet directly select between `g_list_free_full`/`g_list_free` and gate every unsafe GLib free in `Drop` (lines 194-274); the file's only doc comment is on the private `free_string_list` helper. The convention claim holds for the named peers: toggle_ref.rs has 80 module-doc + 124 item-doc lines, dispatch.rs 40/51, managed.rs 32/61, types.rs 36/23, value.rs 18/23 — the crate's ownership/threading-sensitive modules carry detailed written contracts, and these two files hold the same kind of contract with none. No tooling enforces this: Cargo.toml enables clippy pedantic/nursery but allows missing_errors_doc/missing_panics_doc, and neither rustc missing_docs nor missing_docs_in_private_items is on, so cargo clippy -D warnings cannot catch it. Minor caveat: a few small codec files (types/string.rs, types/trampoline.rs, ffi/value.rs) also lack docs, so the convention is not perfectly uniform — but that does not refute the finding, since the comparable core modules cited are all heavily documented and the undocumented flags here decide unsafe free paths. Accurate, non-tooling-covered, soundness-relevant documentation gap; stands as low-severity non-idiomatic.

  </details>

## Tooling

- [x] **RS-T01** · `packages/native/package.json` — the `lint` script runs `cargo clippy -- -D warnings` without `--all-targets`, so benches and integration tests are never checked against the crate's `pedantic`/`nursery` lint levels. Add `--all-targets` (and fix whatever it surfaces) so the gap cannot reopen.

## Verified non-issues (14)

Claims raised during review and refuted with a concrete trace. Kept here so future audits do not re-report them; each entry notes the guarding invariant, which would become a real defect if the invariant is ever removed.

### NI-01 · `src/managed/fundamental.rs:46` · memory-leak — Fundamental takes a reference whenever ref_fn exists but can only release it when unref_fn exists

The (Some ref_fn, None unref_fn) state the finding depends on is unreachable from every descriptor producer in the system, so the claimed leak has no trigger. The only Rust construction sites of Fundamental are FundamentalType::wrap_ptr and ptr_to_value (packages/native/src/types/fundamental.rs:53-59, 101-110), fed by FundamentalFnCache::lookup, which maps an empty symbol string to None — so the half-empty pair requires a descriptor with a non-empty refFn and an empty unrefFn string. No producer creates one: (1) codegen emits t.fundamental only when both functions are present, with structural fallbacks otherwise — packages/codegen/src/writers/value.ts:291-293 (`if (cls.glibRefFunc === undefined || cls.glibUnrefFunc === undefined) return t.object(...)`), :323 (`cls.fundamental && cls.glibRefFunc !== undefined && cls.glibUnrefFunc !== undefined`), and :386 (`if (refFunc !== undefined && unrefFunc !== undefined)`), and the INTRINSIC_FUNDAMENTAL_FUNCS map (:251-253) carries complete pairs; (2) both hand-written descriptors pass complete pairs — packages/ffi/src/gobject/gvalue-native.ts:117 ("g_param_spec_ref"/"g_param_spec_unref") and :129 ("g_variant_ref"/"g_variant_unref"); (3) all tests use either both functions or neither (fundamental_codec.rs:34-42 and :135-143). Under this pairing invariant, `owned: ref_fn.is_some()` in fundamental.rs:50 is exactly equivalent to keying on unref_fn — the off-diagonal states never occur at runtime. The asymmetry of the state.rs:212-216 guard is principled, not an oversight half: (None, Some) would unref a reference never taken on Drop — over-release, premature destruction, use-after-free — a memory-safety hazard worth a hard bail; (Some, None) cannot cause UB under any input and would at worst leak under a descriptor that requires deliberately passing an empty unref symbol to a low-level FFI helper whose two required positional string parameters exist precisely to name the ref/unref pair of a ref-counted type. Triggering it needs either hand-crafted misuse (t.fundamental(lib, "x_ref", "")) or a GIR file declaring glib:ref-func with an empty glib:unref-func attribute — none exists in the repository, the generated output, or real GIR files. By the audit's own standard ("a finding without a concrete trigger scenario is not a finding"), this is at most a defense-in-depth validation suggestion, not a real memory leak.

### NI-02 · `src/types/ref_type.rs:248` · undefined-behavior — Ref<String> fixed-length buffer decode scans past the Vec when the callee fills it without a NUL

The mechanics quoted are accurate (encode at ref_type.rs:86-97 builds a zeroed Vec with NUL guaranteed only at encode time; decode_ref_string at ref_type.rs:247-250 runs an unbounded CStr::from_ptr over the Buffer storage). But the finding fails on both reachability and contract.

Reachability: the trigger requires a binding that declares t.ref(t.string(ownership, N)) for a callee that fills all N bytes without writing a NUL. No such binding exists or can be generated. The codegen pipeline never emits a length — packages/codegen/src/writers/value.ts:231 emits `t.string(${quote(ownership)})` unconditionally — so all generated bindings take the length-None path. Every native test uses `length: None`. The single consumer of the length knob in the entire repository is readInfoLog in packages/gl/src/gl.ts:205-215, binding glGetShaderInfoLog/glGetProgramInfoLog with maxLength = logLength = GL_INFO_LOG_LENGTH and a buffer of exactly logLength bytes. The OpenGL specification guarantees both functions return the log as a null-terminated string and that maxLength counts the terminator (GL_INFO_LOG_LENGTH likewise includes it), so a NUL is always written inside the Vec and CStr::from_ptr stays in bounds; the `logLength <= 0` guard at gl.ts:224 additionally skips the call when there is no log. The cited g_unichar_to_utf8 family is bound nowhere via this path — unichar has its own dedicated codec (src/types/unichar.rs) that never touches Ref<String>.

Contract: this crate is a raw FFI primitive layer in which every codec trusts the JS-supplied descriptor to model the callee faithfully; that is the crate's fundamental (and necessarily unsafe) caller contract. In this type system, "string" denotes a NUL-terminated C string, and Ref<String> with length denotes a caller-allocated buffer the callee fills with a NUL-terminated string — exactly the glGetShaderInfoLog shape it was built for. A fixed-size-buffer API that does not NUL-terminate does not produce a C string, and the descriptor system already provides the correct declaration for it: t.ref over t.fixedArray(t.uint8, N) or t.sizedArray. Declaring such an API as a string is a descriptor/callee mismatch — the same caller-side UB class the public call API admits everywhere (declaring t.string as the return of an int-returning function makes string.rs:60 CStr::from_ptr scan from a garbage pointer, a strictly worse outcome reachable from the same public surface). The finding's premise, "the codec never enforces the callee's contract," would condemn every codec in src/types/; enforcement of callee behavior is impossible at this layer and is delegated to binding declarations by design. Using CStr::from_bytes_until_nul over the retained Vec would be cheap defense-in-depth, but a hardening opportunity without a reachable trigger is not a defect. Per the verification standard (default to false when the trigger is unreachable), the finding is refuted.

### NI-03 · `src/types.rs:234` · undefined-behavior — Plain aligned dereferences in the default RawPtrCodec, BooleanType, and UnicharType on JS-controlled field addresses contradict the codec family's documented no-alignment contract

Every reachable path into the cited plain dereferences satisfies the aligned precondition, and the finding's premises fail on inspection. (1) Trampoline paths: BooleanType/UnicharType read_from_raw_ptr at trampoline.rs:136 receive libffi closure argument pointers, which libffi supplies suitably aligned for the declared ffi_type (built from ty.libffi_type()); write_return_to_raw_ptr at trampoline.rs:167/174 writes into `result: &mut u64` (trampoline.rs:244), an 8-aligned slot; inout cell pointers are addresses of real C variables, aligned per the C ABI. (2) Field paths: the finding's trigger hypothesis ('misdeclared GIR offset' or 'packed struct field') is structurally impossible because codegen never forwards GIR offsets to the native read/write exports — it computes its own ABI layout in packages/codegen/src/gir/size.ts, where computeFieldSlots executes `cursor = roundUp(cursor, align)` (line 104) before every slot, so every emitted offset is a multiple of the field type's natural alignment on a heap base pointer; a packed GIR struct would yield aligned-but-wrong offsets (wrong data, not UB). UnicharType has no field path at all: isAccessorEligibleType (boxed-field-accessor.ts:71) excludes 'unichar', so no unichar field accessor is ever generated; hand-written callers (ffi/src/cairo.ts, value-marshal.ts) use fixed 4/8-multiple offsets. (3) The only residual trigger is JS deliberately calling the raw `read(handle, {type:'boolean'}, 3)` export with a misaligned offset — but that export is a trusted-bindings peek/poke primitive whose validity contract already rests on the caller (the same caller can pass an out-of-bounds offset and read unmapped memory, which no read_unaligned conversion mitigates); alignment is a strict subset of the validity precondition the caller must already uphold. The register_class contrast does not hold: there the native side has a size oracle (g_type_query yields class_size, register_class.rs:264-271) so validation is meaningful and complete; field.rs has no allocation-size oracle and cannot validate bounds at all. (4) The 'documented no-alignment contract' premise overstates: the cited comment (raw_ptr.rs:30-36) documents one helper's own behavior, and the RawPtrCodec trait docs (types.rs:227-256) promise nothing about alignment, so no family-wide contract is contradicted. What remains is a uniformity/hardening preference (three derefs that are aligned on every reachable path could use unaligned accessors for symmetry) with no concrete trigger inside the system's contract — per the audit rules, not a finding.

### NI-04 · `src/trampoline.rs:215` · undefined-behavior — Trampoline out-parameter flush reads an uninitialized out slot and unrefs/frees the garbage pointer it finds

The finding accurately quotes the Rust code but its trigger is unreachable through every producer of trampoline descriptors. (1) The headline scenario — a registered-class vfunc with a `GObject **out` parameter — is structurally impossible: isVtableSlotEligible (packages/codegen/src/writers/class-struct.ts:64) rejects any vfunc slot with a non-caller-allocated out/inout parameter ("if ((param.direction === \"out\" || param.direction === \"inout\") && !param.callerAllocates) return false;"), so such vfuncs are omitted from the vtable metadata and registerClass never installs a trampoline for them; no vfunc trampoline ever carries a Type::Ref argument. (2) Signal trampolines (signal.ts:529) wrap pure outs and scalar inouts in t.ref, but an exhaustive scan of every GIR file on the system (Gtk-4.0, Adw-1, GtkSource-5, WebKit-6.0, Gio, GLib, Pango, GdkPixbuf, HarfBuzz, Soup, etc.) finds zero signals with a non-scalar pure out parameter; signal Refs are exclusively scalars, and the scalar write_value_to_raw_ptr impls (numeric.rs:170-183, boolean.rs:60-66) write through the slot without reading or freeing the prior value, so no garbage pointer is ever released. (3) Pointer-typed inouts never marshal as Ref: isCellInout (value.ts:160-161) requires isScalarRef, and object/boxed inouts pass as in-place handles (passesHandleInPlace/isBoxedInout), refuting the transfer-none-inout over-unref sub-claim. (4) The single pointer-typed pure out in the whole corpus is GdkPixbuf.PixbufSaveFunc's GError** (generated as t.ref(t.boxed("GError", ...)), confirmed at node_modules/.gtkx/gi/gdkpixbuf/gdkpixbuf.js:325). That slot is never uninitialized: the GTKX runtime allocates it NULL-initialized via RefType::null_ptr_storage (ref_type.rs:232-240) when calling gdk_pixbuf_save_to_callbackv, and the GLib GError** contract mandates NULL-initialized storage (g_set_error dereferences *err). BoxedType::write_value_to_raw_ptr (boxed.rs:208-224) therefore reads NULL or a valid owned GError written by a previous flush, and g_boxed_free on the latter is the correct cleanup across repeated chunked invocations; a Null cell writes NULL (Value::object_ptr maps Null to a null pointer, value.rs:233-246) and frees nothing. (5) Residual inner types are inert: ArrayType/HashTableType/UnicharType fall through to the RawPtrCodec default which bails with an error report (types.rs:252-255), StringType writes a fresh g_strdup without reading the old slot (string.rs:91-104), and StructType-with-size is never produced as a signal/callback pure out. No hand-written trampoline descriptors with Ref args exist anywhere (grep across ffi/templates/react/gl/testing). Every path that could exercise the flush on a pointer-typed slot either does not exist, writes scalars blindly, or operates on a slot that the GLib convention and the runtime's own allocation guarantee is initialized — so no interleaving hands uninitialized bytes to g_object_unref, g_boxed_free, unref_fn, or copy_nonoverlapping.

### NI-05 · `src/types/trampoline.rs:94` · undefined-behavior — Trampoline CIF arity and pushed libffi args derive from independent fields (hasDestroy vs scope); mismatched descriptors shift arguments, read out of bounds, or panic the GLib thread

The arity divergence exists exactly as described (CIF slots keyed on has_destroy in types/trampoline.rs:94-100; pushed args keyed on scope-derived destroy_ptr in ffi/value.rs:150-158; no validation in from_js_value, t.trampoline, or codegen), but the claimed undefined behavior is unreachable. Every call in the crate goes through libffi::middle::Cif::call — the default FfiEncoder::call_cif (types.rs:194-203) and all overrides (numeric.rs:90, 332-333); grep shows zero low::call/raw::ffi_call sites — and the pinned libffi 5.1.0 (Cargo.toml `libffi = "5.1.0"`, locked in Cargo.lock) asserts `assert_eq!(self.cif.nargs as usize, args.len(), "Cif::call: passed wrong number of arguments")` (middle/mod.rs:250-254) before any foreign call. Any single mismatched trampoline shifts the total count by ±1, so both mismatch directions deterministically trip the assert; the argument shifting, out-of-bounds read, and wild-GDestroyNotify scenarios never execute. The panic is contained: dispatch_pending wraps every GLib task in catch_unwind (dispatch.rs:296-301) and reports the assert message to JS as an exception via NativeErrorReporter (error_reporter.rs:68-75); the GLib thread survives (no panic=abort profile exists), and the caller's GlibDisconnectedError is accompanied by the real panic text. Even the leak claim fails for the headline combo: with scope:"call" + hasDestroy:true the TrampolineState is retained in TrampolineValue::_owned_state (trampoline.rs:160-167) and is dropped during unwind — only notified/async/forever variants leak one state per malformed call. Reachability is nil under the API contract: codegen can never emit the notified+hasDestroy:false polarity (GIR scope="notified" implies a destroy param → destroyIndex → hasDestroy:true; omitted scope is derived from hasDestroy, trampoline.rs:72-78), and the async+destroy GIR case produces a CIF faithful to the real 3-slot C signature with a contained assert panic — a functional failure of one binding, not memory unsafety. A caller deliberately lying in a descriptor is outside the trust contract under which this FFI surface can be sound at all (the same caller gets direct UB by declaring t.int32 for a pointer parameter). What remains is a low-severity robustness gap (missing descriptor validation yielding a panic-shaped error plus a small leak), not the high-severity UB finding under verification.

### NI-06 · `src/types/boxed.rs:140` · use-after-free — Transfer-full encode of structs and non-copyable boxed values passes a JS-owned pointer without relinquishing ownership, causing double free

The degradation branch the finding points at exists in the code, but every concrete trigger it proposes is unreachable, and the descriptor-generation layer is the contract that guarantees encode's preconditions.

1. The freeFn trigger (cairo_path_t) never reaches encode. The only descriptor carrying a freeFn in the entire system is PATH_STRUCT_T (/home/eugenio/gtkx/packages/ffi/src/cairo.ts:85). Grep over the codegen templates and the generated store (node_modules/.gtkx/gi) shows it appears exclusively in RETURN position of three bindings: `cairo_copy_path`, `cairo_copy_path_flat`, `cairo_mesh_pattern_get_path`. `FfiEncoder::encode`/`ref_for_transfer` run only for argument marshalling; freeFn descriptors therefore only ever flow through decode (which fails loudly, as the finding itself notes). No transfer-full freeFn in-argument exists anywhere.

2. Generated transfer-full boxed arguments always carry a resolvable get-type. boxedExpression in /home/eugenio/gtkx/packages/codegen/src/writers/value.ts:386-396 emits `t.boxed(...)` only when `boxed.glibGetType !== undefined`, always with the owning namespace's shared library; records with ref/copy pairs become `t.fundamental`, records without get-type become `t.struct`. An exhaustive dump of every `t.boxed(..., "full")` in the generated store confirms each carries a library list plus getTypeFn.

3. The gtype-resolution-failure path cannot fire for any instance JS can actually possess. To pass an owned boxed X transfer-full, JS must first have obtained X from a call into X's own shared library — which the same thread-local LibraryCache loaded and cached (state.rs:85-91). LibraryCache::load iterates the comma-separated soname list (state.rs:96) with RTLD_NOW|RTLD_GLOBAL, and the GIR-declared get-type symbol is exported by that exact library (verified with nm: g_error_get_type, g_option_group_get_type, g_source_get_type, g_date_time_get_type are all in libgobject-2.0.so.0, the first list entry). So at encode time `self.gtype()` succeeds, `g_boxed_copy` runs, the callee receives its own copy and the JS wrapper keeps the original — precisely the "intended pattern" the finding claims these paths cannot honor. Core libraries cannot fail to dlopen in-process (the native module links them); an optional library (soup, webkit) failing to load means no instance of its boxed types can exist to be passed. A GIR scan of all relevant namespaces for in-parameters with transfer-ownership="full" of record type found that every one resolves to a record that carries a get-type (GError, GOptionGroup, GskTransform, GtkScrollInfo, GdkContentFormats, SoupCookie, ...) or to fundamentals; none lack a get-type.

4. StructType::encode's missing ownership branch has no trigger. `t.struct("full")` never appears as a directly passed argument in the generated store — only inside `t.ref(...)` out-parameter slots (the callee writes the slot; it never frees the seeded pointer) and as sized-array elements with elementSize, where ArrayType::encode (/home/eugenio/gtkx/packages/native/src/types/array.rs:580-596) packs by-value copies into a fresh Rust-owned buffer, so JS element wrappers are never handed over. All direct struct arguments are transfer-none, for which passing the raw pointer is correct, leaving ownership nothing to branch on.

5. The "only a logged warning" characterization is wrong: NativeErrorReporter::report (/home/eugenio/gtkx/packages/native/src/error_reporter.rs:60-75) raises the resolution error as an uncaught JavaScript exception through a threadsafe function, so the hypothetical failure would be loudly visible.

The only way to reach the claimed double free is a hand-crafted descriptor that misdescribes the C API (a freeFn/full boxed in argument position, or a misnamed getTypeFn for a type whose instances somehow exist anyway). The descriptor layer of an FFI runtime is inherently trusted — a wrong elementSize, ownership, or type tag corrupts memory identically — so that is a caller contract violation, not a crate defect. At most one could argue encode should bail defensively when a full-transfer copy is impossible, which is a low-severity hardening suggestion, not the medium use-after-free/double-free defect claimed.

### NI-07 · `src/types/array.rs:275` · memory-leak — encode_handles error paths leak per-element refs/copies already taken via ref_for_transfer and partially built lists

The leak-shaped code exists, but no reachable execution can take a per-element ref and then hit an error path. (1) The `?` on ref_for_transfer cannot fail mid-loop: GObjectType::ref_for_transfer (gobject.rs:103-112) and BoxedType::ref_for_transfer (boxed.rs:155-165) are structurally infallible (always Ok; gtype() is an Option, not a Result), and FundamentalType::ref_for_transfer (fundamental.rs:81-89) can fail only via lookup_fns(), which is deterministic for the single shared item_type and cached on first success (state.rs:218-220) — so it can only fail on element 0, before any ref has been taken. (2) The null-pointer bails (array.rs:272, 327, 384, 682) are unreachable: every Value::Object producer guards null — tracked_gobject_value is reached only behind as_non_null_ptr (gobject.rs:127) or null_guarded (gobject.rs:140); all boxed_value callers are behind as_non_null_ptr/null_guarded (boxed.rs:170, 192, 257, 276); Fundamental wrapping is behind as_non_null_ptr (fundamental.rs:94) or null_guarded (fundamental.rs:103); alloc bails on null (alloc.rs:35-38) and is the only ModuleRequest returning a NativeHandle. JS cannot fabricate an External<NativeHandle> (napi-rs type-tags externals), value.rs:279 only round-trips module-created non-null pointers, and NativeHandle::ptr is immutable after construction. A JS null/undefined array element is rejected by extract_handles (array.rs:467-477), which collects the full Vec and short-circuits before the encoder loop runs — zero refs taken at that point. The finding's stated trigger ("full-ownership boxed array where element 2 carries a null pointer") is therefore unproducible: the loops' null checks are defensive dead code, and the encode_garray error unref (array.rs:750-752) covers the only genuinely fallible pre-ref path (element-0 fundamental lookup failure), where no element refs exist yet.

### NI-08 · `src/trampoline.rs:191` · undefined-behavior — seed_ref_cell reads scalar out-parameter slots that the signal emitter leaves uninitialized

The memory event the finding describes does occur: GTK 4.22.4's gtk_spin_button_update emits ::input with a pointer to an uninitialized `double val` (verified from the 4.22.4 source), the generated connect case wires it as t.ref(t.float64), and seed_ref_cell (trampoline.rs:191-205) reads the f64. But the finding is refuted on both of its harm claims. First, the claim that garbage is 'visible to user callbacks' is factually wrong: planTrampolineArgs (packages/codegen/src/writers/method.ts:589-601) drops every pure-out parameter from the handler call, so the generated invoke for 'input' is `const _result = handler(); args[1].value = _result[1];` — the seed is overwritten before any code can observe it. Seeds are only consumed for isCellInout parameters (value.ts:160-161), i.e. GIR direction="inout" scalars such as GtkEditable::insert-text's position, which the emitter initializes by contract; that inout case is exactly why scalar seeding exists, and RefType (ref_type.rs:13-16) carries no direction metadata, so the codegen layer — which knows direction — is the correct place for the distinction, and it already enforces it. Second, the 'UB in the abstract machine' claim has no abstract machine to occur in: the slot lives in a frame of libgtk-4.so.1, a separately compiled shared library resolved by symbol at runtime and reached through GLib signal dispatch plus a libffi closure; no compiler sees both sides and cross-language LTO is impossible by construction. Within the Rust AM the pointer is opaque foreign memory, and the accepted FFI operational semantics (the model Miri's native-call support implements) treats memory exposed to foreign code as holding arbitrary initialized bytes, making the read defined with a nondeterministic value; LLVM cannot synthesize poison for a load through an opaque pointer, and double has no trap representations on the supported targets. GLib's own g_cclosure_marshal_generic performs the identical read of the identical slot for every C-based binding (PyGObject, gjs), so the pattern is the GObject ecosystem's specified behavior for ::input. The doc comment at trampoline.rs:182-190 records the deliberate trade-off and excludes the genuinely unsound case (dereferencing a garbage pointer). With no observable misbehavior, no realizable miscompilation model, and the finding's visibility claim disproven by the generated code, no defect remains.

### NI-09 · `src/module/freeze.rs:26` · race-condition — Nested freeze defeats the callback-depth task isolation

The finding's mechanics are correctly read: if a freeze task were enqueued while Mailbox::callback_depth >= 1, a GLib thread parked in wait_for_node_result(depth) (js_bridge.rs:156-172, draining via dispatch_pending_from_depth(callback_depth)) would drain it, and run_freeze_loop (dispatch.rs:195-206) would then call dispatch_pending() = dispatch_pending_from_depth(0) inside the signal emission. But the trigger requires freeze() to run on the JS thread while a node-callback frame is on the JS stack, and that never happens.

1) callback_depth is mutated in exactly one place: js_bridge.rs:190/198, where process_node_pending brackets execute_callback with enter_callback/leave_callback. So depth >= 1 holds only while a JS signal handler is synchronously on the stack.

2) freeze() has exactly one caller in the entire workspace: prepareForCommit in /home/eugenio/gtkx/packages/react/src/host-config.ts:354-358. @gtkx/native is not app-facing API — .dependency-cruiser.cjs restricts it to ffi/react/codegen, and grep confirms @gtkx/ffi does not re-export freeze — so the contract is with the reconciler alone.

3) The reconciler can never run prepareForCommit inside a handler frame. The root is ConcurrentRoot (tag 1, render.tsx:124-135; same in testing/src/render.tsx:56), and the host config sets supportsMicrotasks: true with scheduleMicrotask: queueMicrotask (host-config.ts:244-245). In react-reconciler for React 19, all updates — including the DiscreteEventPriority/SyncLane updates this config forces — are processed via processRootScheduleInMicrotask; setState on a concurrent root never flushes synchronously. The only synchronous entry points are the reconciler's flushSync exports, and grep over packages/react, packages/testing, packages/e2e, and examples sources finds zero uses (the only matches are Gio.DBusConnection.flushSync, a generated D-Bus binding method, in built dist bundles).

4) Microtasks cannot fire while the handler frame is live. Handlers are invoked via raw napi_call_function (js_bridge.rs:281-290), which performs no microtask checkpoint; the checkpoint runs only when the outermost callback scope unwinds — on the TSFN-wake path after process_node_pending returns, and on the wait_for_glib_result path after the blocking native call returns to JS and that turn ends. In both cases leave_callback (js_bridge.rs:198) has already restored depth to 0 before the commit microtask runs, so the freeze task in push_glib_task (dispatch.rs:208-218) is always tagged depth 0.

5) A depth-0 freeze task is exactly what the isolation handles correctly: a GLib thread parked in wait_for_node_result(1) skips it (depth filter at dispatch.rs:290), so run_freeze_loop never starts inside an emission; the task waits until the emission unwinds and the HIGH_IDLE idle source (dispatch.rs:235-238) drains it at top level, where draining depth-0 tasks is the intended semantics. No deadlock arises in the interim because the JS thread parked in wait_for_glib_result keeps servicing node callbacks (js_bridge.rs:96-104). Nested freeze during a commit is additionally a no-op (only the outermost freeze schedules the loop, dispatch.rs:180-182, freeze.rs:21-23).

The scenario therefore postulates a caller ("flushSync inside a signal handler") that does not exist anywhere in the stack and that the renderer configuration structurally rules out. The depth-tagging invariant composes correctly with freeze for every commit the React layer can produce; the finding is refuted.

### NI-10 · `src/module/register_class.rs:361` · undefined-behavior — register_class truncates parent class/instance sizes to u16 after validating against u32

The finding's trigger is unreachable. The casts at /home/eugenio/gtkx/packages/native/src/module/register_class.rs:361-362 (`let class_size = query.class_size as u16; let instance_size = query.instance_size as u16;`) operate on values produced exclusively by g_type_query (query_parent_gtype, line 245), and the GObject type system guarantees those values always fit in u16. GTypeInfo — the sole deep-copied source of class_size/instance_size for every type ever registered — declares both fields as guint16 (/usr/include/glib-2.0/gobject/gtype.h:1129, 1140). All registration entry points funnel through it: g_type_register_static and g_type_register_fundamental take `const GTypeInfo *` directly (gtype.h:1461, 1480); g_type_register_dynamic gets its sizes via g_type_plugin_complete_type_info, which also fills a GTypeInfo (gtypeplugin.h:118-121); g_type_register_static_simple takes guint parameters but GLib rejects values above G_MAXUINT16 with g_return_val_if_fail before copying into a GTypeInfo, and GLib's internal TypeNode stores the sizes as guint16. GTypeQuery's `guint class_size; guint instance_size;` (gtype.h:488-494) are merely widened copies of those internal guint16 values. A parent type with class_size or instance_size above 65535 therefore cannot exist anywhere in the GType universe — not in GTK, not in any third-party library — because no GObject API can register one. The hypothesized chain (parent class_size 70000 → validate_vfunc_offset accepts offset 69992 → GTypeInfo.class_size wraps to 4464 → class_init_trampoline writes 64KB past the allocation) requires a GTypeQuery state that g_type_query can never produce; the validate-with-u32/register-with-u16 pairing is consistent on every reachable input because the u32 is definitionally a widened u16. The residual style suggestion (u16::try_from with a descriptive error) would guard against an impossible state, and the audit standard explicitly discounts findings without a concrete reachable trigger.

### NI-11 · `src/module/toggle_ref.rs:161` · memory-leak — set_wrapper dispatch-failure path pins the wrapper forever and its recovery comment describes an unreachable finalizer

The finding's central mechanism is incorrect. (1) The error path is reachable only after orchestrated shutdown: the only mark_stopped call site is src/module/stop.rs:44, and dispatch_to_glib_and_wait (src/dispatch/js_bridge.rs:66-105) fails only when schedule_glib drops the task because the mailbox is stopped (src/dispatch.rs:225-227) or the GLib thread is dying — so a set_wrapper dispatch failure occurs only while the runtime is terminating. (2) The claim that on_wrapper_finalize can never run is false: a strong napi_ref blocks GC-triggered finalization, but Node-API invokes all remaining finalize callbacks at environment teardown regardless of reference refcount (the napi_env destructor finalizes every tracked reference; this is why "basic finalizer" semantics exist — finalizers may run during environment teardown when JS can no longer execute). At teardown on_wrapper_finalize (src/module/toggle_ref.rs:46-58) runs with binding_addr == 0, Box::from_raw frees the FinalizeData, schedule_cleanup hits the is_stopped fence (src/toggle_ref.rs:351) and no-ops, and the reference structure is reclaimed by env reference cleanup — so the FinalizeData doc comment's zero-binding path (lines 30-35) is reachable, not dead. (3) Retaining the wrapper between stop() and process exit is the crate's explicit documented shutdown policy, not a leak: stop.rs:18-21 ("JS handles that GC after the mark-stopped fence are intentionally leaked ... running GLib finalizers after the main loop has exited can crash on libraries like WebKit") and toggle_ref.rs:208-210 (the statement that shutdown leaks the reference instead of touching a dead runtime). Recovery on the error path would be worse: napi_add_finalizer finalizers cannot be unregistered, so freeing FinalizeData early would hand the teardown finalizer a dangling pointer. (4) The `while count > 1` loop (src/module/toggle_ref.rs:155-157) never iterates on current Node internals (napi_add_finalizer creates the result ref at refcount 0), but that initial refcount is undocumented in the Node-API contract; normalizing to exactly 1 is a defensive invariant assertion, not evidence of a wrong API model. What survives is at most a bounded, intentional shutdown-window retention reclaimed at env teardown — not a permanent leak and not a defect.

### NI-12 · `src/module/field.rs:49` · undefined-behavior — read/write field offsets are unvalidated f64 casts: negative offsets silently clamp to 0 and oversized offsets are immediate pointer-arithmetic UB

The Rust semantics cited are correct (f64-to-usize `as` saturates; `ptr.add(usize::MAX)` breaches `add`'s safety contract), but the finding fails on contract and reachability. (1) The read/write shims (src/module/field.rs:113-121, 136-143) implement a raw peek/poke primitive whose soundness is delegated to the caller by design — the shim cannot know the allocation size (NativeHandle.size_hint is only a V8 GC hint, 0 for borrowed handles), so the dominant invalid class, a finite non-negative integral offset outside the allocation, is undetectable and equally UB; rejecting negative/non-finite input would not change the primitive's soundness class, just as nothing validates the base pointer, `registerClass` vfunc byteOffsets, or `call` type descriptors. (2) No reachable trigger exists: every offset passed to read/write in the repository is either a compile-time non-negative integer literal emitted by codegen from GIR struct layout (packages/codegen/src/writers/boxed-field-accessor.ts:125-298, boxed-constructor.ts:96-102) or loop-index arithmetic over JS array lengths and cairo-guaranteed non-negative counts (packages/ffi/src/cairo.ts:114-265, cairo templates). The cited trigger requires hand-calling the low-level export with contract-violating arguments — the same misuse class as passing a dangling handle, which no validation can catch. (3) f64 + saturating cast is the idiomatic napi-rs pattern for a JS number offset; napi u32 (ToUint32: -8 → 4294967288) would be worse and BigInt would tax the hot path. The finding is a defense-in-depth hardening suggestion against hypothetical misuse of a trusted primitive, not a defect with a concrete reachable trigger.

### NI-13 · `src/module/handler.rs:27` · logic-bug — Every dispatch-based export hangs the JS thread forever if called before init

The Rust-level mechanics are correctly described: ModuleRequest::dispatch (src/module/handler.rs:27-30) performs no init check; schedule_glib (src/dispatch.rs:224-239) queues the task (keeping the result Sender alive, so Disconnected never fires) onto a default main context nothing iterates pre-init; wait_for_glib_result (src/dispatch/js_bridge.rs:96-104) would park forever on wake_js, whose only notifiers run on the GLib thread or during stop. However, the claimed trigger does not exist. The package's sole entry point, packages/native/index.ts, calls native.init() at module evaluation (line 146) in the same module that defines every dispatch-based export, so no consumer can obtain call/alloc/read/write/registerClass/setWrapper/getWrapper/freeze before init has run and blocked until the GLib main loop proved liveness. init is not even exported, so there is no caller-driven ordering for "an ordering bug in the JS facade" to break. The package.json exports map (only "." and "./package.json") blocks require of the private native-binding.cjs, and the production bundler plugin (packages/cli/src/vite-plugins/native.ts) preserves the same module-evaluation ordering. The one reachable lifecycle edge, post-stop, is already handled cleanly via mark_stopped → dropped Sender → GlibDisconnectedError. The pre-init state is structurally unreachable through any supported path; the finding's stated exposure ("a direct consumer of @gtkx/native or an ordering bug in the JS facade") is factually wrong about the current code, making this a speculative hardening suggestion without a concrete trigger, not a reportable defect.

### NI-14 · `src/types/string.rs:91` · memory-leak — StringType::write_value_to_raw_ptr overwrites the slot without freeing the previous string, unlike every sibling pointer codec

Refuted. The finding's premise — that StringType is the lone outlier among sibling pointer codecs — is factually wrong, and the behavior it labels a bug is the only sound implementation. (1) The consistent rule across codecs is "free the previous occupant only when a type-identified destructor exists": GObjectType uses g_object_unref (src/types/gobject.rs:153-168), BoxedType uses g_boxed_free ONLY when the GType resolves and otherwise overwrites without freeing (src/types/boxed.rs:208-211), FundamentalType unrefs only when unref_fn is Some (src/types/fundamental.rs:129-133), and StructType — also a sibling pointer codec — never frees the old pointer (src/types/boxed.rs:290-307). A bare char* has no type-registered destructor, so StringType (src/types/string.rs:91-104) correctly sits with StructType. (2) Freeing the old string would be UB on the field-write path: the codec is stateless and the old char* in a C struct field may be a string literal, an interned string, or a slice of a larger allocation; g_free on it corrupts the heap. Codegen renders field descriptors with transfer "none" (packages/codegen/src/writers/boxed-field-accessor.ts:51), so ownership cannot gate a free. libgirepository's gi_field_info_set_field refuses string field writes entirely for this reason; GTKX's dup-in/never-free-old is strictly more capable and only orphans intermediate values on repeated JS writes (the final value is still released by g_boxed_free). (3) The trampoline trigger contradicts the documented contract in the same file: seed_ref_cell (src/trampoline.rs:182-190) states pointer-typed Ref cells may be uninitialized pure-out slots, so dereferencing the old pointer is unsound; RefType carries no direction/transfer info (src/types/ref_type.rs:13-16), so flush_out_cells cannot distinguish pure-out from inout, and reading-and-freeing would be UB on the designed pure-out case. On that case there is no leak — the g_strdup'd string written to a transfer-full out cell is owned and freed by the C caller. The "inout char** callback whose incoming string is callee-owned" scenario names no concrete API and is outside the design (the handler never sees the incoming string since pointer cells are seeded Null). The double-field-write orphan is a deliberate, forced trade-off — leak-on-edge-use versus heap corruption on a plausible one — not a defect.
