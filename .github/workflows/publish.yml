name: Publish

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ vars.TURBO_TEAM }}

jobs:
  build-native:
    permissions:
      contents: read
    strategy:
      matrix:
        include:
          - arch: x64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build native binary
        uses: ./.github/actions/run-in-container
        with:
          command: pnpm install --frozen-lockfile && pnpm --filter @gtkx/native native-build
          cache-cargo: "true"
          cache-pnpm: "true"

      - name: Upload native binary
        uses: actions/upload-artifact@v7
        with:
          name: native-linux-${{ matrix.arch }}-gnu
          path: packages/native-linux-${{ matrix.arch }}-gnu/index.node

  publish:
    needs: build-native
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download x64 binary
        uses: actions/download-artifact@v8
        with:
          name: native-linux-x64-gnu
          path: packages/native-linux-x64-gnu

      - name: Download arm64 binary
        uses: actions/download-artifact@v8
        with:
          name: native-linux-arm64-gnu
          path: packages/native-linux-arm64-gnu

      - name: Build and publish
        uses: ./.github/actions/run-in-container
        with:
          command: pnpm install --frozen-lockfile && pnpm build && pnpm -r publish --access public --no-git-checks
          cache-cargo: "true"
          cache-pnpm: "true"
          extra-env: -e NPM_CONFIG_PROVENANCE=true
