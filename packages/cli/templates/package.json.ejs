{
    "name": "<%= name %>",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "gtkx dev src/dev.tsx",
        "build": "gtkx build",
        "typecheck": "tsc --noEmit",
        "start": "node dist/bundle.js"<% if (testing === 'vitest') { %>,
        "test": "vitest"<% } %>
    },
    "gtkx": {
        "appId": "<%= appId %>"
    }
}
