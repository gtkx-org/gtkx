{
    "extends": "../../tsconfig.lib.json",
    "compilerOptions": {
        "rootDir": "${configDir}",
        "outDir": "${configDir}"
    },
    "include": ["${configDir}/binding.ts"],
    "exclude": []
}
