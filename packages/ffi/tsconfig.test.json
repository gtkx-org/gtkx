{
    "extends": "../../tsconfig.test.json",
    "compilerOptions": {
        "customConditions": ["source"]
    },
    "include": ["${configDir}/tests/**/*.ts", "${configDir}/scripts/**/*.ts"],
    "references": [
        {
            "path": "./tsconfig.lib.json"
        }
    ]
}
