import { defineConfig } from "@gtkx/config";

export default defineConfig({
    applicationId: "<%= applicationId %>",
    applicationIcon: "data/icons",
    deploy: {
        name: <%- sourceStringLiteral(displayName) %>,
        summary: "A Linux application built with GTKX",
        description: [
            <%- sourceStringLiteral(`${displayName} is built with GTKX. Replace this paragraph with a description of what your application does.`) %>,
        ],
        categories: ["Utility"],
    },
});
