import { defineConfig } from "@gtkx/config";

export default defineConfig({
    applicationId: "<%= applicationId %>",
    applicationIcon: "data/icons",
    deploy: {
        name: <%- sourceStringLiteral(displayName) %>,
        summary: "A GNOME application built with GTKX",
        description: [
            <%- sourceStringLiteral(`${displayName} is a GNOME application built with GTKX on Adwaita and GTK4, with native GObject widgets rendered from React. Replace this paragraph with a description of what your application does.`) %>,
        ],
        categories: ["Utility"],
    },
});
