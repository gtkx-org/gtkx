{
    "name": "<%= name %>",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "imports": {
        "#data/*": "./data/*"
    },
    "scripts": {
        "dev": "gtkx dev",
        "build": "gtkx build",
        "codegen": "gtkx codegen",
<% if (isTypescript) { -%>
        "typecheck": "gtkx codegen && tsc",
<% } -%>
        "start": "node dist/bundle.js"
    }
}
