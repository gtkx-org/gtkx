{
    "name": "hello-world",
    "version": "0.0.0",
    "private": true,
    "license": "MPL-2.0",
    "description": "Simple counter app demonstrating GTKX basics",
    "type": "module",
    "scripts": {
        "build": "gtkx build",
        "typecheck": "gtkx codegen && tsc -b --emitDeclarationOnly",
        "dev": "gtkx dev",
        "start": "node dist/bundle.js"
    },
    "dependencies": {
        "@gtkx/react": "workspace:*",
        "react": "catalog:"
    },
    "devDependencies": {
        "@gtkx/cli": "workspace:*",
        "@gtkx/config": "workspace:*"
    }
}
