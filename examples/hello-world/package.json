{
    "name": "hello-world",
    "version": "0.0.0",
    "private": true,
    "license": "MPL-2.0",
    "description": "Simple counter app demonstrating GTKX basics",
    "type": "module",
    "scripts": {
        "build": "gtkx build",
        "typecheck": "gtkx codegen && tsc -b --noEmit",
        "dev": "gtkx dev",
        "start": "node dist/bundle.js"
    },
    "dependencies": {
        "@gtkx/config": "workspace:*",
        "@gtkx/react": "workspace:*",
        "react": "^19.2.7"
    },
    "devDependencies": {
        "@gtkx/cli": "workspace:*",
        "@gtkx/testing": "workspace:*"
    }
}
